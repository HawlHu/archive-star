/* ExOS built-in XSH applications
 * Version: 6.4.0-dev-os84
 * Requires: exos.js 6.4.0-dev-os84
 * Requires: exos_advapi32.js 6.4.0-dev-os84
 * Requires: exos_media.js 6.4.0-dev-os84
 * Requires: exos_gdi32.js 6.4.0-dev-os84
 * Requires: exos_d3d11.js / exos_d3dx.js 6.4.0-dev-os84
 * Requires: exos_comctl32.js 6.4.0-dev-os84
 * Requires: exos_comdlg32.js 6.4.0-dev-os84
 * Requires: exos_wininet.js / exos_ws2_32.js / exos_ole32.js 6.4.0-dev-os84
 * Requires: exos_crypt32.js / exos_richedit.js / exos_webview2.js 6.4.0-dev-os84
 * Requires: exos_shell32.js 6.4.0-dev-os84
 * Requires: exos_uxtheme.js / exos_dwmapi.js / exos_winui.js 6.4.0-dev-os84
 * Image format: EXOS_XSH_PE_V1
 * Common Controls: EXOS_COMCTL32_V2
 * Shell API: EXOS_SHELL32_V1
 * V8-only client module.
 */
(function(global){
'use strict';
function deepFreeze(x){if(!x||typeof x!=='object'||Object.isFrozen(x))return x;Object.keys(x).forEach(function(k){deepFreeze(x[k]);});return Object.freeze(x);}
global.jplopsoft_EXOS_XSH_APPS=deepFreeze({"version":"6.4.0-dev-os84","format":"EXOS_XSH_APPS_V2_PE","imageFormat":"EXOS_XSH_PE_V1","commonControls":"EXOS_COMCTL32_V2","advapiApi":"EXOS_ADVAPI32_V2","gdiApi":"EXOS_GDI32_V1","d3d11Api":"EXOS_D3D11_V1","d3dxApi":"EXOS_D3DX_V1","shellApi":"EXOS_SHELL32_V1","sharedResources":"EXOS_SHARED_RES_V1","uxTheme":"EXOS_UXTHEME_V1","dwmApi":"EXOS_DWMAPI_V1","winUI":"EXOS_WINUI_V2","apps":{"calc":{"id":"calc","fileName":"calc.xsh","title":"小算盤","description":"ExOS Calculator (.xsh / responsive keypad / keyboard)","icon":"calc","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst hwnd=await user32.CreateWindow({\n  title:'小算盤',\n  icon:'calc',\n  width:410,\n  height:610\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',id:'root',\n  style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',overflow:'hidden'}\n});\nawait user32.CreateControl(hwnd,{\n  type:'div',id:'displayPane',parentId:'root',\n  style:{display:'flex',flexDirection:'column',gap:'2px',padding:'4px 4px 10px',margin:'0'}\n});\nawait user32.CreateControl(hwnd,{\n  type:'label',id:'expression',parentId:'displayPane',text:'',\n  style:{height:'22px',margin:'0',textAlign:'right',fontSize:'12px',color:'#64748b',overflow:'hidden',whiteSpace:'nowrap'}\n});\nawait user32.CreateControl(hwnd,{\n  type:'input',id:'display',parentId:'displayPane',text:'0',readOnly:true,\n  style:{width:'100%',height:'74px',margin:'0',padding:'8px 12px',fontSize:'34px',fontWeight:'600',textAlign:'right',boxSizing:'border-box',background:'#ffffff'}\n});\nawait user32.CreateControl(hwnd,{\n  type:'div',id:'grid',parentId:'root',\n  style:{display:'grid',gridTemplateColumns:'repeat(4,minmax(0,1fr))',gridTemplateRows:'repeat(6,minmax(48px,1fr))',gap:'6px',flex:'1',minHeight:'0',width:'100%',margin:'0',padding:'0'}\n});\n\nlet input='0',acc=null,op='',waiting=false,lastOperand=null;\nconst fmt=n=>{\n  if(!Number.isFinite(n))return'Error';\n  const v=parseFloat(Number(n).toPrecision(14));\n  return Object.is(v,-0)?'0':String(v);\n};\nconst value=()=>Number(input)||0;\nconst symbol=o=>o==='*'?'×':o==='/'?'÷':o==='-'?'−':o;\nconst bin=(a,b,o)=>o==='+'?a+b:o==='-'?a-b:o==='*'?a*b:o==='/'?(b===0?NaN:a/b):b;\nconst show=async()=>{\n  await user32.SetControlText('display',input);\n  await user32.SetControlText('expression',acc!==null&&op?`${fmt(acc)} ${symbol(op)}`:'');\n};\nconst clearEntry=()=>{input='0';waiting=false;};\nconst press=async a=>{\n  if(input==='Error'&&!['C','CE'].includes(a))clearEntry();\n  if(/^\\d$/.test(a)){\n    input=waiting?a:(input==='0'?a:input+a);\n    waiting=false;\n  }else if(a==='.'){\n    if(waiting){input='0.';waiting=false;}\n    else if(!input.includes('.'))input+='.';\n  }else if(a==='C'){\n    input='0';acc=null;op='';waiting=false;lastOperand=null;\n  }else if(a==='CE'){\n    clearEntry();\n  }else if(a==='BACK'){\n    if(!waiting&&input!=='Error')input=input.length>1?input.slice(0,-1):'0';\n  }else if(a==='±'){\n    input=fmt(-value());\n  }else if(a==='%'){\n    input=fmt(value()/100);waiting=true;\n  }else if(a==='√'){\n    const v=value();input=fmt(v<0?NaN:Math.sqrt(v));waiting=true;\n  }else if(a==='x²'){\n    const v=value();input=fmt(v*v);waiting=true;\n  }else if(a==='1/x'){\n    const v=value();input=fmt(v===0?NaN:1/v);waiting=true;\n  }else if(['+','-','*','/'].includes(a)){\n    if(acc===null)acc=value();\n    else if(op&&!waiting){acc=bin(acc,value(),op);input=fmt(acc);}\n    op=a;waiting=true;lastOperand=null;\n  }else if(a==='='){\n    if(op&&acc!==null){\n      const rhs=waiting&&lastOperand!==null?lastOperand:value();\n      input=fmt(bin(acc,rhs,op));\n      lastOperand=rhs;\n      acc=Number(input);\n      waiting=true;\n      op='';\n    }\n  }\n  await show();\n  await user32.FocusControl('display');\n};\n\nconst buttons=[\n  ['%','%'],['CE','CE'],['C','C'],['⌫','BACK'],\n  ['1/x','1/x'],['x²','x²'],['√x','√'],['÷','/'],\n  ['7','7'],['8','8'],['9','9'],['×','*'],\n  ['4','4'],['5','5'],['6','6'],['−','-'],\n  ['1','1'],['2','2'],['3','3'],['+','+'],\n  ['±','±'],['0','0'],['.','.'],['=','=']\n];\nfor(const [text,action] of buttons){\n  const id='k_'+action.replace(/[^a-z0-9]/gi,c=>'x'+c.charCodeAt(0));\n  await user32.CreateControl(hwnd,{\n    type:'button',id,parentId:'grid',text,\n    title:text,\n    style:{width:'100%',height:'100%',minWidth:'0',minHeight:'0',margin:'0',padding:'0',fontSize:action==='='?'22px':'18px',fontWeight:action==='='?'600':'400'}\n  });\n  user32.OnControl(id,()=>press(action));\n}\n\nuser32.OnControl('display',async e=>{\n  if(e.action!=='keydown')return;\n  const k=String(e.key||'');\n  if(/^\\d$/.test(k))return press(k);\n  if(k==='.'||k===',')return press('.');\n  if(k==='+'||k==='-'||k==='*'||k==='/')return press(k);\n  if(k==='Enter'||k==='=')return press('=');\n  if(k==='Backspace')return press('BACK');\n  if(k==='Escape')return press('C');\n  if(k==='%')return press('%');\n});\nawait show();\nawait user32.FocusControl('display');\n"},"calendar":{"id":"calendar","fileName":"calendar.xsh","title":"日曆","description":"ExOS Calendar (.xsh / responsive MonthCalendar)","icon":"calendar","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.MONTHCAL_CLASS,comctl32.STATUSCLASSNAME]});\nconst hwnd=await user32.CreateWindow({title:'日曆',icon:'calendar',width:500,height:600});\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',overflow:'hidden'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'topbar',parentId:'root',style:{display:'flex',alignItems:'center',gap:'8px',margin:'0 0 6px',padding:'0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'today',parentId:'topbar',text:'今天',style:{margin:'0',minWidth:'72px'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'headline',parentId:'topbar',text:'',style:{margin:'0',fontSize:'18px',fontWeight:'600',flex:'1'}});\nconst todayIso=(()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;})();\nawait comctl32.CreateMonthCalendar(hwnd,{id:'monthcal',parentId:'root',value:todayIso,style:{flex:'1',width:'100%',height:'100%',minHeight:'0',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'detail',parentId:'root',style:{display:'flex',alignItems:'center',gap:'12px',minHeight:'58px',margin:'6px 0 0',padding:'8px 10px',border:'1px solid #dbe3ec',borderRadius:'5px',background:'#ffffff'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'selectedDate',parentId:'detail',text:'',style:{margin:'0',fontSize:'16px',fontWeight:'600'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'selectedWeekday',parentId:'detail',text:'',style:{margin:'0',fontSize:'13px',color:'#64748b'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'status',parentId:'root',parts:[{text:'',width:0},{text:'ExOS MonthCalendar',width:150}],style:{width:'100%',margin:'6px 0 0'}});\nconst describe=async iso=>{\n  const [y,m,d]=String(iso).split('-').map(Number);\n  const date=new Date(y,m-1,d);\n  const week=['星期日','星期一','星期二','星期三','星期四','星期五','星期六'][date.getDay()];\n  const text=`${y} 年 ${m} 月 ${d} 日`;\n  await user32.SetControlText('headline',`${y} 年 ${m} 月`);\n  await user32.SetControlText('selectedDate',text);\n  await user32.SetControlText('selectedWeekday',week);\n  await comctl32.StatusBar_SetText('status',0,iso===todayIso?'今天':`${iso} ｜ ${week}`);\n};\ncomctl32.OnNotify('monthcal',async e=>{\n  if(e.code==='MCN_SELCHANGE')await describe(String(e.data&&e.data.value||todayIso));\n  if(e.code==='MCN_VIEWCHANGE'){\n    const iso=String(e.data&&e.data.month||'');\n    const p=iso.split('-');\n    if(p.length>=2)await user32.SetControlText('headline',`${Number(p[0])} 年 ${Number(p[1])} 月`);\n  }\n});\nuser32.OnControl('today',async e=>{\n  if(e.action!=='click')return;\n  await comctl32.MonthCal_SetCurSel('monthcal',todayIso);\n  await describe(todayIso);\n});\nawait describe(todayIso);\n"},"html_editor":{"id":"html_editor","fileName":"html_editor.xsh","title":"HTML 編輯器","description":"ExOS HTML Editor (.xsh / live preview / basic format / HTML5 template)","icon":"html","singleInstance":false,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst comdlg32=ExOS.LoadLibrary('comdlg32.dll');\nconst richedit=ExOS.LoadLibrary('msftedit.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.TOOLBARCLASSNAME,comctl32.STATUSCLASSNAME]});\n\nlet path=String(process.argv[1]||'');\nconst leaf=p=>{p=String(p||'').replace(/\\//g,'\\\\');const i=p.lastIndexOf('\\\\');return i>=0?p.substring(i+1):p;};\nconst parentPath=p=>{p=String(p||'').replace(/\\//g,'\\\\');const i=p.lastIndexOf('\\\\');return i>2?p.substring(0,i):'C:\\\\';};\nconst defaultDocument=()=>`<!doctype html>\\n<html lang=\"zh-Hant\">\\n<head>\\n  <meta charset=\"utf-8\">\\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\\n  <title>ExOS HTML</title>\\n</head>\\n<body>\\n  <h1>新文件</h1>\\n  <p>開始輸入內容…</p>\\n</body>\\n</html>`;\nconst bodyInner=source=>{const m=/<body\\b[^>]*>([\\s\\S]*?)<\\/body\\s*>/i.exec(String(source||''));return m?m[1]:String(source||'');};\nconst replaceBody=(source,inner)=>{source=String(source||'');inner=String(inner||'');if(/<body\\b[^>]*>[\\s\\S]*?<\\/body\\s*>/i.test(source))return source.replace(/(<body\\b[^>]*>)[\\s\\S]*?(<\\/body\\s*>)/i,'$1\\n'+inner+'\\n$2');if(/<\\/html\\s*>/i.test(source))return source.replace(/<\\/html\\s*>/i,'<body>\\n'+inner+'\\n</body>\\n</html>');return '<!doctype html>\\n<html lang=\"zh-Hant\">\\n<head><meta charset=\"utf-8\"><title>ExOS HTML</title></head>\\n<body>\\n'+inner+'\\n</body>\\n</html>';};\n\nlet src=path?await kernel32.ReadTextFile(path):defaultDocument();\nlet fileName=path?(leaf(path)||'index.html'):'未命名.html';\nlet dirty=false,mode='wysiwyg',pageZoom=1,history=[src],historyIndex=0,historyMute=false;\nconst hwnd=await user32.CreateWindow({title:'HTML 編輯器 - '+fileName,icon:'html',width:1240,height:800});\nawait uxtheme.SetWindowTheme(hwnd,'HtmlEditor','Document');\nawait dwmapi.DwmSetWindowAttribute(hwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',overflow:'hidden',background:'#f8fafc'}});\n\nawait comctl32.CreateToolbar(hwnd,{id:'filebar',parentId:'root',buttons:[\n  {id:'new',text:'新增',title:'Ctrl+N'},{id:'open',text:'開啟',title:'Ctrl+O'},{id:'save',text:'儲存',title:'Ctrl+S'},{id:'saveas',text:'另存新檔',title:'Ctrl+Shift+S'},{separator:true},\n  {id:'undo',text:'復原',title:'Ctrl+Z'},{id:'redo',text:'取消復原',title:'Ctrl+Y'},{separator:true},\n  {id:'find',text:'尋找'},{id:'replace',text:'取代'},{separator:true},\n  {id:'stats',text:'文件統計'},{id:'validate',text:'檢查 HTML'}\n],style:{width:'100%',margin:'0'}});\n\nawait user32.CreateControl(hwnd,{type:'div',id:'modebar',parentId:'root',style:{display:'flex',alignItems:'center',gap:'4px',padding:'6px 8px',margin:'0',background:'#eef2f7',borderTop:'1px solid #dbe3ec',borderBottom:'1px solid #cbd5e1'}});\nfor(const [id,text] of [['modeRich','所見即所得'],['modeSource','原始碼'],['modePreview','檢視']])await user32.CreateControl(hwnd,{type:'button',id,parentId:'modebar',text,style:{margin:'0',minWidth:'100px',fontWeight:id==='modeRich'?'700':'400',background:id==='modeRich'?'#ffffff':'#e2e8f0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'zoomOut',parentId:'modebar',text:'－',title:'縮小頁面',style:{margin:'0 0 0 auto',minWidth:'34px'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'zoomLabel',parentId:'modebar',text:'100%',style:{margin:'0',width:'48px',textAlign:'center',fontSize:'11px',color:'#475569'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'zoomIn',parentId:'modebar',text:'＋',title:'放大頁面',style:{margin:'0',minWidth:'34px'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'modeHelp',parentId:'modebar',text:'Word-like Rich Edit ｜ msftedit.dll',style:{margin:'0 0 0 8px',color:'#64748b',fontSize:'11px'}});\n\nawait user32.CreateControl(hwnd,{type:'div',id:'richRibbon',parentId:'root',style:{display:'flex',alignItems:'center',gap:'5px',padding:'7px 8px',margin:'0',background:'#ffffff',borderBottom:'1px solid #dbe3ec',flexWrap:'wrap'}});\nfor(const [id,text,title] of [\n  ['font','字型…','ChooseFont'],['color','字色…','ChooseColor'],['hilite','螢光筆','背景色'],['bold','B','粗體'],['italic','I','斜體'],['underline','U','底線'],['strike','S','刪除線'],\n  ['left','靠左',''],['center','置中',''],['right','靠右',''],['justify','左右對齊',''],['bullet','• 清單',''],['number','1. 清單',''],['outdent','減少縮排',''],['indent','增加縮排',''],\n  ['h1','標題 1',''],['h2','標題 2',''],['para','本文',''],['link','連結',''],['unlink','移除連結',''],['image','圖片',''],['table','表格',''],['hr','水平線',''],['clearfmt','清除格式','']\n])await user32.CreateControl(hwnd,{type:'button',id:'r_'+id,parentId:'richRibbon',text,title,style:{margin:'0',minWidth:(id==='bold'||id==='italic'||id==='underline'||id==='strike')?'34px':'58px',fontWeight:id==='bold'?'700':'400',fontStyle:id==='italic'?'italic':'normal'}});\n\nawait user32.CreateControl(hwnd,{type:'div',id:'sourceTools',parentId:'root',style:{display:'none',alignItems:'center',gap:'5px',padding:'6px 8px',margin:'0',background:'#fff',borderBottom:'1px solid #dbe3ec',flexWrap:'wrap'}});\nfor(const [id,text] of [['format','基本排版'],['boiler','HTML5 範本'],['minify','壓縮'],['wrap','切換換行']])await user32.CreateControl(hwnd,{type:'button',id:'s_'+id,parentId:'sourceTools',text,style:{margin:'0'}});\n\nawait user32.CreateControl(hwnd,{type:'div',id:'searchPanel',parentId:'root',style:{display:'none',alignItems:'center',gap:'6px',padding:'6px 8px',margin:'0',background:'#fff7ed',borderBottom:'1px solid #fed7aa'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'findText',parentId:'searchPanel',placeholder:'尋找 HTML',style:{flex:'1',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'replaceText',parentId:'searchPanel',placeholder:'取代為',style:{flex:'1',margin:'0',display:'none'}});\nfor(const [id,text] of [['findPrev','上一個'],['findNext','下一個'],['replaceOne','取代'],['replaceAll','全部取代'],['closeSearch','×']])await user32.CreateControl(hwnd,{type:'button',id,parentId:'searchPanel',text,style:{margin:'0',display:(id==='replaceOne'||id==='replaceAll')?'none':'block'}});\n\nawait user32.CreateControl(hwnd,{type:'div',id:'workspace',parentId:'root',style:{position:'relative',flex:'1',minHeight:'0',margin:'0',padding:'0',overflow:'hidden',background:'#dbe3ec'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'richPane',parentId:'workspace',style:{position:'absolute',inset:'0',display:'flex',alignItems:'flex-start',padding:'22px',boxSizing:'border-box',overflow:'auto',background:'#dbe3ec'}});\nawait richedit.CreateRichEdit(hwnd,{id:'rich',parentId:'richPane',html:bodyInner(src),spellcheck:true,style:{width:'794px',minHeight:'1123px',height:'max-content',margin:'0 auto',padding:'48px 54px',boxSizing:'border-box',background:'#ffffff',color:'#111827',fontFamily:'Segoe UI,\"Microsoft JhengHei\",sans-serif',fontSize:'14px',lineHeight:'1.65',border:'1px solid #cbd5e1',boxShadow:'0 5px 22px rgba(15,23,42,.16)',overflow:'visible'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'sourcePane',parentId:'workspace',style:{position:'absolute',inset:'0',display:'none',background:'#fff'}});\nawait user32.CreateControl(hwnd,{type:'textarea',id:'source',parentId:'sourcePane',text:src,spellcheck:false,style:{width:'100%',height:'100%',margin:'0',padding:'12px 14px',boxSizing:'border-box',resize:'none',border:'0',borderRadius:'0',fontFamily:'Consolas,monospace',fontSize:'13px',lineHeight:'1.5',whiteSpace:'pre',overflow:'auto'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'previewPane',parentId:'workspace',style:{position:'absolute',inset:'0',display:'none',padding:'12px',boxSizing:'border-box',background:'#e2e8f0'}});\nawait user32.CreateControl(hwnd,{type:'htmlpreview',id:'preview',parentId:'previewPane',html:src,style:{width:'100%',height:'100%',margin:'0',border:'1px solid #cbd5e1',background:'#fff'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'statusbar',parentId:'root',parts:[{text:path?'已載入':'新文件',width:0},{text:'所見即所得',width:130},{text:`${src.length} 字元`,width:120},{text:'HTML5 ｜ UTF-8',width:140}],style:{width:'100%',margin:'0'}});\n\nconst updateTitle=()=>user32.SetWindowText(hwnd,(dirty?'*':'')+'HTML 編輯器 - '+fileName);\nconst markDirty=async()=>{if(!dirty){dirty=true;await updateTitle();}await comctl32.StatusBar_SetText('statusbar',0,'尚未儲存');};\nconst pushHistory=value=>{value=String(value??'');if(historyMute||history[historyIndex]===value)return;history=history.slice(0,historyIndex+1);history.push(value);if(history.length>100)history.shift();historyIndex=history.length-1;};\nconst syncRichToSource=async()=>{const inner=await richedit.GetHTML('rich');const current=String(await user32.GetControlText('source'));const out=replaceBody(current,inner);historyMute=true;await user32.SetControlText('source',out);historyMute=false;src=out;return out;};\nconst syncSourceToRich=async()=>{src=String(await user32.GetControlText('source'));await richedit.SetHTML('rich',bodyInner(src));return src;};\nconst currentSource=async()=>mode==='wysiwyg'?await syncRichToSource():String(await user32.GetControlText('source'));\nconst updatePreview=async()=>{const text=await currentSource();await user32.SetControlProperty('preview','html',text);};\nconst updateStatus=async()=>{const text=String(await user32.GetControlText('source'));await comctl32.StatusBar_SetText('statusbar',1,mode==='wysiwyg'?'所見即所得':mode==='source'?'原始碼':'安全檢視');await comctl32.StatusBar_SetText('statusbar',2,`${text.length} 字元`);};\n\nconst applyPageZoom=async()=>{const pct=Math.round(pageZoom*100);await user32.SetControlStyle('rich',{transform:`scale(${pageZoom})`,transformOrigin:'top center',marginBottom:`${Math.max(0,(pageZoom-1)*1123)+22}px`});await user32.SetControlText('zoomLabel',pct+'%');};\nconst newDocument=async()=>{if(dirty&&!await user32.ConfirmBox('目前 HTML 尚未儲存，仍要建立新文件嗎？','HTML 編輯器'))return false;path='';fileName='未命名.html';src=defaultDocument();dirty=false;history=[src];historyIndex=0;historyMute=true;await user32.SetControlText('source',src);historyMute=false;await syncSourceToRich();await updatePreview();await updateTitle();await updateStatus();await comctl32.StatusBar_SetText('statusbar',0,'新 HTML 文件');await setMode('wysiwyg');return true;};\nconst chooseOpen=async()=>{const cwd=await kernel32.GetCurrentDirectory();return await comdlg32.GetOpenFileName({hwndOwner:hwnd,title:'開啟 HTML 文件',initialDir:path?parentPath(path):cwd,filter:[{name:'HTML 文件',pattern:'*.html;*.htm;*.xhtml'},{name:'所有檔案',pattern:'*.*'}]});};\nconst chooseSave=async()=>{const cwd=await kernel32.GetCurrentDirectory();return await comdlg32.GetSaveFileName({hwndOwner:hwnd,title:'HTML 另存新檔',initialDir:path?parentPath(path):cwd,file:path?leaf(path):'index.html',defaultExt:'html',overwritePrompt:true,filter:[{name:'HTML 文件',pattern:'*.html;*.htm'},{name:'所有檔案',pattern:'*.*'}]});};\nconst openFile=async()=>{if(dirty&&!await user32.ConfirmBox('目前 HTML 尚未儲存，仍要開啟其他文件嗎？','HTML 編輯器'))return false;const r=await chooseOpen();if(!r)return false;path=String(r.file||'');fileName=leaf(path)||'index.html';src=await kernel32.ReadTextFile(path);history=[src];historyIndex=0;historyMute=true;await user32.SetControlText('source',src);historyMute=false;await syncSourceToRich();await updatePreview();dirty=false;await updateTitle();await updateStatus();await comctl32.StatusBar_SetText('statusbar',0,'已開啟 ｜ '+path);return true;};\nconst save=async forceAs=>{if(mode==='wysiwyg')await syncRichToSource();if(forceAs||!path){const r=await chooseSave();if(!r)return false;path=String(r.file||'');fileName=leaf(path)||'index.html';}const text=String(await user32.GetControlText('source'));const bytes=await kernel32.WriteTextFile(path,text);src=text;dirty=false;pushHistory(text);await updateTitle();await updatePreview();await updateStatus();await comctl32.StatusBar_SetText('statusbar',0,`已儲存 ｜ ${bytes} bytes ｜ ${path}`);return true;};\n\nconst setMode=async next=>{if(next===mode)return;if(mode==='wysiwyg')await syncRichToSource();if(next==='wysiwyg')await syncSourceToRich();if(next==='preview')await updatePreview();mode=next;await user32.SetControlStyle('richPane',{display:mode==='wysiwyg'?'flex':'none'});await user32.SetControlStyle('sourcePane',{display:mode==='source'?'block':'none'});await user32.SetControlStyle('previewPane',{display:mode==='preview'?'block':'none'});await user32.SetControlStyle('richRibbon',{display:mode==='wysiwyg'?'flex':'none'});await user32.SetControlStyle('sourceTools',{display:mode==='source'?'flex':'none'});for(const [id,m] of [['modeRich','wysiwyg'],['modeSource','source'],['modePreview','preview']])await user32.SetControlStyle(id,{background:mode===m?'#ffffff':'#e2e8f0',fontWeight:mode===m?'700':'400'});await updateStatus();if(mode==='wysiwyg')await user32.FocusControl('rich');else if(mode==='source')await user32.FocusControl('source');};\n\nconst restoreHistory=async index=>{if(index<0||index>=history.length||index===historyIndex)return false;historyIndex=index;historyMute=true;await user32.SetControlText('source',history[index]);historyMute=false;src=history[index];await syncSourceToRich();await markDirty();await updatePreview();await updateStatus();return true;};\nconst undo=async()=>{if(mode==='wysiwyg'){await richedit.ExecCommand('rich','undo');await syncRichToSource();await markDirty();return;}await restoreHistory(historyIndex-1);};\nconst redo=async()=>{if(mode==='wysiwyg'){await richedit.ExecCommand('rich','redo');await syncRichToSource();await markDirty();return;}await restoreHistory(historyIndex+1);};\nconst richCmd=async(cmd,val)=>{await richedit.ExecCommand('rich',cmd,val);await syncRichToSource();pushHistory(String(await user32.GetControlText('source')));await markDirty();await updateStatus();await user32.FocusControl('rich');};\nconst rgbHex=r=>{const c=r.rgb||{r:0,g:0,b:0};return '#'+[c.r,c.g,c.b].map(x=>('0'+Number(x).toString(16)).slice(-2)).join('');};\nconst chooseFont=async()=>{const r=await comdlg32.ChooseFont({hwndOwner:hwnd,title:'字型',pointSize:12,logFont:{lfFaceName:'Segoe UI',lfWeight:400}});if(!r)return;await richCmd('fontname',r.faceName||r.logFont.lfFaceName);const pt=Number(r.pointSize)||12,sz=pt<=8?1:pt<=10?2:pt<=12?3:pt<=14?4:pt<=18?5:pt<=24?6:7;await richCmd('fontsize',sz);if(r.logFont&&r.logFont.lfUnderline)await richCmd('underline');if(r.logFont&&r.logFont.lfStrikeOut)await richCmd('strikethrough');};\nconst chooseColor=async background=>{const r=await comdlg32.ChooseColor({hwndOwner:hwnd,title:background?'螢光筆色彩':'字型色彩'});if(r)await richCmd(background?'hilitecolor':'forecolor',rgbHex(r));};\nconst insertLink=async()=>{const url=await user32.PromptBox('連結網址：','插入連結','https://');if(url===null||!String(url).trim())return;await richCmd('createlink',String(url).trim());};\nconst insertImage=async()=>{const f=await user32.PickImageDataUrl();if(!f)return;await richedit.InsertImage('rich',f.dataUrl);await syncRichToSource();await markDirty();};\nconst insertTable=async()=>{const rr=await user32.PromptBox('列數 (1-20)：','插入表格','3');if(rr===null)return;const cc=await user32.PromptBox('欄數 (1-12)：','插入表格','3');if(cc===null)return;const rows=Math.max(1,Math.min(20,parseInt(rr,10)||3)),cols=Math.max(1,Math.min(12,parseInt(cc,10)||3));let out='<table style=\"border-collapse:collapse;width:100%\"><tbody>';for(let r=0;r<rows;r++){out+='<tr>';for(let c=0;c<cols;c++)out+=(r===0?'<th>':'<td>')+(r===0?'欄位 '+(c+1):'內容')+(r===0?'</th>':'</td>');out+='</tr>';}out+='</tbody></table><p><br></p>';await richedit.InsertHTML('rich',out);await syncRichToSource();await markDirty();};\n\nconst showSearch=async replaceMode=>{if(mode!=='source')await setMode('source');await user32.SetControlStyle('searchPanel',{display:'flex'});await user32.SetControlStyle('replaceText',{display:replaceMode?'block':'none'});await user32.SetControlStyle('replaceOne',{display:replaceMode?'block':'none'});await user32.SetControlStyle('replaceAll',{display:replaceMode?'block':'none'});await user32.FocusControl('findText');};\nconst hideSearch=async()=>{await user32.SetControlStyle('searchPanel',{display:'none'});await user32.FocusControl('source');};\nconst findNext=async backwards=>{const needle=String(await user32.GetControlText('findText'));if(!needle)return false;const text=String(await user32.GetControlText('source'));const sel=await user32.GetControlProperty('source','selection')||{start:0,end:0};let at=backwards?text.lastIndexOf(needle,Math.max(0,sel.start-1)):text.indexOf(needle,Math.max(0,sel.end));if(at<0)at=backwards?text.lastIndexOf(needle):text.indexOf(needle);if(at<0){await comctl32.StatusBar_SetText('statusbar',0,`找不到「${needle}」`);return false;}await user32.SetControlProperty('source','selection',{start:at,end:at+needle.length});return true;};\nconst replaceOne=async()=>{const needle=String(await user32.GetControlText('findText'));if(!needle)return;const rep=String(await user32.GetControlText('replaceText'));const text=String(await user32.GetControlText('source'));const sel=await user32.GetControlProperty('source','selection')||{start:0,end:0};if(text.slice(sel.start,sel.end)!==needle){if(await findNext(false))return replaceOne();return;}await user32.InsertControlText('source',rep);await markDirty();};\nconst replaceAll=async()=>{const needle=String(await user32.GetControlText('findText'));if(!needle)return;const rep=String(await user32.GetControlText('replaceText'));const text=String(await user32.GetControlText('source'));const count=text.split(needle).length-1;if(!count)return;const out=text.split(needle).join(rep);await user32.SetControlText('source',out);pushHistory(out);await markDirty();await updateStatus();};\nconst validate=async()=>{const text=await currentSource();const tags=(text.match(/<\\/?[a-zA-Z][^>]*>/g)||[]).length;const hasBody=/<body\\b/i.test(text),hasTitle=/<title\\b[^>]*>[\\s\\S]*?<\\/title>/i.test(text);await user32.MessageBox(`標籤：${tags}\\n<body>：${hasBody?'有':'缺少'}\\n<title>：${hasTitle?'有':'缺少'}\\n\\n檢視模式會停用 script。`,'HTML 檢查');};\nconst stats=async()=>{const text=await currentSource();await user32.MessageBox(`字元：${text.length}\\nUTF-8 bytes：${new TextEncoder().encode(text).length}\\n標籤：${(text.match(/<\\/?[a-zA-Z][^>]*>/g)||[]).length}\\n圖片：${(text.match(/<img\\b/gi)||[]).length}\\n連結：${(text.match(/<a\\b/gi)||[]).length}`,'HTML 文件統計');};\nconst basicFormat=async()=>{let text=String(await user32.GetControlText('source')).replace(/>\\s*</g,'>\\n<');let depth=0,out=[];for(const raw of text.split('\\n')){const line=raw.trim();if(!line)continue;if(/^<\\//.test(line))depth=Math.max(0,depth-1);out.push('  '.repeat(depth)+line);if(/^<[^!/?][^>]*>$/.test(line)&&!/<\\//.test(line)&&!/^<(?:area|base|br|hr|img|input|link|meta)\\b/i.test(line)&&!/\\/>$/.test(line))depth++;}text=out.join('\\n');await user32.SetControlText('source',text);pushHistory(text);await markDirty();await updateStatus();};\n\ncomctl32.OnNotify('filebar',async e=>{if(e.code!=='TBN_CLICK')return;const b=e.data&&e.data.button;if(!b)return;switch(b.id){case'new':await newDocument();break;case'open':await openFile();break;case'save':await save(false);break;case'saveas':await save(true);break;case'undo':await undo();break;case'redo':await redo();break;case'find':await showSearch(false);break;case'replace':await showSearch(true);break;case'stats':await stats();break;case'validate':await validate();break;}});\nuser32.OnControl('modeRich',async e=>{if(e.action==='click')await setMode('wysiwyg');});user32.OnControl('modeSource',async e=>{if(e.action==='click')await setMode('source');});user32.OnControl('modePreview',async e=>{if(e.action==='click')await setMode('preview');});\nuser32.OnControl('zoomOut',async e=>{if(e.action==='click'){pageZoom=Math.max(.6,Math.round((pageZoom-.1)*10)/10);await applyPageZoom();}});user32.OnControl('zoomIn',async e=>{if(e.action==='click'){pageZoom=Math.min(1.6,Math.round((pageZoom+.1)*10)/10);await applyPageZoom();}});\nconst richButtons={r_bold:['bold'],r_italic:['italic'],r_underline:['underline'],r_strike:['strikethrough'],r_left:['justifyleft'],r_center:['justifycenter'],r_right:['justifyright'],r_justify:['justifyfull'],r_bullet:['insertunorderedlist'],r_number:['insertorderedlist'],r_outdent:['outdent'],r_indent:['indent'],r_h1:['formatblock','H1'],r_h2:['formatblock','H2'],r_para:['formatblock','P'],r_unlink:['unlink'],r_hr:['inserthorizontalrule'],r_clearfmt:['removeformat']};\nfor(const id of Object.keys(richButtons))user32.OnControl(id,async e=>{if(e.action==='click')await richCmd(...richButtons[id]);});\nuser32.OnControl('r_font',async e=>{if(e.action==='click')await chooseFont();});user32.OnControl('r_color',async e=>{if(e.action==='click')await chooseColor(false);});user32.OnControl('r_hilite',async e=>{if(e.action==='click')await chooseColor(true);});user32.OnControl('r_link',async e=>{if(e.action==='click')await insertLink();});user32.OnControl('r_image',async e=>{if(e.action==='click')await insertImage();});user32.OnControl('r_table',async e=>{if(e.action==='click')await insertTable();});\nuser32.OnControl('s_format',async e=>{if(e.action==='click')await basicFormat();});user32.OnControl('s_boiler',async e=>{if(e.action==='click'){const text=String(await user32.GetControlText('source'));if(!text.trim()||await user32.ConfirmBox('以 HTML5 範本取代目前內容？','HTML 編輯器')){await user32.SetControlText('source',defaultDocument());pushHistory(defaultDocument());await markDirty();await updateStatus();}}});user32.OnControl('s_minify',async e=>{if(e.action==='click'){let text=String(await user32.GetControlText('source')).replace(/<!--(?!\\[if)[\\s\\S]*?-->/g,'').replace(/>\\s+</g,'><').replace(/[ \\t]{2,}/g,' ').trim();await user32.SetControlText('source',text);pushHistory(text);await markDirty();await updateStatus();}});user32.OnControl('s_wrap',async e=>{if(e.action==='click'){const w=String(await user32.GetControlProperty('source','wrap')||'')==='soft';await user32.SetControlProperty('source','wrap',w?false:'soft');await user32.SetControlStyle('source',{whiteSpace:w?'pre':'pre-wrap',overflowX:w?'auto':'hidden'});}});\nuser32.OnControl('rich',async e=>{if(e.action==='input'){await markDirty();return;}if(e.action==='keydown'&&e.ctrlKey){const k=String(e.key||'').toLowerCase();if(k==='s')return save(!!e.shiftKey);if(k==='o')return openFile();if(k==='n')return newDocument();}});\nuser32.OnControl('source',async e=>{if(e.action==='input'){src=String(e.value??'');pushHistory(src);await markDirty();await updateStatus();return;}if(e.action==='keydown'&&e.ctrlKey){const k=String(e.key||'').toLowerCase();if(k==='s')return save(!!e.shiftKey);if(k==='o')return openFile();if(k==='n')return newDocument();if(k==='f')return showSearch(false);if(k==='h')return showSearch(true);if(k==='z')return undo();if(k==='y')return redo();}});\nuser32.OnControl('findPrev',async e=>{if(e.action==='click')await findNext(true);});user32.OnControl('findNext',async e=>{if(e.action==='click')await findNext(false);});user32.OnControl('replaceOne',async e=>{if(e.action==='click')await replaceOne();});user32.OnControl('replaceAll',async e=>{if(e.action==='click')await replaceAll();});user32.OnControl('closeSearch',async e=>{if(e.action==='click')await hideSearch();});\nawait updatePreview();await updateStatus();await applyPageZoom();await user32.FocusControl('rich');\n"},"explorer":{"id":"explorer","fileName":"explorer.xsh","title":"檔案總管","description":"ExOS File Explorer (.xsh / system locations / panes / shell32 / 1GiB upload)","icon":"explorer","singleInstance":false,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst shell32=ExOS.LoadLibrary('shell32.dll');\nconst advapi32=ExOS.LoadLibrary('advapi32.dll');\n/* XSH4 prebinds exes.dll as the read-only `exes` API object. */\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\nconst WinUI=ExOS.WinUI;\n\nawait comctl32.InitCommonControlsEx({\n  classes:[\n    comctl32.WC_LISTVIEW,\n    comctl32.WC_TREEVIEW,\n    comctl32.WC_HEADER,\n    comctl32.STATUSCLASSNAME\n  ]\n});\n\nconst explorerImages=\n  await comctl32.ImageList_Create({\n    id:'explorerImages',\n    width:32,\n    height:32,\n    flags:\n      comctl32.ILC_COLOR32|\n      comctl32.ILC_MASK\n  });\n\nconst IMG={\n  disk:await comctl32.ImageList_Add(\n    explorerImages,{icon:'disk'}\n  ),\n  folder:await comctl32.ImageList_Add(\n    explorerImages,{icon:'folder'}\n  ),\n  xsh:await comctl32.ImageList_Add(\n    explorerImages,{icon:'xsh'}\n  ),\n  csv:await comctl32.ImageList_Add(\n    explorerImages,{icon:'csv'}\n  ),\n  html:await comctl32.ImageList_Add(\n    explorerImages,{icon:'html'}\n  ),\n  txt:await comctl32.ImageList_Add(\n    explorerImages,{icon:'txt'}\n  ),\n  file:await comctl32.ImageList_Add(\n    explorerImages,{icon:'file'}\n  ),\n  computer:await comctl32.ImageList_Add(\n    explorerImages,{icon:'computer'}\n  ),\n  history:await comctl32.ImageList_Add(\n    explorerImages,{icon:'history'}\n  ),\n  desktop:await comctl32.ImageList_Add(\n    explorerImages,{icon:'desktop'}\n  ),\n  download:await comctl32.ImageList_Add(\n    explorerImages,{icon:'download'}\n  ),\n  trash:await comctl32.ImageList_Add(\n    explorerImages,{icon:'trash'}\n  ),\n  control:await comctl32.ImageList_Add(\n    explorerImages,{icon:'control'}\n  ),\n  topology:await comctl32.ImageList_Add(\n    explorerImages,{icon:'computer'}\n  ),\n  paint:await comctl32.ImageList_Add(\n    explorerImages,{icon:'image'}\n  ),\n  device:await comctl32.ImageList_Add(\n    explorerImages,{icon:'computer'}\n  ),\n  minesweeper:await comctl32.ImageList_Add(\n    explorerImages,{icon:'minesweeper'}\n  )\n};\n\nconst normalizePath=input=>{\n  let p=String(input||'C:\\\\')\n    .replace(/\\//g,'\\\\')\n    .replace(/\\\\+/g,'\\\\');\n\n  if(/^[a-z]:$/i.test(p))p+='\\\\';\n  if(!/^[a-z]:\\\\/i.test(p))p='C:\\\\';\n\n  p='C:'+p.substring(2);\n\n  if(p.length>3)p=p.replace(/\\\\+$/,'');\n  return p;\n};\n\nconst parentPath=p=>{\n  p=normalizePath(p);\n\n  if(/^C:\\\\$/i.test(p))return'C:\\\\';\n\n  const pos=p.lastIndexOf('\\\\');\n\n  if(pos<=2)return'C:\\\\';\n  return p.substring(0,pos);\n};\n\nconst joinPath=(base,name)=>{\n  base=normalizePath(base);\n  name=String(name||'').replace(/[\\\\\\/]+/g,'');\n\n  return base==='C:\\\\'\n    ?base+name\n    :base+'\\\\'+name;\n};\n\nconst quote=s=>{\n  s=String(s||'');\n\n  return /[\\s\"]/g.test(s)\n    ?'\"'+s.replace(/\"/g,'\\\\\"')+'\"'\n    :s;\n};\n\nconst extOf=name=>{\n  const m=/\\.([^.]+)$/.exec(String(name||''));\n  return m?m[1].toLowerCase():'';\n};\n\nconst typeLabel=e=>{\n  if(e.directory)return'資料夾';\n\n  const ext=extOf(e.name);\n\n  if(ext==='xsh')return'XSH 程式';\n  if(ext==='xba')return'ExOS Batch';\n  if(ext==='csv')return'CSV';\n  if(ext==='html'||ext==='htm')return'HTML 文件';\n  if(ext==='pdf')return'PDF 文件';\n\n  if(\n    ['txt','ini','log','md','json','xml','js','css','php']\n      .includes(ext)\n  ){\n    return'文字文件';\n  }\n\n  if(\n    ['png','jpg','jpeg','gif','webp','bmp']\n      .includes(ext)\n  ){\n    return'圖片';\n  }\n\n  if(\n    ['mp3','wav','ogg','m4a'].includes(ext)\n  ){\n    return'音訊';\n  }\n\n  if(\n    ['mp4','webm','mov'].includes(ext)\n  ){\n    return'影片';\n  }\n\n  return ext\n    ?ext.toUpperCase()+' 檔案'\n    :'檔案';\n};\n\nconst imageIndexFor=e=>{\n  if(e.directory)return IMG.folder;\n\n  const ext=extOf(e.name);\n\n  if(ext==='xsh'||ext==='xba')return IMG.xsh;\n  if(ext==='csv')return IMG.csv;\n  if(ext==='html'||ext==='htm')return IMG.html;\n\n  if(\n    ['txt','ini','log','md','json','xml','js','css','php']\n      .includes(ext)\n  ){\n    return IMG.txt;\n  }\n\n  return IMG.file;\n};\n\nconst sizeText=n=>{\n  n=Number(n)||0;\n\n  if(n<1024)return n+' B';\n  if(n<1024*1024)return(n/1024).toFixed(1)+' KB';\n\n  if(n<1024*1024*1024){\n    return(n/1024/1024).toFixed(1)+' MB';\n  }\n\n  return(n/1024/1024/1024).toFixed(2)+' GB';\n};\n\nconst safeName=name=>{\n  name=String(name||'').trim();\n\n  if(\n    !name||\n    name.length>120||\n    /[\\\\\\/:*?\"<>|]/.test(name)\n  ){\n    throw new Error('無效的檔案或資料夾名稱。');\n  }\n\n  return name;\n};\n\nlet currentPath=normalizePath(\n  process.argv[1]||process.cwd||'C:\\\\'\n);\n\nlet history=[currentPath];\nlet historyPos=0;\nlet entries=[];\nlet selected=[];\nlet searchText='';\nlet busy=false;\nlet viewMode=comctl32.LV_VIEW_DETAILS;\nlet treeVisible=true;\nlet previewVisible=false;\nlet previewSeq=0;\nlet sortColumn=0;\nlet sortAscending=true;\n\nconst clipboard={\n  mode:'',\n  items:[]\n};\n\nconst treeLoaded=new Set();\n\ntry{\n  await kernel32.SetCurrentDirectory(currentPath);\n}catch(e){\n  currentPath='C:\\\\';\n  await kernel32.SetCurrentDirectory(currentPath);\n  history=[currentPath];\n  historyPos=0;\n}\n\nconst profileRoot=normalizePath(\n  process.env&&process.env.USERPROFILE\n    ?process.env.USERPROFILE\n    :'C:\\\\Users\\\\administrator'\n);\n\nconst quickChildren=[];\nconst quickCandidates=[\n  {id:'quick_desktop',text:'桌面',path:joinPath(profileRoot,'Desktop'),imageIndex:IMG.desktop},\n  {id:'quick_documents',text:'文件',path:joinPath(profileRoot,'Documents'),imageIndex:IMG.folder},\n  {id:'quick_downloads',text:'下載',path:joinPath(profileRoot,'Downloads'),imageIndex:IMG.download},\n  {id:'quick_public_documents',text:'公用文件',path:'C:\\\\Users\\\\Public\\\\Documents',imageIndex:IMG.folder},\n  {id:'quick_public_desktop',text:'公用桌面',path:'C:\\\\Users\\\\Public\\\\Desktop',imageIndex:IMG.desktop}\n];\n\nfor(const q of quickCandidates){\n  try{\n    const a=await kernel32.GetFileAttributes(q.path);\n    if(a&&a.directory){\n      quickChildren.push({\n        id:q.id,\n        text:q.text,\n        icon:'folder',\n        imageList:explorerImages,\n        imageIndex:q.imageIndex,\n        data:{path:q.path,quick:true}\n      });\n    }\n  }catch(ignoreQuickFolder){}\n}\n\nlet volumeLabel='ExFS';\ntry{const disk=await exes.QueryDiskInfo();if(disk&&disk.label!==undefined)volumeLabel=String(disk.label||'').trim();}catch(ignoreVolumeLabel){}\nconst volumeDisplayName=()=>volumeLabel?`${volumeLabel} (C:)`:'本機磁碟 (C:)';\n\nconst navigationItems=[\n  {\n    id:'nav_quick',\n    text:'快速存取',\n    icon:'history',\n    imageList:explorerImages,\n    imageIndex:IMG.history,\n    data:{group:true},\n    expanded:true,\n    children:quickChildren\n  },\n  {\n    id:'nav_thispc',\n    text:'我的電腦',\n    icon:'computer',\n    imageList:explorerImages,\n    imageIndex:IMG.computer,\n    data:{group:true},\n    expanded:true,\n    children:[\n      {\n        id:'C:\\\\',\n        text:volumeDisplayName(),\n        icon:'disk',\n        imageList:explorerImages,\n        imageIndex:IMG.disk,\n        data:{path:'C:\\\\'},\n        hasChildren:true,\n        expanded:true\n      }\n    ]\n  },\n  {\n    id:'nav_system',\n    text:'系統位置',\n    icon:'control',\n    imageList:explorerImages,\n    imageIndex:IMG.control,\n    data:{group:true},\n    expanded:true,\n    children:[\n      {id:'system_control',text:'控制台',icon:'control',imageList:explorerImages,imageIndex:IMG.control,data:{systemApp:'control'}},\n      {id:'system_devmgmt',text:'裝置管理員',icon:'computer',imageList:explorerImages,imageIndex:IMG.device,data:{systemApp:'devmgmt'}},\n      {id:'system_paint',text:'小畫家',icon:'image',imageList:explorerImages,imageIndex:IMG.paint,data:{systemApp:'paint'}},\n      {id:'system_winmine',text:'踩地雷',icon:'minesweeper',imageList:explorerImages,imageIndex:IMG.minesweeper,data:{systemApp:'winmine'}},\n      {id:'system_trash',text:'資源回收桶',icon:'trash',imageList:explorerImages,imageIndex:IMG.trash,data:{systemApp:'trash'}},\n      {id:'system_topology',text:'3D 拓譜',icon:'computer',imageList:explorerImages,imageIndex:IMG.topology,data:{systemApp:'volume3d'}}\n    ]\n  }\n];\n\nconst hwnd=await user32.CreateWindow({\n  title:'檔案總管',\n  icon:'explorer',\n  width:1240,\n  height:800\n});\n\nawait uxtheme.SetWindowTheme(hwnd,'Explorer','Navigation');\nawait dwmapi.DwmSetWindowAttribute(hwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');\nawait dwmapi.DwmEnableBlurBehindWindow(hwnd,{enabled:true,blurRadius:10,opacity:.94});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'root',\n  style:{\n    display:'flex',\n    flexDirection:'column',\n    width:'100%',\n    height:'100%',\n    margin:'0',\n    padding:'0',\n    boxSizing:'border-box',\n    background:'#f8fafc'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'nav',\n  parentId:'root',\n  style:{\n    display:'flex',\n    alignItems:'center',\n    gap:'6px',\n    width:'100%',\n    margin:'0',\n    padding:'7px 8px',\n    boxSizing:'border-box',\n    background:'#f8fafc',\n    border:'0'\n  }\n});\n\nfor(const [id,text,title] of [\n  ['back','←','上一個位置 (Alt+Left)'],\n  ['forward','→','下一個位置 (Alt+Right)'],\n  ['up','↑','上一層 (Alt+Up)'],\n  ['home','⌂','我的電腦 / C:'],\n  ['refresh','↻','重新整理 (F5)']\n]){\n  await user32.CreateControl(hwnd,{\n    type:'button',\n    id,\n    parentId:'nav',\n    text,\n    title,\n    style:{\n      width:'34px',\n      minWidth:'34px',\n      height:'30px',\n      padding:'0',\n      margin:'0',\n      fontSize:'16px'\n    }\n  });\n}\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'addressHost',\n  parentId:'nav',\n  style:{\n    display:'flex',\n    alignItems:'center',\n    flex:'1',\n    minWidth:'220px',\n    height:'31px',\n    margin:'0',\n    padding:'0',\n    boxSizing:'border-box',\n    border:'1px solid #cbd5e1',\n    borderRadius:'6px',\n    background:'#ffffff'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'addressDisplay',\n  parentId:'addressHost',\n  text:'我的電腦  ›  本機磁碟 (C:)',\n  title:'按一下編輯完整路徑 (Ctrl+L / Alt+D)',\n  style:{\n    display:'flex',\n    alignItems:'center',\n    flex:'1',\n    minWidth:'0',\n    height:'100%',\n    margin:'0',\n    padding:'0 9px',\n    boxSizing:'border-box',\n    whiteSpace:'nowrap',\n    overflow:'hidden',\n    fontSize:'12.5px',\n    color:'#334155'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'input',\n  id:'address',\n  parentId:'addressHost',\n  text:currentPath,\n  spellcheck:false,\n  style:{\n    display:'none',\n    flex:'1',\n    width:'100%',\n    height:'100%',\n    margin:'0',\n    padding:'0 8px',\n    boxSizing:'border-box',\n    border:'0',\n    borderRadius:'6px',\n    fontFamily:'Consolas,monospace',\n    fontSize:'12.5px'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'input',\n  id:'search',\n  parentId:'nav',\n  spellcheck:false,\n  placeholder:'搜尋目前資料夾',\n  title:'搜尋語法：名稱、*.txt、ext:csv、type:file、type:image、size>1MB',\n  style:{\n    width:'250px',\n    height:'31px',\n    margin:'0',\n    boxSizing:'border-box',\n    borderRadius:'6px'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'button',\n  id:'clear_search',\n  parentId:'nav',\n  text:'×',\n  title:'清除搜尋',\n  style:{\n    display:'none',\n    width:'30px',\n    minWidth:'30px',\n    height:'30px',\n    padding:'0',\n    margin:'0'\n  }\n});\n\nawait comctl32.CreateToolbar(hwnd,{\n  id:'commandBar',\n  parentId:'root',\n  buttons:[\n    {id:'newfolder',text:'新增資料夾',icon:'folder'},\n    {id:'newtext',text:'文字',icon:'txt',title:'新增文字文件'},\n    {id:'newcsv',text:'CSV',icon:'csv',title:'新增 CSV 試算表'},\n    {id:'newhtml',text:'HTML',icon:'html',title:'新增 HTML 文件'},\n    {id:'upload',text:'上傳',icon:'upload'},\n    {id:'cmdhere',text:'CMD',icon:'cmd',title:'在目前資料夾開啟命令提示字元'},\n    {id:'copypath',text:'複製路徑',icon:'copy',title:'複製目前位置或選取項目的完整路徑'},\n    {separator:true},\n    {id:'cut',text:'剪下',icon:'cut'},\n    {id:'copy',text:'複製',icon:'copy'},\n    {id:'paste',text:'貼上',icon:'paste'},\n    {separator:true},\n    {id:'rename',text:'重新命名',icon:'rename'},\n    {id:'delete',text:'刪除',icon:'delete'},\n    {id:'download',text:'下載',icon:'download'},\n    {separator:true},\n    {id:'tree',text:'導覽窗格',icon:'folder',checkable:true,checked:true,title:'導覽窗格 (Ctrl+Shift+E)'},\n    {id:'preview',text:'預覽',icon:'view',checkable:true,checked:false,title:'預覽窗格 (Alt+P)'},\n    {id:'selectall',text:'全選',title:'全選 (Ctrl+A)'},\n    {separator:true},\n    {id:'view_details',text:'詳細',checkable:true,checked:true,group:'view'},\n    {id:'view_list',text:'清單',checkable:true,group:'view'},\n    {id:'view_icons',text:'圖示',checkable:true,group:'view'},\n    {separator:true},\n    {id:'properties',text:'內容',icon:'properties'}\n  ],\n  style:{\n    width:'100%',\n    minHeight:'38px',\n    margin:'0',\n    boxSizing:'border-box'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'workspace',\n  parentId:'root',\n  style:{\n    display:'grid',\n    gridTemplateColumns:'220px minmax(0,1fr)',\n    gap:'0',\n    flex:'1',\n    minHeight:'0',\n    width:'100%',\n    margin:'0',\n    padding:'0',\n    boxSizing:'border-box',\n    border:'1px solid #d7dee8',\n    background:'#ffffff'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'treePane',\n  parentId:'workspace',\n  style:{\n    minWidth:'0',\n    minHeight:'0',\n    height:'100%',\n    margin:'0',\n    padding:'0',\n    boxSizing:'border-box',\n    background:'#f8fafc'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'listPane',\n  parentId:'workspace',\n  style:{\n    position:'relative',\n    minWidth:'0',\n    minHeight:'0',\n    height:'100%',\n    margin:'0',\n    padding:'0',\n    boxSizing:'border-box',\n    background:'#ffffff'\n  }\n});\n\nawait comctl32.CreateTreeView(hwnd,{\n  id:'folderTree',\n  parentId:'treePane',\n  dragDrop:true,\n  style:{\n    width:'100%',\n    height:'100%',\n    border:'0'\n  },\n  items:navigationItems\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'previewPane',\n  parentId:'workspace',\n  style:{\n    display:'none',\n    flexDirection:'column',\n    minWidth:'0',\n    minHeight:'0',\n    height:'100%',\n    overflow:'auto',\n    padding:'14px',\n    boxSizing:'border-box',\n    border:'0',\n    background:'#fbfcfe',\n    fontFamily:'Segoe UI,Arial,sans-serif',\n    lineHeight:'1.45'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'previewHeader',\n  parentId:'previewPane',\n  text:'預覽',\n  style:{\n    margin:'0 0 10px',\n    fontSize:'15px',\n    fontWeight:'600',\n    color:'#0f172a',\n    whiteSpace:'nowrap',\n    overflow:'hidden'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'image',\n  id:'previewImage',\n  parentId:'previewPane',\n  alt:'預覽圖片',\n  style:{\n    display:'none',\n    width:'100%',\n    maxWidth:'280px',\n    maxHeight:'240px',\n    margin:'0 auto 12px'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'previewBody',\n  parentId:'previewPane',\n  text:'選取一個項目以顯示詳細資訊。',\n  style:{\n    margin:'0',\n    whiteSpace:'pre-wrap',\n    overflow:'auto',\n    fontSize:'12.5px',\n    color:'#334155',\n    lineHeight:'1.55'\n  }\n});\n\nawait comctl32.CreateListView(hwnd,{\n  id:'fileList',\n  parentId:'listPane',\n  view:viewMode,\n  dragDrop:true,\n  autoSort:true,\n  columns:[\n    {id:'name',text:'名稱',width:360,minWidth:160,sortable:true,sortType:'text'},\n    {id:'type',text:'類型',width:155,minWidth:85,sortable:true,sortType:'text'},\n    {id:'size',text:'大小',width:115,minWidth:80,align:'right',sortable:true,sortType:'number'},\n    {id:'modified',text:'修改日期',width:185,minWidth:125,sortable:true,sortType:'text'}\n  ],\n  style:{\n    width:'100%',\n    height:'100%',\n    border:'0'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'emptyState',\n  parentId:'listPane',\n  text:'',\n  style:{\n    display:'none',\n    position:'absolute',\n    left:'50%',\n    top:'45%',\n    transform:'translate(-50%,-50%)',\n    pointerEvents:'none',\n    zIndex:'30',\n    width:'min(460px,80%)',\n    margin:'0',\n    padding:'20px',\n    textAlign:'center',\n    whiteSpace:'pre-wrap',\n    color:'#64748b',\n    fontSize:'13px',\n    lineHeight:'1.6',\n    background:'rgba(255,255,255,.92)',\n    border:'1px solid #e2e8f0',\n    borderRadius:'8px',\n    boxShadow:'0 8px 26px rgba(15,23,42,.08)'\n  }\n});\n\nawait comctl32.CreateStatusBar(hwnd,{\n  id:'statusbar',\n  parentId:'root',\n  parts:[\n    {text:'',width:0},\n    {text:'',width:250},\n    {text:'詳細資料',width:105},\n    {text:'ExFS',width:80}\n  ],\n  style:{\n    width:'100%',\n    margin:'0'\n  }\n});\n\nconst breadcrumbText=path=>{\n  path=normalizePath(path);\n  if(path==='C:\\\\')return'我的電腦  ›  本機磁碟 (C:)';\n  return '我的電腦  ›  本機磁碟 (C:)  ›  '+\n    path.substring(3).split('\\\\').join('  ›  ');\n};\n\nconst folderTitle=path=>{\n  path=normalizePath(path);\n  if(path==='C:\\\\')return'本機磁碟 (C:)';\n  const p=path.lastIndexOf('\\\\');\n  return p>=0?path.substring(p+1):path;\n};\n\nconst showAddressEditor=async()=>{\n  await user32.SetControlText('address',currentPath);\n  await user32.SetControlStyle('addressDisplay',{display:'none'});\n  await user32.SetControlStyle('address',{display:'block'});\n  await user32.FocusControl('address');\n};\n\nconst hideAddressEditor=async()=>{\n  await user32.SetControlStyle('address',{display:'none'});\n  await user32.SetControlStyle('addressDisplay',{display:'flex'});\n};\n\nconst setStatus=(text,part=0)=>\n  comctl32.StatusBar_SetText(\n    'statusbar',\n    part,\n    String(text||'')\n  );\n\nconst viewName=()=>(\n  viewMode===comctl32.LV_VIEW_ICON\n    ?'大圖示'\n    :(viewMode===comctl32.LV_VIEW_SMALLICON\n      ?'小圖示'\n      :(viewMode===comctl32.LV_VIEW_LIST?'清單':'詳細資料'))\n);\n\nconst refreshCommandState=async()=>{\n  const one=selected.length===1?selected[0]:null;\n  const selectedFiles=selected.filter(x=>!x.directory);\n  const enable={\n    newfolder:!busy,\n    newtext:!busy,\n    newcsv:!busy,\n    newhtml:!busy,\n    upload:!busy,\n    copypath:!busy,\n    cmdhere:!busy,\n    cut:!busy&&selected.length>0,\n    copy:!busy&&selected.length>0,\n    paste:!busy&&clipboard.items.length>0,\n    rename:!busy&&selected.length===1,\n    delete:!busy&&selected.length>0,\n    download:!busy&&selectedFiles.length>0,\n    tree:!busy,\n    preview:!busy,\n    selectall:!busy,\n    view_details:!busy,\n    view_list:!busy,\n    view_icons:!busy,\n    properties:!busy&&selected.length===1\n  };\n\n  for(const id of Object.keys(enable)){\n    await comctl32.Toolbar_EnableButton('commandBar',id,!!enable[id]);\n  }\n\n  await comctl32.Toolbar_CheckButton('commandBar','tree',treeVisible);\n  await comctl32.Toolbar_CheckButton('commandBar','preview',previewVisible);\n  await comctl32.Toolbar_CheckButton('commandBar','view_details',viewMode===comctl32.LV_VIEW_DETAILS);\n  await comctl32.Toolbar_CheckButton('commandBar','view_list',viewMode===comctl32.LV_VIEW_LIST);\n  await comctl32.Toolbar_CheckButton('commandBar','view_icons',viewMode===comctl32.LV_VIEW_ICON);\n\n  await setStatus(\n    clipboard.items.length\n      ?((clipboard.mode==='cut'?'剪下':'複製')+' '+clipboard.items.length+' 個項目 ｜ '+currentPath)\n      :currentPath,\n    1\n  );\n\n  await setStatus(viewName(),2);\n};\n\nconst updateNavigationState=async()=>{\n  await user32.SetControlProperty(\n    'back',\n    'disabled',\n    busy||historyPos<=0\n  );\n\n  await user32.SetControlProperty(\n    'forward',\n    'disabled',\n    busy||\n      historyPos>=history.length-1\n  );\n\n  await user32.SetControlProperty(\n    'up',\n    'disabled',\n    busy||currentPath==='C:\\\\'\n  );\n};\n\nconst setBusy=async on=>{\n  busy=!!on;\n\n  for(const id of ['back','forward','up','home','refresh','clear_search']){\n    await user32.SetControlProperty(id,'disabled',busy);\n  }\n\n  for(const id of [\n    'newfolder','newtext','newcsv','newhtml','upload','cmdhere','copypath','cut','copy','paste','rename','delete','download',\n    'tree','preview','selectall','view_details','view_list','view_icons','properties'\n  ]){\n    await comctl32.Toolbar_EnableButton('commandBar',id,!busy);\n  }\n\n  if(!busy){\n    await updateNavigationState();\n    await refreshCommandState();\n  }\n};\n\nconst parseSizeQuery=value=>{\n  const m=/^([0-9]+(?:\\.[0-9]+)?)(B|KB|MB|GB)?$/i.exec(\n    String(value||'').trim()\n  );\n\n  if(!m)return null;\n\n  let n=Number(m[1]);\n  const unit=String(m[2]||'B').toUpperCase();\n\n  if(unit==='KB')n*=1024;\n  else if(unit==='MB')n*=1024*1024;\n  else if(unit==='GB')n*=1024*1024*1024;\n\n  return Math.max(0,n);\n};\n\nconst wildcardRegex=value=>{\n  const safe=String(value||'')\n    .replace(/[.+^${}()|[\\]\\\\]/g,'\\\\$&')\n    .replace(/\\*/g,'.*')\n    .replace(/\\?/g,'.');\n\n  return new RegExp('^'+safe+'$','i');\n};\n\nconst typeMatches=(e,value)=>{\n  const t=String(value||'').toLowerCase();\n  const ext=extOf(e.name);\n\n  if(t==='folder'||t==='dir'||t==='directory')return !!e.directory;\n  if(t==='file')return !e.directory;\n  if(t==='image')return !e.directory&&['png','jpg','jpeg','gif','webp','bmp'].includes(ext);\n  if(t==='audio')return !e.directory&&['mp3','wav','ogg','m4a'].includes(ext);\n  if(t==='video')return !e.directory&&['mp4','webm','mov'].includes(ext);\n  if(t==='text')return !e.directory&&['txt','csv','html','htm','xsh','xba','log','ini','json','xml','css','js'].includes(ext);\n\n  return typeLabel(e).toLowerCase().includes(t);\n};\n\nconst matchesSearch=e=>{\n  const raw=String(searchText||'').trim();\n  if(!raw)return true;\n\n  const tokens=raw.match(/\"[^\"]*\"|\\S+/g)||[];\n\n  for(const tokenRaw of tokens){\n    const token=String(tokenRaw||'').replace(/^\"|\"$/g,'');\n    const lower=token.toLowerCase();\n\n    if(lower.startsWith('ext:')){\n      const wanted=lower.substring(4).replace(/^\\./,'');\n      if(extOf(e.name)!==wanted)return false;\n      continue;\n    }\n\n    if(lower.startsWith('type:')){\n      if(!typeMatches(e,lower.substring(5)))return false;\n      continue;\n    }\n\n    let m=/^size\\s*(>=|<=|>|<|=)\\s*(.+)$/i.exec(token);\n    if(m){\n      const wanted=parseSizeQuery(m[2]);\n      const actual=e.directory?0:(Number(e.size)||0);\n      if(wanted===null)return false;\n      if(m[1]==='>'&&!(actual>wanted))return false;\n      if(m[1]==='<'&&!(actual<wanted))return false;\n      if(m[1]==='>='&&!(actual>=wanted))return false;\n      if(m[1]==='<='&&!(actual<=wanted))return false;\n      if(m[1]==='='&&!(actual===wanted))return false;\n      continue;\n    }\n\n    if(token.includes('*')||token.includes('?')){\n      if(!wildcardRegex(token).test(String(e.name||'')))return false;\n      continue;\n    }\n\n    if(!String(e.name||'').toLowerCase().includes(lower))return false;\n  }\n\n  return true;\n};\n\nconst visibleEntries=()=>entries.filter(matchesSearch);\n\nconst listItem=e=>{\n  const customIcon=String(e.shellIcon||'');\n  return {\n  id:e.path,\n  text:e.name,\n  subItems:[\n    typeLabel(e),\n    e.directory?'':sizeText(e.size),\n    String(e.modified||'')\n  ],\n  sortValues:[\n    (e.directory?'0|':'1|')+\n      String(e.name||'').toLowerCase(),\n    typeLabel(e).toLowerCase(),\n    e.directory?-1:(Number(e.size)||0),\n    String(e.modified||'')\n  ],\n  icon:customIcon||'file',\n  imageList:customIcon?'':explorerImages,\n  imageIndex:customIcon?-1:imageIndexFor(e),\n  data:{\n    path:e.path,\n    name:e.name,\n    directory:!!e.directory,\n    size:Number(e.size)||0,\n    modified:String(e.modified||'')\n  }\n};\n};\n\nconst updateWorkspaceLayout=async()=>{\n  let columns=[];\n\n  if(treeVisible)columns.push('220px');\n  columns.push('minmax(0,1fr)');\n  if(previewVisible)columns.push('320px');\n\n  await user32.SetControlStyle(\n    'workspace',\n    {gridTemplateColumns:columns.join(' ')}\n  );\n\n  await user32.SetControlStyle(\n    'treePane',\n    {display:treeVisible?'block':'none'}\n  );\n\n  await user32.SetControlStyle(\n    'previewPane',\n    {display:previewVisible?'flex':'none'}\n  );\n};\n\nconst textPreviewExtension=ext=>[\n  'txt','csv','html','htm','xsh','xba','log','ini','json','xml','css','js','md'\n].includes(String(ext||'').toLowerCase());\n\nconst imagePreviewExtension=ext=>\n  ['png','jpg','jpeg','gif','webp'].includes(String(ext||'').toLowerCase());\n\nconst imageMime=ext=>{\n  ext=String(ext||'').toLowerCase();\n  if(ext==='png')return'image/png';\n  if(ext==='gif')return'image/gif';\n  if(ext==='webp')return'image/webp';\n  return'image/jpeg';\n};\n\nconst previewImageDataUrl=async item=>{\n  if(!item||item.directory||Number(item.size)>4*1024*1024)return'';\n  const h=await kernel32.CreateFile(item.path,kernel32.GENERIC_READ,kernel32.OPEN_EXISTING);\n  try{\n    const r=await kernel32.ReadFile(h,Number(item.size)||4*1024*1024,0);\n    const bytes=new Uint8Array(r&&r.data?r.data:[]);\n    let bin='';\n    for(let i=0;i<bytes.length;i+=0x8000){\n      bin+=String.fromCharCode(...bytes.subarray(i,i+0x8000));\n    }\n    return 'data:'+imageMime(extOf(item.name))+';base64,'+btoa(bin);\n  }finally{\n    await kernel32.CloseHandle(h);\n  }\n};\n\nconst setPreviewText=async(header,body)=>{\n  await user32.SetControlText('previewHeader',String(header||'預覽'));\n  await user32.SetControlText('previewBody',String(body||''));\n};\n\nconst updatePreview=async()=>{\n  const token=++previewSeq;\n\n  if(!previewVisible)return;\n\n  await user32.SetControlStyle('previewImage',{display:'none'});\n\n  if(!selected.length){\n    await setPreviewText(\n      '預覽',\n      searchText\n        ?'目前搜尋結果沒有選取項目。\\n\\n提示：Ctrl+F 可快速回到搜尋框。'\n        :'選取一個檔案或資料夾，可在這裡查看內容與詳細資訊。'\n    );\n    return;\n  }\n\n  if(selected.length>1){\n    const bytes=selected.filter(x=>!x.directory).reduce((sum,x)=>sum+(Number(x.size)||0),0);\n    await setPreviewText(\n      '已選取 '+selected.length+' 個項目',\n      '資料夾：'+selected.filter(x=>x.directory).length+'\\n'+\n      '檔案：'+selected.filter(x=>!x.directory).length+'\\n'+\n      '檔案大小合計：'+sizeText(bytes)+'\\n\\n'+\n      '可使用工具列進行複製、剪下、刪除或下載。'\n    );\n    return;\n  }\n\n  const item=selected[0];\n  let info=null;\n  try{info=await shell32.SHGetFileInfo(item.path);}catch(ignoreInfo){}\n\n  const lines=[\n    '類型：'+(info?info.typeName:typeLabel(item)),\n    '位置：'+parentPath(item.path),\n    item.directory?'':'大小：'+sizeText(item.size)+' ('+Number(item.size||0)+' bytes)',\n    item.modified?'修改日期：'+item.modified:'',\n    '檔案系統：ExFS'\n  ].filter(Boolean);\n\n  if(!item.directory){\n    const ext=extOf(item.name);\n\n    if(imagePreviewExtension(ext)&&Number(item.size)<=4*1024*1024){\n      try{\n        const url=await previewImageDataUrl(item);\n        if(token!==previewSeq)return;\n        if(url){\n          await user32.SetControlProperty('previewImage','src',url);\n          await user32.SetControlStyle('previewImage',{display:'block'});\n        }\n      }catch(err){\n        lines.push('圖片預覽：無法載入');\n      }\n    }\n\n    if(textPreviewExtension(ext)&&Number(item.size)<=256*1024){\n      try{\n        let text=String(await kernel32.ReadTextFile(item.path));\n        if(token!==previewSeq)return;\n        text=text.replace(/\\u0000/g,'');\n        if(text.length>5000)text=text.substring(0,5000)+'\\n\\n… 預覽已截斷 …';\n        lines.push('', '──────── 內容預覽 ────────', text);\n      }catch(err){\n        lines.push('', '無法建立文字預覽：'+String(err&&err.message?err.message:err));\n      }\n    }else if(!imagePreviewExtension(ext)){\n      lines.push('', '按 Enter 或雙擊，以已註冊的應用程式開啟。');\n    }\n  }else{\n    lines.push('', '按 Enter 或雙擊進入此資料夾。');\n  }\n\n  if(token!==previewSeq)return;\n  await setPreviewText(item.name,lines.join('\\n'));\n};\n\nconst applySort=async()=>{\n  await comctl32.ListView_SortItems(\n    'fileList',\n    sortColumn,\n    sortAscending\n  );\n\n  const names=['名稱','類型','大小','修改日期'];\n  await setStatus(\n    (names[sortColumn]||'名稱')+' '+(sortAscending?'↑':'↓'),\n    2\n  );\n};\n\nconst syncSelection=async items=>{\n  selected=(Array.isArray(items)?items:[])\n    .map(x=>x&&x.data?x.data:null)\n    .filter(Boolean);\n\n  if(!selected.length){\n    await setStatus(\n      visibleEntries().length+\n      (searchText?' / '+entries.length:'')+\n      ' 個項目'\n    );\n\n    await refreshCommandState();\n    await updatePreview();\n    return;\n  }\n\n  if(selected.length===1){\n    const e=selected[0];\n\n    await setStatus(\n      e.name+\n      (e.directory\n        ?' ｜ 資料夾'\n        :' ｜ '+sizeText(e.size))\n    );\n\n    await refreshCommandState();\n    await updatePreview();\n    return;\n  }\n\n  const selectedBytes=\n    selected\n      .filter(x=>!x.directory)\n      .reduce(\n        (sum,x)=>sum+(Number(x.size)||0),\n        0\n      );\n\n  await setStatus(\n    '已選取 '+\n    selected.length+\n    ' 個項目'+\n    (\n      selectedBytes\n        ?' ｜ '+sizeText(selectedBytes)\n        :''\n    )\n  );\n\n  await refreshCommandState();\n  await updatePreview();\n};\n\nconst renderList=async()=>{\n  const list=visibleEntries();\n\n  await user32.SetControlStyle('clear_search',{display:searchText?'block':'none'});\n\n  await comctl32.ListView_SetItems(\n    'fileList',\n    list.map(listItem)\n  );\n\n  await syncSelection(\n    await comctl32.ListView_GetSelectedItems(\n      'fileList'\n    )\n  );\n\n  if(list.length){\n    await user32.SetControlStyle('emptyState',{display:'none'});\n    await applySort();\n  }\n\n  if(!list.length){\n    await user32.SetControlText(\n      'emptyState',\n      searchText\n        ?'沒有符合搜尋條件的項目\\n\\n試試較短的關鍵字、*.txt、ext:csv 或 size>1MB'\n        :'這個資料夾是空的\\n\\n可使用「新增資料夾」「新增文字」「上傳」，或在空白處按右鍵。'\n    );\n    await user32.SetControlStyle('emptyState',{display:'block'});\n    await setStatus(searchText?'找不到符合的項目。':'這個資料夾是空的。');\n    if(previewVisible&&!selected.length){\n      await setPreviewText(\n        searchText?'沒有搜尋結果':'這個資料夾是空的',\n        searchText\n          ?'找不到符合「'+searchText+'」的項目。\\n\\n可嘗試較短的關鍵字，或使用 *.txt / ext:csv / size>1MB。'\n          :'可使用工具列「新增資料夾」、上傳檔案，或在空白處按右鍵建立新項目。'\n      );\n    }\n  }\n};\n\nconst pathSegments=path=>{\n  path=normalizePath(path);\n\n  if(path==='C:\\\\')return['C:\\\\'];\n\n  const parts=path.substring(3).split('\\\\');\n  const out=['C:\\\\'];\n  let cur='C:\\\\';\n\n  for(const part of parts){\n    cur=joinPath(cur,part);\n    out.push(cur);\n  }\n\n  return out;\n};\n\nconst loadTreeChildren=async path=>{\n  path=normalizePath(path);\n\n  if(treeLoaded.has(path))return;\n\n  const list=\n    await kernel32.ListDirectory(path);\n\n  const dirs=list\n    .filter(x=>x.directory)\n    .sort((a,b)=>\n      String(a.name||'').localeCompare(\n        String(b.name||''),\n        'zh-Hant'\n      )\n    )\n    .map(e=>({\n      id:e.path,\n      text:e.name,\n      icon:'folder',\n      imageList:explorerImages,\n      imageIndex:IMG.folder,\n      data:{path:e.path},\n      hasChildren:true\n    }));\n\n  await comctl32.TreeView_SetChildren(\n    'folderTree',\n    path,\n    dirs\n  );\n\n  treeLoaded.add(path);\n};\n\nconst ensureTreePath=async path=>{\n  const segments=pathSegments(path);\n\n  for(let i=0;i<segments.length;i++){\n    const p=segments[i];\n\n    if(i===0||i<segments.length-1){\n      await loadTreeChildren(p);\n    }\n\n    if(i<segments.length-1){\n      await comctl32.TreeView_Expand(\n        'folderTree',\n        p,\n        comctl32.TVE_EXPAND\n      );\n    }\n  }\n\n  await comctl32.TreeView_SelectItem(\n    'folderTree',\n    path\n  );\n};\n\nconst loadDirectory=async()=>{\n  await setBusy(true);\n\n  try{\n    entries=\n      await kernel32.ListDirectory(currentPath);\n\n    for(const entry of entries){\n      if(!entry.directory&&extOf(entry.name)==='xsh'){\n        try{const si=await shell32.SHGetFileInfo(entry.path);if(si&&si.icon)entry.shellIcon=String(si.icon);}catch(ignoreEmbeddedXshIcon){}\n      }\n    }\n\n    entries.sort((a,b)=>{\n      if(a.directory&&!b.directory)return-1;\n      if(!a.directory&&b.directory)return 1;\n\n      return String(a.name||'').localeCompare(\n        String(b.name||''),\n        'zh-Hant'\n      );\n    });\n\n    await user32.SetControlText('address',currentPath);\n    await user32.SetControlText('addressDisplay',breadcrumbText(currentPath));\n    await user32.SetControlProperty('search','placeholder','在「'+folderTitle(currentPath)+'」中搜尋');\n    await user32.SetWindowText(hwnd,'檔案總管 — '+folderTitle(currentPath));\n\n    await kernel32.SetCurrentDirectory(\n      currentPath\n    );\n\n    await renderList();\n    await ensureTreePath(currentPath);\n    await updateNavigationState();\n    await setStatus(\n      currentPath+(currentPath==='C:\\\\Windows'||currentPath==='C:\\\\Program Files'||currentPath==='C:\\\\Program Files (x86)'?' ｜ 系統唯讀位置':''),\n      1\n    );\n    await setStatus(viewName(),2);\n    await setStatus('ExFS',3);\n  }finally{\n    await setBusy(false);\n  }\n};\n\nasync function navigate(path,pushHistory=true){\n  path=normalizePath(path);\n\n  const attr=\n    await kernel32.GetFileAttributes(path);\n\n  if(!attr.directory){\n    throw new Error('指定位置不是資料夾。');\n  }\n\n  if(path===currentPath){\n    await loadDirectory();\n    return;\n  }\n\n  currentPath=path;\n\n  if(pushHistory){\n    history=history.slice(0,historyPos+1);\n    history.push(currentPath);\n    historyPos=history.length-1;\n  }\n\n  await loadDirectory();\n}\n\nconst uniquePath=async (name,targetFolder=currentPath)=>{\n  name=safeName(name);\n  targetFolder=normalizePath(targetFolder||currentPath);\n\n  const dot=name.lastIndexOf('.');\n  const base=dot>0?name.substring(0,dot):name;\n  const ext=dot>0?name.substring(dot):'';\n\n  for(let i=0;i<1000;i++){\n    const candidate=i===0\n      ?name\n      :base+' ('+i+')'+ext;\n\n    const p=joinPath(\n      targetFolder,\n      candidate\n    );\n\n    try{\n      await kernel32.GetFileAttributes(p);\n    }catch(e){\n      return p;\n    }\n  }\n\n  throw new Error('無法產生不重複檔名。');\n};\n\nconst selectedPaths=()=>\n  selected.map(\n    x=>String(x.path||'')\n  ).filter(Boolean);\n\nconst syncShellClipboard=async()=>{\n  const c=\n    await shell32.GetClipboardState();\n\n  clipboard.mode=\n    String(c.effect||'');\n\n  clipboard.items=[];\n\n  for(const p of c.paths||[]){\n    try{\n      const info=\n        await shell32.SHGetFileInfo(p);\n\n      clipboard.items.push({\n        path:info.path,\n        name:info.name,\n        directory:!!info.directory,\n        size:Number(info.size)||0\n      });\n    }catch(ignoreClipboardInfo){}\n  }\n\n  await refreshCommandState();\n  return c;\n};\n\nconst shellPropertiesText=items=>{\n  const list=Array.isArray(items)?items:[];\n\n  if(!list.length)return'';\n\n  if(list.length>1){\n    const bytes=list.reduce(\n      (sum,x)=>sum+(\n        x.directory?0:(Number(x.size)||0)\n      ),\n      0\n    );\n\n    return[\n      '選取項目：'+list.length,\n      '檔案總大小：'+sizeText(bytes),\n      '檔案系統：ExFS',\n      'Shell：shell32.dll / EXOS_SHELL32_V1'\n    ].join('\\n');\n  }\n\n  const x=list[0];\n\n  return[\n    '名稱：'+x.name,\n    '類型：'+x.typeName,\n    '位置：'+parentPath(x.path),\n    '完整路徑：'+x.path,\n    x.directory\n      ?'大小：資料夾'\n      :'大小：'+sizeText(x.size)+' ('+x.size+' bytes)',\n    '檔案系統：'+x.filesystem,\n    'VDO：'+x.backingVdo\n  ].join('\\n');\n};\n\nlet propertyWindowSeq=0;\nconst propertyValueText=v=>String(v===undefined||v===null?'':v);\nconst propertyRow=async(hwnd,parent,id,label,value)=>{\n  const row=id+'_row';\n  await user32.CreateControl(hwnd,{type:'div',id:row,parentId:parent,style:{display:'grid',gridTemplateColumns:'150px minmax(0,1fr)',gap:'12px',alignItems:'start',margin:'0',padding:'7px 4px',borderBottom:'1px solid #e5e7eb'}});\n  await user32.CreateControl(hwnd,{type:'label',id:id+'_label',parentId:row,text:label,style:{margin:'0',fontWeight:'600',color:'#475569'}});\n  await user32.CreateControl(hwnd,{type:'label',id:id+'_value',parentId:row,text:propertyValueText(value),style:{margin:'0',color:'#111827',overflowWrap:'anywhere',userSelect:'text'}});\n};\nconst aceMaskText=ace=>{\n  if(!ace||typeof ace!=='object')return'';\n  const mask=ace.mask!==undefined?ace.mask:(ace.access_mask!==undefined?ace.access_mask:'');\n  return propertyValueText(mask);\n};\nconst showPropertiesDialog=async path=>{\n  const info=await shell32.SHGetFileInfo(path);\n  const attr=await kernel32.GetFileAttributes(path);\n  const seq=++propertyWindowSeq;\n  const prefix='prop'+seq+'_';\n  const phwnd=await user32.CreateWindow({title:info.name+' 內容',icon:info.icon||'properties',width:690,height:620});\n  try{await uxtheme.SetWindowTheme(phwnd,'ExplorerProperties','Dialog');}catch(ignorePropTheme){}\n  try{await dwmapi.DwmSetWindowAttribute(phwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');}catch(ignorePropDwm){}\n  await user32.CreateControl(phwnd,{type:'div',id:prefix+'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',overflow:'hidden',background:'#f8fafc'}});\n  await user32.CreateControl(phwnd,{type:'div',id:prefix+'head',parentId:prefix+'root',style:{display:'flex',alignItems:'center',gap:'14px',padding:'14px 16px',margin:'0',background:'#fff',borderBottom:'1px solid #dbe3ec'}});\n  await user32.CreateControl(phwnd,{type:'image',id:prefix+'icon',parentId:prefix+'head',src:await user32.LoadIcon('shell32.dll',info.icon||'file'),style:{width:'42px',height:'42px',margin:'0',objectFit:'contain'}});\n  await user32.CreateControl(phwnd,{type:'div',id:prefix+'headText',parentId:prefix+'head',style:{display:'flex',flexDirection:'column',gap:'3px',minWidth:'0',margin:'0'}});\n  await user32.CreateControl(phwnd,{type:'label',id:prefix+'name',parentId:prefix+'headText',text:info.name,style:{margin:'0',fontSize:'17px',fontWeight:'600',overflowWrap:'anywhere'}});\n  await user32.CreateControl(phwnd,{type:'label',id:prefix+'path',parentId:prefix+'headText',text:info.path,style:{margin:'0',fontSize:'12px',color:'#64748b',overflowWrap:'anywhere',userSelect:'text'}});\n  await comctl32.CreateTabControl(phwnd,{id:prefix+'tabs',parentId:prefix+'root',current:0,items:[{id:'general',text:'一般'},{id:'security',text:'安全性'},{id:'details',text:'詳細資料'}],style:{flex:'1',width:'100%',minHeight:'0',margin:'8px 0 0'}});\n  const general=await comctl32.TabCtrl_GetPageId(prefix+'tabs',0);\n  const security=await comctl32.TabCtrl_GetPageId(prefix+'tabs',1);\n  const details=await comctl32.TabCtrl_GetPageId(prefix+'tabs',2);\n  for(const page of [general,security,details])await user32.SetControlStyle(page,{height:'100%',overflow:'auto',padding:'12px 16px',boxSizing:'border-box',background:'#fff'});\n\n  await propertyRow(phwnd,general,prefix+'g_type','檔案類型',info.typeName);\n  await propertyRow(phwnd,general,prefix+'g_location','位置',parentPath(info.path));\n  await propertyRow(phwnd,general,prefix+'g_size','大小',info.directory?'資料夾':sizeText(Number(attr.size)||Number(info.size)||0)+' ('+(Number(attr.size)||Number(info.size)||0)+' bytes)');\n  await propertyRow(phwnd,general,prefix+'g_fs','檔案系統',info.filesystem||'ExFS');\n  await propertyRow(phwnd,general,prefix+'g_vdo','儲存體',info.backingVdo||'PHP /_exfs/');\n  await propertyRow(phwnd,general,prefix+'g_attr','屬性',info.directory?'資料夾':'一般檔案');\n\n  /*\n   * Windows 10-style Mark-of-the-Web presentation.\n   *\n   * Browser/network imports already receive Zone.Identifier by default.\n   * Explorer does not expose arbitrary ADS editing here. The only supported\n   * change is removing Zone.Identifier through shell32.UnblockFile().\n   */\n  let motwInitiallyLocked=!info.directory&&!!info.markOfTheWeb;\n  let motwLocked=motwInitiallyLocked;\n  let motwDirty=false;\n  const motwBoxId=prefix+'motw_locked';\n  const motwHintId=prefix+'motw_hint';\n  const motwApplyId=prefix+'apply';\n\n  if(!info.directory){\n    await user32.CreateControl(phwnd,{\n      type:'div',\n      id:prefix+'motw_section',\n      parentId:general,\n      style:{\n        display:'flex',\n        flexDirection:'column',\n        gap:'7px',\n        margin:'18px 4px 4px',\n        padding:'12px 12px',\n        borderTop:'1px solid #cbd5e1',\n        background:motwInitiallyLocked?'#fffaf0':'#f8fafc'\n      }\n    });\n\n    await user32.CreateControl(phwnd,{\n      type:'label',\n      id:prefix+'motw_title',\n      parentId:prefix+'motw_section',\n      text:'安全性',\n      style:{margin:'0',fontWeight:'700',color:'#111827'}\n    });\n\n    await user32.CreateControl(phwnd,{\n      type:'label',\n      id:prefix+'motw_desc',\n      parentId:prefix+'motw_section',\n      text:motwInitiallyLocked\n        ?'此檔案來自其他電腦或網路。為協助保護此電腦，ExOS 已將此檔案鎖定。'\n        :'此檔案目前沒有 Zone.Identifier，未受到網路來源封鎖。',\n      style:{margin:'0',fontSize:'12px',lineHeight:'1.55',color:'#475569'}\n    });\n\n    await user32.CreateControl(phwnd,{\n      type:'div',\n      id:prefix+'motw_checkrow',\n      parentId:prefix+'motw_section',\n      style:{\n        display:'flex',\n        alignItems:'center',\n        gap:'8px',\n        margin:'2px 0 0'\n      }\n    });\n\n    await user32.CreateControl(phwnd,{\n      type:'input',\n      inputType:'checkbox',\n      checked:motwInitiallyLocked,\n      disabled:!motwInitiallyLocked,\n      id:motwBoxId,\n      parentId:prefix+'motw_checkrow',\n      title:'取消勾選後按「套用」或「確定」即可移除 Zone.Identifier',\n      style:{\n        width:'17px',\n        height:'17px',\n        margin:'0',\n        padding:'0'\n      }\n    });\n\n    await user32.CreateControl(phwnd,{\n      type:'label',\n      id:prefix+'motw_checklabel',\n      parentId:prefix+'motw_checkrow',\n      text:'鎖定檔案',\n      style:{margin:'0',fontWeight:'600'}\n    });\n\n    await user32.CreateControl(phwnd,{\n      type:'label',\n      id:motwHintId,\n      parentId:prefix+'motw_section',\n      text:motwInitiallyLocked\n        ?'取消「鎖定檔案」後按「套用」或「確定」，即可解除封鎖。'\n        :'此檔案已解除封鎖。Windows 風格的內容頁不提供重新加入網路封鎖。',\n      style:{margin:'0 0 0 25px',fontSize:'11px',color:'#64748b'}\n    });\n\n    user32.OnControl(motwBoxId,async e=>{\n      if(e.action!=='change'&&e.action!=='click')return;\n\n      if(!motwInitiallyLocked){\n        await user32.SetControlProperty(motwBoxId,'checked',false);\n        return;\n      }\n\n      motwLocked=!!e.checked;\n      motwDirty=(motwLocked!==motwInitiallyLocked);\n\n      try{\n        await user32.SetControlProperty(\n          motwApplyId,\n          'disabled',\n          !motwDirty\n        );\n      }catch(ignoreApplyBeforeCreate){}\n\n      await user32.SetControlText(\n        motwHintId,\n        motwLocked\n          ?'檔案仍維持鎖定。'\n          :'按「套用」或「確定」後會刪除 Zone.Identifier，解除網路來源封鎖。'\n      );\n    });\n  }\n\n  let sec=null,desc=null,ownerName='',securityError='';\n  try{\n    sec=await advapi32.GetNamedSecurityInfo(info.path,advapi32.SE_FILE_OBJECT);\n    desc=sec&&sec.descriptor?sec.descriptor:null;\n    if(!desc&&sec&&sec.sddl)desc=await advapi32.ConvertStringSecurityDescriptorToSecurityDescriptor(sec.sddl);\n    if(desc&&desc.owner_sid){\n      try{const principal=await advapi32.LookupAccountSid(desc.owner_sid);ownerName=String(principal&&principal.name||principal&&principal.account||'');}catch(ignoreOwnerLookup){}\n    }\n  }catch(e){securityError=String(e&&e.message||e);}\n  if(securityError){\n    await user32.CreateControl(phwnd,{type:'label',id:prefix+'sec_error',parentId:security,text:'無法讀取安全性資訊：'+securityError,style:{margin:'4px 0',padding:'10px',color:'#b91c1c',background:'#fef2f2',border:'1px solid #fecaca'}});\n  }else{\n    await propertyRow(phwnd,security,prefix+'s_object','物件名稱',info.path);\n    await propertyRow(phwnd,security,prefix+'s_owner','擁有者',(ownerName?ownerName+' · ':'')+propertyValueText(desc&&desc.owner_sid));\n    await propertyRow(phwnd,security,prefix+'s_integrity','完整性層級',propertyValueText(desc&&desc.mandatory_label&&desc.mandatory_label.integrity_level||desc&&desc.integrity_level||'MEDIUM'));\n    const dacl=desc&&Array.isArray(desc.dacl)?desc.dacl:[];\n    await user32.CreateControl(phwnd,{type:'label',id:prefix+'aceTitle',parentId:security,text:'權限項目 ('+dacl.length+')',style:{margin:'14px 0 7px',fontWeight:'700'}});\n    if(!dacl.length){\n      await user32.CreateControl(phwnd,{type:'label',id:prefix+'aceEmpty',parentId:security,text:'沒有可列出的 DACL ACE。',style:{margin:'0 0 8px',color:'#64748b'}});\n    }else{\n      for(let i=0;i<dacl.length;i++){\n        const ace=dacl[i]||{};\n        const sid=String(ace.sid||'');let who=sid;\n        if(sid){try{const p=await advapi32.LookupAccountSid(sid);who=String(p&&p.name||p&&p.account||sid);}catch(ignoreAceLookup){}}\n        await user32.CreateControl(phwnd,{type:'div',id:prefix+'ace_'+i,parentId:security,style:{display:'grid',gridTemplateColumns:'minmax(150px,1fr) 95px minmax(120px,1fr)',gap:'8px',padding:'7px 8px',margin:'0 0 4px',border:'1px solid #e5e7eb',background:i%2?'#f8fafc':'#fff'}});\n        await user32.CreateControl(phwnd,{type:'label',id:prefix+'ace_who_'+i,parentId:prefix+'ace_'+i,text:who,style:{margin:'0',overflowWrap:'anywhere'}});\n        await user32.CreateControl(phwnd,{type:'label',id:prefix+'ace_type_'+i,parentId:prefix+'ace_'+i,text:String(ace.type||ace.ace_type||'ALLOW'),style:{margin:'0',fontWeight:'600'}});\n        await user32.CreateControl(phwnd,{type:'label',id:prefix+'ace_mask_'+i,parentId:prefix+'ace_'+i,text:aceMaskText(ace),style:{margin:'0',fontFamily:'Consolas,monospace',fontSize:'11px',overflowWrap:'anywhere'}});\n      }\n    }\n    await user32.CreateControl(phwnd,{type:'label',id:prefix+'sddlTitle',parentId:security,text:'安全性描述元 (SDDL)',style:{margin:'14px 0 6px',fontWeight:'700'}});\n    await user32.CreateControl(phwnd,{type:'textarea',id:prefix+'sddl',parentId:security,text:String(sec&&sec.sddl||''),readOnly:true,spellcheck:false,style:{width:'100%',height:'92px',resize:'vertical',margin:'0',padding:'8px',boxSizing:'border-box',fontFamily:'Consolas,monospace',fontSize:'11px'}});\n    await user32.CreateControl(phwnd,{type:'button',id:prefix+'editSecurity',parentId:security,text:'編輯安全性…',title:'以 SDDL 編輯 ACL；實際寫入仍由 ExFS DACL / Token 驗證',style:{margin:'10px 0 0',minWidth:'110px'}});\n    user32.OnControl(prefix+'editSecurity',async e=>{\n      if(e.action!=='click')return;\n      const current=String(await user32.GetControlText(prefix+'sddl')||'');\n      const next=await user32.PromptBox('輸入新的 SDDL。伺服器會依目前 Token 驗證 WRITE_DAC / 所有權權限。','安全性',current);\n      if(next===null||String(next)===current)return;\n      try{\n        const out=await advapi32.SetFileSecurity(info.path,String(next));\n        const refreshed=await advapi32.GetFileSecurity(info.path);\n        await user32.SetControlText(prefix+'sddl',String(refreshed&&refreshed.sddl||next));\n        await user32.MessageBox('安全性設定已更新。','ExOS 安全性');\n      }catch(err){await user32.MessageBox('無法更新安全性：'+String(err&&err.message||err),'ExOS 安全性');}\n    });\n  }\n\n  const assoc=info.association||{};\n  const detailRows=[\n    ['名稱',info.name],['完整路徑',info.path],['父資料夾',parentPath(info.path)],['類型',info.typeName],['副檔名',info.extension||''],\n    ['目錄',info.directory?'是':'否'],['大小 (bytes)',info.directory?'':String(Number(info.size)||0)],['Node ID',String(info.nodeId||0)],\n    ['檔案系統',info.filesystem||'ExFS'],['Backing VDO',info.backingVdo||''],['Shell 圖示',info.icon||''],\n    ['關聯已註冊',assoc.registered?'是':'否'],['預設動作',assoc.defaultVerb||'open'],['唯讀',info.attributes&&info.attributes.readOnly?'是':'否'],['隱藏',info.attributes&&info.attributes.hidden?'是':'否'],['系統',info.attributes&&info.attributes.system?'是':'否']\n  ];\n  for(let i=0;i<detailRows.length;i++)await propertyRow(phwnd,details,prefix+'d_'+i,detailRows[i][0],detailRows[i][1]);\n\n  if(!info.directory){\n    await propertyRow(\n      phwnd,\n      details,\n      prefix+'d_motw',\n      '網路來源鎖定',\n      motwInitiallyLocked\n        ?'是 · Zone.Identifier / ZoneId=3'\n        :'否'\n    );\n  }\n\n  const applyMotwChange=async()=>{\n    if(info.directory||!motwInitiallyLocked||!motwDirty)return true;\n\n    let checked=true;\n    try{\n      checked=!!await user32.GetControlProperty(\n        motwBoxId,\n        'checked'\n      );\n    }catch(ignoreCheckedRead){}\n\n    /* Re-checking before Apply simply cancels the pending change. */\n    if(checked){\n      motwLocked=true;\n      motwDirty=false;\n      await user32.SetControlProperty(\n        motwApplyId,\n        'disabled',\n        true\n      );\n      return true;\n    }\n\n    try{\n      await shell32.UnblockFile(info.path);\n\n      motwInitiallyLocked=false;\n      motwLocked=false;\n      motwDirty=false;\n\n      await user32.SetControlProperty(\n        motwBoxId,\n        'checked',\n        false\n      );\n      await user32.SetControlProperty(\n        motwBoxId,\n        'disabled',\n        true\n      );\n      await user32.SetControlProperty(\n        motwApplyId,\n        'disabled',\n        true\n      );\n\n      await user32.SetControlText(\n        prefix+'motw_desc',\n        '此檔案的 Zone.Identifier 已移除，ExOS 不再將它視為來自網路的鎖定檔案。'\n      );\n      await user32.SetControlText(\n        motwHintId,\n        '已解除封鎖。重新開啟「內容」時會顯示為未鎖定。'\n      );\n      await user32.SetControlText(\n        prefix+'d_motw_value',\n        '否'\n      );\n\n      return true;\n    }catch(err){\n      await user32.MessageBox(\n        '無法解除檔案鎖定：\\n\\n'+\n        String(err&&err.message?err.message:err),\n        '解除封鎖失敗'\n      );\n\n      try{\n        await user32.SetControlProperty(\n          motwBoxId,\n          'checked',\n          true\n        );\n      }catch(ignoreRestoreCheck){}\n\n      motwLocked=true;\n      motwDirty=false;\n\n      try{\n        await user32.SetControlProperty(\n          motwApplyId,\n          'disabled',\n          true\n        );\n      }catch(ignoreDisableApply){}\n\n      return false;\n    }\n  };\n\n  await user32.CreateControl(phwnd,{\n    type:'div',\n    id:prefix+'foot',\n    parentId:prefix+'root',\n    style:{\n      display:'flex',\n      justifyContent:'flex-end',\n      gap:'8px',\n      padding:'10px 14px',\n      margin:'0',\n      background:'#f8fafc',\n      borderTop:'1px solid #dbe3ec'\n    }\n  });\n\n  await user32.CreateControl(phwnd,{\n    type:'button',\n    id:prefix+'ok',\n    parentId:prefix+'foot',\n    text:'確定',\n    style:{margin:'0',minWidth:'86px'}\n  });\n\n  await user32.CreateControl(phwnd,{\n    type:'button',\n    id:prefix+'cancel',\n    parentId:prefix+'foot',\n    text:'取消',\n    style:{margin:'0',minWidth:'86px'}\n  });\n\n  await user32.CreateControl(phwnd,{\n    type:'button',\n    id:motwApplyId,\n    parentId:prefix+'foot',\n    text:'套用',\n    disabled:true,\n    style:{margin:'0',minWidth:'86px'}\n  });\n\n  user32.OnControl(motwApplyId,async e=>{\n    if(e.action!=='click')return;\n    await applyMotwChange();\n  });\n\n  user32.OnControl(prefix+'ok',async e=>{\n    if(e.action!=='click')return;\n    if(await applyMotwChange()){\n      await user32.DestroyWindow(phwnd);\n    }\n  });\n\n  user32.OnControl(prefix+'cancel',async e=>{\n    if(e.action==='click'){\n      await user32.DestroyWindow(phwnd);\n    }\n  });\n\n  return true;\n};\n\nconst invokeShellVerb=async(\n  verb,\n  paths,\n  targetPath\n)=>{\n  verb=String(verb||'');\n  paths=Array.isArray(paths)\n    ?paths\n    :[];\n\n  if(!verb)return false;\n\n  if(verb==='refresh'){\n    treeLoaded.delete(currentPath);\n    await loadDirectory();\n    return true;\n  }\n\n  if(verb==='newfolder'){\n    const e={action:'click'};\n    await createFolderFromShell();\n    return true;\n  }\n\n  if(verb==='newcsv'){\n    await createFile('新增 CSV','新增試算表.csv','.csv','');\n    return true;\n  }\n\n  if(verb==='newhtml'){\n    await createFile(\n      '新增 HTML',\n      '新增網頁.html',\n      '.html',\n      '<!doctype html>\\n<html lang=\"zh-Hant\">\\n<head><meta charset=\"utf-8\"><title>ExOS HTML</title></head>\\n<body>\\n\\n</body>\\n</html>\\n'\n    );\n    return true;\n  }\n\n  if(verb==='newxsh'){\n    await createFile(\n      '新增 XSH',\n      'newapp.xsh',\n      '.xsh',\n      '/*EXOS_XSH_PE_V1\\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_CUI\\nSubsystemVersion=1.0\\nEntryPoint=main\\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\\nMinimumEngine=V8\\nIcon=res://shell32.dll/xsh\\n*/\\nconsole.log(\"Hello from ExOS XSH\");\\n'\n    );\n    return true;\n  }\n\n  if(verb==='newtext'){\n    await createFile(\n      '新增文字文件',\n      '新增文字文件.txt',\n      '.txt',\n      ''\n    );\n    return true;\n  }\n\n  if(verb==='renamevolume'){\n    const label=await user32.PromptBox('請輸入本機磁碟 C: 的新磁碟名稱：','重新命名磁碟',volumeLabel);\n    if(label===null)return false;\n    if(String(label).length>32){await user32.MessageBox('磁碟名稱太長，請使用 32 個字元以內。','重新命名磁碟');return false;}\n    const result=await shell32.InvokeCommand('renamevolume',['C:\\\\'],{label:String(label)});\n    volumeLabel=String(result&&result.label!==undefined?result.label:label).trim();\n    try{await comctl32.TreeView_SetItem('folderTree','C:\\\\',{text:volumeDisplayName()});}catch(ignoreTreeLabel){}\n    await setStatus('磁碟名稱已變更為 '+volumeDisplayName());\n    return true;\n  }\n\n  if(verb==='rename'){\n    if(paths.length!==1)return false;\n\n    const info=\n      await shell32.SHGetFileInfo(\n        paths[0]\n      );\n\n    const name=\n      await user32.PromptBox(\n        '新的名稱：',\n        '重新命名',\n        info.name\n      );\n\n    if(name===null)return false;\n\n    const dest=joinPath(\n      parentPath(info.path),\n      safeName(name)\n    );\n\n    await shell32.InvokeCommand(\n      'rename',\n      paths,\n      {newPath:dest}\n    );\n\n    await loadDirectory();\n    return true;\n  }\n\n  if(verb==='delete'){\n    if(!paths.length)return false;\n\n    const ok=\n      await user32.ConfirmBox(\n        '確定要刪除選取的 '+\n        paths.length+\n        ' 個項目嗎？',\n        '刪除'\n      );\n\n    if(!ok)return false;\n\n    await shell32.InvokeCommand(\n      'delete',\n      paths,\n      {}\n    );\n\n    treeLoaded.delete(currentPath);\n    await loadDirectory();\n    return true;\n  }\n\n  if(verb==='properties'){\n    if(paths.length!==1)return false;\n    await showPropertiesDialog(paths[0]);\n    return true;\n  }\n\n  if(verb==='copy'||verb==='cut'){\n    await shell32.InvokeCommand(\n      verb,\n      paths,\n      {}\n    );\n\n    await syncShellClipboard();\n    return true;\n  }\n\n  if(verb==='paste'){\n    const destination=\n      targetPath||currentPath;\n\n    const result=\n      await shell32.InvokeCommand(\n        'paste',\n        [],\n        {targetPath:destination}\n      );\n\n    await syncShellClipboard();\n    treeLoaded.delete(currentPath);\n    await loadDirectory();\n\n    await setStatus(\n      '已貼上 '+\n      Number(result.completed||0)+\n      ' 個物件'\n    );\n\n    return true;\n  }\n\n  if(verb==='cmdhere'){\n    await shell32.InvokeCommand(\n      'cmdhere',\n      paths,\n      {\n        targetPath:\n          targetPath||currentPath\n      }\n    );\n\n    return true;\n  }\n\n  await shell32.InvokeCommand(\n    verb,\n    paths,\n    {\n      targetPath:\n        targetPath||currentPath\n    }\n  );\n\n  return true;\n};\n\nconst createFolderFromShell=async()=>{\n  const name=\n    await user32.PromptBox(\n      '請輸入資料夾名稱：',\n      '新增資料夾',\n      '新增資料夾'\n    );\n\n  if(name===null)return false;\n\n  const p=\n    await uniquePath(\n      safeName(name)\n    );\n\n  await kernel32.CreateDirectory(p);\n\n  treeLoaded.delete(currentPath);\n  await loadDirectory();\n  return true;\n};\n\nconst openItem=async item=>{\n  if(!item)return;\n\n  if(item.directory){\n    await navigate(\n      item.path,\n      true\n    );\n    return;\n  }\n\n  try{\n    await setStatus(\n      '正在開啟 '+String(item.name||item.path||'')+'…',\n      0\n    );\n\n    const result=\n      await shell32.ShellExecute(\n        item.path,\n        'open'\n      );\n\n    if(\n      result&&\n      result.executable&&\n      result.pid\n    ){\n      await setStatus(\n        '已啟動 '+String(item.name||'XSH')+\n        ' ｜ PID '+String(result.pid),\n        0\n      );\n    }\n  }catch(err){\n    await setStatus(\n      '開啟失敗：'+String(item.name||item.path||''),\n      0\n    );\n\n    await user32.MessageBox(\n      '無法開啟：'+String(item.path||item.name||'')+\n      '\\n\\n'+\n      String(err&&err.message?err.message:err),\n      extOf(item.name||'')==='xsh'\n        ?'XSH 啟動失敗'\n        :'開啟失敗'\n    );\n  }\n};\n\nconst createFile=async(\n  title,\n  suggested,\n  extension,\n  initialText\n)=>{\n  let name=\n    await user32.PromptBox(\n      '請輸入檔案名稱：',\n      title,\n      suggested\n    );\n\n  if(name===null)return false;\n\n  name=safeName(name);\n\n  if(\n    extension&&\n    !extOf(name)\n  ){\n    name+=extension;\n  }\n\n  const p=\n    await uniquePath(name);\n\n  await kernel32.WriteTextFile(\n    p,\n    String(initialText||'')\n  );\n\n  await loadDirectory();\n  return true;\n};\n\nconst copyEntryRecursive=async(\n  source,\n  destination,\n  depth=0\n)=>{\n  if(depth>32){\n    throw new Error(\n      '資料夾層級太深，已停止複製。'\n    );\n  }\n\n  const attr=\n    await kernel32.GetFileAttributes(\n      source\n    );\n\n  if(!attr.directory){\n    await kernel32.CopyFile(\n      source,\n      destination,\n      false\n    );\n    return;\n  }\n\n  const srcNorm=\n    normalizePath(source);\n\n  const dstNorm=\n    normalizePath(destination);\n\n  if(\n    dstNorm===srcNorm||\n    dstNorm.indexOf(srcNorm+'\\\\')===0\n  ){\n    throw new Error(\n      '不能將資料夾複製到自己的子資料夾。'\n    );\n  }\n\n  await kernel32.CreateDirectory(\n    destination\n  );\n\n  const children=\n    await kernel32.ListDirectory(\n      source\n    );\n\n  for(const child of children){\n    await copyEntryRecursive(\n      child.path,\n      joinPath(\n        destination,\n        child.name\n      ),\n      depth+1\n    );\n  }\n};\n\nconst pasteClipboard=async()=>{\n  if(!clipboard.items.length)return;\n\n  await setBusy(true);\n\n  try{\n    let completed=0;\n    const failed=[];\n\n    for(const item of clipboard.items){\n      let dest;\n\n      try{\n        dest=await uniquePath(\n          item.name\n        );\n\n        if(\n          item.directory&&\n          (\n            currentPath===\n              normalizePath(item.path)||\n            currentPath.indexOf(\n              normalizePath(item.path)+'\\\\'\n            )===0\n          )\n        ){\n          throw new Error(\n            '不能貼到來源資料夾本身或其子資料夾。'\n          );\n        }\n\n        if(clipboard.mode==='cut'){\n          await kernel32.MoveFile(\n            item.path,\n            dest\n          );\n        }else{\n          await copyEntryRecursive(\n            item.path,\n            dest,\n            0\n          );\n        }\n\n        completed++;\n      }catch(err){\n        failed.push(\n          item.name+\n          '：'+\n          String(\n            err&&err.message\n              ?err.message\n              :err\n          )\n        );\n      }\n    }\n\n    if(\n      clipboard.mode==='cut'&&\n      completed===clipboard.items.length\n    ){\n      clipboard.mode='';\n      clipboard.items=[];\n    }\n\n    treeLoaded.delete(currentPath);\n    await loadDirectory();\n\n    await setStatus(\n      '已貼上 '+\n      completed+\n      ' 個項目'+\n      (\n        failed.length\n          ?'；失敗 '+failed.length+' 個'\n          :''\n      )\n    );\n\n    if(failed.length){\n      await user32.MessageBox(\n        failed.join('\\n'),\n        '貼上結果'\n      );\n    }\n  }finally{\n    await setBusy(false);\n  }\n};\n\nconst showProperties=async item=>{\n  if(!item)return;\n\n  const attr=\n    await kernel32.GetFileAttributes(\n      item.path\n    );\n\n  const lines=[\n    '名稱：'+item.name,\n    '類型：'+(\n      item.directory\n        ?'資料夾'\n        :typeLabel(item)\n    ),\n    '位置：'+\n      parentPath(item.path),\n    '完整路徑：'+item.path\n  ];\n\n  if(!item.directory){\n    lines.push(\n      '大小：'+\n      sizeText(\n        Number(attr.size)||item.size\n      )+\n      ' ('+\n      Number(attr.size||item.size||0)+\n      ' bytes)'\n    );\n  }\n\n  if(item.modified){\n    lines.push(\n      '修改日期：'+item.modified\n    );\n  }\n\n  await user32.MessageBox(\n    lines.join('\\n'),\n    '內容'\n  );\n};\n\ncomctl32.OnNotify('fileList',async e=>{\n  if(e.action!=='notify')return;\n\n  if(e.code==='NM_RCLICK_BACKGROUND'){\n    const verb=\n      await shell32.TrackContextMenu(\n        [],\n        e.data.x,\n        e.data.y,\n        {\n          background:true,\n          targetPath:currentPath\n        }\n      );\n\n    await invokeShellVerb(\n      verb,\n      [],\n      currentPath\n    );\n\n    return;\n  }\n\n  if(e.code==='NM_RCLICK'){\n    const rows=\n      await comctl32.ListView_GetSelectedItems(\n        'fileList'\n      );\n\n    selected=rows\n      .map(x=>x.data)\n      .filter(Boolean);\n\n    await syncSelection(rows);\n\n    const paths=selectedPaths();\n\n    const verb=\n      await shell32.TrackContextMenu(\n        paths,\n        e.data.x,\n        e.data.y,\n        {\n          background:false,\n          targetPath:currentPath\n        }\n      );\n\n    const target=\n      selected.length===1&&\n      selected[0].directory\n        ?selected[0].path\n        :currentPath;\n\n    await invokeShellVerb(\n      verb,\n      paths,\n      target\n    );\n\n    return;\n  }\n\n  if(e.code==='LVN_BEGINDRAG'){\n    const rows=\n      await comctl32.ListView_GetSelectedItems(\n        'fileList'\n      );\n\n    selected=rows\n      .map(x=>x.data)\n      .filter(Boolean);\n\n    if(selected.length){\n      await shell32.BeginDragDrop(\n        selectedPaths(),\n        {\n          allowedEffects:[\n            'copy',\n            'move'\n          ]\n        }\n      );\n    }\n\n    return;\n  }\n\n  if(e.code==='LVN_DRAGOVER'){\n    const item=\n      e.data&&e.data.item&&e.data.item.data\n        ?e.data.item.data\n        :null;\n\n    const target=\n      item&&item.directory\n        ?item.path\n        :(item&&String(item.extension||'').toLowerCase()==='zip'\n          ?'zip://'+encodeURIComponent(item.path)+'!/'\n          :currentPath);\n\n    await shell32.DragOver(\n      target,\n      {\n        ctrlKey:!!e.data.ctrlKey\n      }\n    );\n\n    return;\n  }\n\n  if(e.code==='NM_DROP'){\n    const item=\n      e.data&&e.data.item&&e.data.item.data\n        ?e.data.item.data\n        :null;\n\n    const target=\n      item&&item.directory\n        ?item.path\n        :(item&&String(item.extension||'').toLowerCase()==='zip'\n          ?'zip://'+encodeURIComponent(item.path)+'!/'\n          :currentPath);\n\n    if(e.data&&Array.isArray(e.data.externalFiles)){\n      if(/^zip:\\/\\//i.test(String(target||''))){\n        await setStatus('外部檔案請先上傳到 ExFS，或在 ZIP 視窗中直接拖入。',0);\n        return;\n      }\n      await uploadPickedRecords(e.data.externalFiles,target);\n      return;\n    }\n\n    await shell32.Drop(\n      target,\n      {\n        ctrlKey:!!e.data.ctrlKey\n      }\n    );\n\n    treeLoaded.delete(currentPath);\n    await loadDirectory();\n    return;\n  }\n\n  if(e.code==='LVN_COLUMNCLICK'){\n    const c=Number(e.data&&e.data.column);\n    if(Number.isFinite(c)&&c>=0&&c<=3){\n      sortColumn=c;\n      sortAscending=!(e.data&&e.data.ascending===false);\n      const names=['名稱','類型','大小','修改日期'];\n      await setStatus((names[c]||'名稱')+' '+(sortAscending?'↑':'↓'),2);\n    }\n    return;\n  }\n\n  if(e.code==='LVN_KEYDOWN'){\n    const key=String(e.data.key||'').toLowerCase();\n\n    if((e.data.ctrlKey&&key==='l')||(e.data.altKey&&key==='d')||key==='f4'){\n      await showAddressEditor();\n      return;\n    }\n\n    if((e.data.ctrlKey&&(key==='f'||key==='e'))||key==='f3'){\n      await user32.FocusControl('search');\n      return;\n    }\n\n    if(e.data.ctrlKey&&e.data.shiftKey&&key==='n'){\n      await createFolderFromShell();\n      return;\n    }\n\n    if(e.data.ctrlKey&&key==='n'){\n      await shell32.ShellExecute(currentPath,'explore');\n      return;\n    }\n\n    if(e.data.altKey&&key==='p'){\n      previewVisible=!previewVisible;\n      await updateWorkspaceLayout();\n      await refreshCommandState();\n      await updatePreview();\n      return;\n    }\n\n    if(e.data.ctrlKey&&e.data.shiftKey&&key==='e'){\n      treeVisible=!treeVisible;\n      await updateWorkspaceLayout();\n      await refreshCommandState();\n      return;\n    }\n\n    if(key==='escape'&&searchText){\n      searchText='';\n      await user32.SetControlText('search','');\n      await renderList();\n      return;\n    }\n\n    if(e.data.altKey&&key==='enter'&&selected.length===1){\n      await invokeShellVerb('properties',selectedPaths(),currentPath);\n      return;\n    }\n\n    if(key==='enter'&&selected.length===1){\n      await openItem(selected[0]);\n      return;\n    }\n\n    if(key==='backspace'){\n      if(currentPath!=='C:\\\\'){\n        await navigate(parentPath(currentPath),true);\n      }\n      return;\n    }\n\n    if(e.data.ctrlKey&&key==='c'){\n      if(selected.length){\n        await invokeShellVerb(\n          'copy',\n          selectedPaths(),\n          currentPath\n        );\n      }\n      return;\n    }\n\n    if(e.data.ctrlKey&&key==='x'){\n      if(selected.length){\n        await invokeShellVerb(\n          'cut',\n          selectedPaths(),\n          currentPath\n        );\n      }\n      return;\n    }\n\n    if(e.data.ctrlKey&&key==='v'){\n      await invokeShellVerb(\n        'paste',\n        [],\n        currentPath\n      );\n      return;\n    }\n\n    if(e.data.ctrlKey&&key==='a'){\n      await comctl32.ListView_SetSelectedItems(\n        'fileList',\n        visibleEntries().map(x=>x.path)\n      );\n      return;\n    }\n\n    if(key==='delete'&&selected.length){\n      await invokeShellVerb(\n        'delete',\n        selectedPaths(),\n        currentPath\n      );\n      return;\n    }\n\n    if(key==='f2'&&selected.length===1){\n      await invokeShellVerb(\n        'rename',\n        selectedPaths(),\n        currentPath\n      );\n      return;\n    }\n\n    if(key==='f5'){\n      await invokeShellVerb(\n        'refresh',\n        [],\n        currentPath\n      );\n      return;\n    }\n\n    if(e.data.altKey&&key==='arrowleft'&&historyPos>0){\n      historyPos--;\n      currentPath=history[historyPos];\n      await loadDirectory();\n      return;\n    }\n\n    if(e.data.altKey&&key==='arrowright'&&historyPos<history.length-1){\n      historyPos++;\n      currentPath=history[historyPos];\n      await loadDirectory();\n      return;\n    }\n\n    if(e.data.altKey&&key==='arrowup'){\n      await navigate(\n        parentPath(currentPath),\n        true\n      );\n      return;\n    }\n  }\n\n  if(e.code==='LVN_ITEMCHANGED'){\n    await syncSelection(\n      e.data&&Array.isArray(e.data.items)\n        ?e.data.items\n        :[]\n    );\n    return;\n  }\n\n  if(e.code==='LVN_ITEMACTIVATE'){\n    const item=e.data&&e.data.item\n      ?e.data.item\n      :null;\n\n    if(!item||!item.data)return;\n\n    await openItem(\n      item.data\n    );\n  }\n});\n\ncomctl32.OnNotify('folderTree',async e=>{\n  if(e.action!=='notify')return;\n\n  const item=e.data&&e.data.item\n    ?e.data.item\n    :null;\n\n  if(e.code==='NM_RCLICK'&&item&&item.data&&item.data.path){\n    const p=normalizePath(item.data.path);\n    const verb=await shell32.TrackContextMenu([p],e.data.x||0,e.data.y||0,{background:false,targetPath:p});\n    await invokeShellVerb(verb,[p],p);\n    return;\n  }\n\n  if(e.code==='TVN_DROP'&&e.data&&Array.isArray(e.data.externalFiles)){\n    const target=item&&item.data&&item.data.path?item.data.path:currentPath;\n    await uploadPickedRecords(e.data.externalFiles,target);\n    return;\n  }\n\n  if(!item||!item.data)return;\n\n  if(e.code==='TVN_SELCHANGED'&&item.data.systemApp){\n    if(!busy)await ExOS.LaunchSystemApp(String(item.data.systemApp),[]);\n    return;\n  }\n\n  if(!item.data.path)return;\n\n  if(\n    e.code==='TVN_ITEMEXPANDING'&&\n    e.data.expanding\n  ){\n    await loadTreeChildren(\n      item.data.path\n    );\n    return;\n  }\n\n  if(e.code==='TVN_DRAGOVER'){\n    await shell32.DragOver(\n      item.data.path,\n      {\n        ctrlKey:!!e.data.ctrlKey\n      }\n    );\n    return;\n  }\n\n  if(e.code==='TVN_DROP'){\n    await shell32.Drop(\n      item.data.path,\n      {\n        ctrlKey:!!e.data.ctrlKey\n      }\n    );\n\n    treeLoaded.delete(item.data.path);\n    treeLoaded.delete(currentPath);\n    await loadDirectory();\n    return;\n  }\n\n  if(e.code==='TVN_SELCHANGED'){\n    const p=normalizePath(\n      item.data.path\n    );\n\n    if(p!==currentPath&&!busy){\n      await navigate(p,true);\n    }\n  }\n});\n\nuser32.OnControl('back',async e=>{\n  if(e.action!=='click'||busy)return;\n  if(historyPos>0){historyPos--;currentPath=history[historyPos];await loadDirectory();}\n});\n\nuser32.OnControl('forward',async e=>{\n  if(e.action!=='click'||busy)return;\n  if(historyPos<history.length-1){historyPos++;currentPath=history[historyPos];await loadDirectory();}\n});\n\nuser32.OnControl('up',async e=>{\n  if(e.action!=='click'||busy)return;\n  await navigate(parentPath(currentPath),true);\n});\n\nuser32.OnControl('home',async e=>{\n  if(e.action!=='click'||busy)return;\n  await navigate('C:\\\\',true);\n});\n\nuser32.OnControl('refresh',async e=>{\n  if(e.action!=='click'||busy)return;\n  treeLoaded.delete(currentPath);\n  await loadDirectory();\n});\n\nuser32.OnControl('addressDisplay',async e=>{\n  if((e.action==='click'||e.action==='dblclick')&&!busy)await showAddressEditor();\n});\n\nuser32.OnControl('address',async e=>{\n  if(e.action==='keydown'&&e.key==='Enter'&&!busy){\n    try{\n      await navigate(e.value,true);\n      await hideAddressEditor();\n    }catch(err){\n      await user32.MessageBox(String(err&&err.message?err.message:err),'無法前往此位置');\n      await user32.SetControlText('address',currentPath);\n    }\n    return;\n  }\n  if(e.action==='keydown'&&e.key==='Escape'){\n    await user32.SetControlText('address',currentPath);\n    await hideAddressEditor();\n    return;\n  }\n  if(e.action==='change'&&!busy){\n    await user32.SetControlText('address',currentPath);\n    await hideAddressEditor();\n  }\n});\n\nlet searchTimer=0;\nuser32.OnControl('search',async e=>{\n  if(e.action==='keydown'&&e.key==='Escape'){\n    searchText='';\n    await user32.SetControlText('search','');\n    await renderList();\n    return;\n  }\n  if(e.action!=='input')return;\n  searchText=String(e.value||'');\n  if(searchTimer)clearTimeout(searchTimer);\n  searchTimer=setTimeout(()=>{renderList().catch(()=>{});},120);\n});\n\nuser32.OnControl('clear_search',async e=>{\n  if(e.action!=='click'||busy)return;\n  searchText='';\n  await user32.SetControlText('search','');\n  await renderList();\n  await user32.FocusControl('search');\n});\n\nconst setExplorerView=async view=>{\n  viewMode=view;\n  await comctl32.ListView_SetView('fileList',viewMode);\n  await refreshCommandState();\n  await setStatus(viewName(),2);\n};\n\nasync function uploadPickedRecords(picked,targetFolder=currentPath){\n  const ONE_GIB=1024*1024*1024;\n  targetFolder=normalizePath(targetFolder||currentPath);\n  if(!picked||!picked.length)return;\n\n  await setBusy(true);\n  try{\n    let ok=0,totalBytes=0,chunked=0;\n    const skipped=[];\n\n    for(const f of picked){\n      const token=String(f.fileToken||'');\n      try{\n        if(f.error){\n          skipped.push(\n            f.name+'：'+(\n              f.error==='FILE_TOO_LARGE'\n                ?'單一檔案超過 1 GiB 上限'\n                :f.error\n            )\n          );\n          continue;\n        }\n        if(!token){\n          skipped.push(f.name+'：瀏覽器檔案 Token 建立失敗');\n          continue;\n        }\n\n        const expected=Number(f.size)||0;\n        if(expected<0||expected>ONE_GIB){\n          skipped.push(f.name+'：檔案大小超過 1 GiB 上限');\n          continue;\n        }\n\n        const p=await uniquePath(safeName(f.name),targetFolder);\n        await setStatus(\n          '正在上傳 '+f.name+' 到 '+targetFolder+'（'+sizeText(expected)+'）…',\n          0\n        );\n\n        const result=await ExOS.UploadPickedFile(token,p);\n        if(!result||!result.ok){\n          throw new Error('ExOS upload pipeline did not return success.');\n        }\n        if(Number(result.size)!==expected){\n          throw new Error(\n            '上傳結果大小不符：'+String(result.size||0)+' / '+expected+' bytes'\n          );\n        }\n\n        const attr=await kernel32.GetFileAttributes(p);\n        if(!attr||Number(attr.size)!==expected){\n          throw new Error(\n            'ExFS metadata 大小驗證失敗：'+String(attr?attr.size:0)+' / '+expected+' bytes'\n          );\n        }\n\n        if(String(result.storageMode||'')==='CHUNKED_V1')chunked++;\n        ok++;\n        totalBytes+=expected;\n      }catch(err){\n        skipped.push(f.name+'：'+String(err&&err.message?err.message:err));\n      }finally{\n        if(token){\n          try{await ExOS.ReleasePickedFile(token);}catch(ignoreRelease){}\n        }\n      }\n    }\n\n    treeLoaded.delete(targetFolder);\n    treeLoaded.delete(currentPath);\n    if(targetFolder===currentPath)await loadDirectory();\n    else{\n      try{await loadTreeChildren(parentPath(targetFolder));}catch(ignoreTreeRefresh){}\n      await setStatus('已將檔案拖放上傳到 '+targetFolder,0);\n    }\n    await setStatus(\n      '已上傳 '+ok+' 個檔案，共 '+sizeText(totalBytes)+\n      (chunked?'；大型分塊檔案 '+chunked+' 個':'')+\n      (skipped.length?'；失敗 '+skipped.length+' 個':'')\n    );\n    if(skipped.length)await user32.MessageBox(skipped.join('\\n'),'上傳結果');\n  }finally{\n    await setBusy(false);\n  }\n}\n\nconst uploadIntoCurrentFolder=async()=>{\n  const ONE_GIB=1024*1024*1024;\n  const picked=await user32.PickFiles({\n    multiple:true,\n    returnFileToken:true,\n    maxBytes:ONE_GIB\n  });\n  await uploadPickedRecords(picked,currentPath);\n};\n\ncomctl32.OnNotify('commandBar',async e=>{\n  if(e.action!=='notify'||e.code!=='TBN_CLICK'||busy)return;\n  const id=String(e.data&&e.data.button?e.data.button.id:'');\n\n  if(id==='newfolder'){await createFolderFromShell();return;}\n  if(id==='newtext'){await invokeShellVerb('newtext',[],currentPath);return;}\n  if(id==='newcsv'){await invokeShellVerb('newcsv',[],currentPath);return;}\n  if(id==='newhtml'){await invokeShellVerb('newhtml',[],currentPath);return;}\n  if(id==='cmdhere'){await invokeShellVerb('cmdhere',[],currentPath);return;}\n  if(id==='copypath'){\n    const value=selected.length?selected.map(x=>x.path).join('\\r\\n'):currentPath;\n    try{await user32.OpenClipboard(hwnd);await user32.EmptyClipboard();await user32.SetClipboardData(user32.CF_UNICODETEXT,value);await user32.CloseClipboard();await setStatus('已複製完整路徑',0);}catch(err){try{await user32.CloseClipboard();}catch(ignoreClipboard){}await user32.MessageBox(String(err&&err.message?err.message:err),'複製路徑');}\n    return;\n  }\n  if(id==='upload'){await uploadIntoCurrentFolder();return;}\n  if(id==='cut'&&selected.length){await invokeShellVerb('cut',selectedPaths(),currentPath);return;}\n  if(id==='copy'&&selected.length){await invokeShellVerb('copy',selectedPaths(),currentPath);return;}\n  if(id==='paste'){await invokeShellVerb('paste',[],currentPath);return;}\n  if(id==='rename'&&selected.length===1){await invokeShellVerb('rename',selectedPaths(),currentPath);return;}\n  if(id==='delete'&&selected.length){await invokeShellVerb('delete',selectedPaths(),currentPath);return;}\n  if(id==='download'&&selected.length){await invokeShellVerb('download',selected.filter(x=>!x.directory).map(x=>x.path),currentPath);return;}\n  if(id==='properties'&&selected.length===1){await invokeShellVerb('properties',selectedPaths(),currentPath);return;}\n  if(id==='selectall'){await comctl32.ListView_SetSelectedItems('fileList',visibleEntries().map(x=>x.path));return;}\n\n  if(id==='tree'){\n    treeVisible=!treeVisible;\n    await updateWorkspaceLayout();\n    await refreshCommandState();\n    return;\n  }\n\n  if(id==='preview'){\n    previewVisible=!previewVisible;\n    await updateWorkspaceLayout();\n    await refreshCommandState();\n    await updatePreview();\n    return;\n  }\n\n  if(id==='view_details'){await setExplorerView(comctl32.LV_VIEW_DETAILS);return;}\n  if(id==='view_list'){await setExplorerView(comctl32.LV_VIEW_LIST);return;}\n  if(id==='view_icons'){await setExplorerView(comctl32.LV_VIEW_ICON);return;}\n});\n\nawait loadDirectory();\nawait updateWorkspaceLayout();\nawait syncShellClipboard();\n"},"cmd":{"id":"cmd","fileName":"cmd.xsh","title":"命令提示字元","description":"ExOS Command Prompt (.xsh / CMD-compatible .xba batch runtime)","icon":"cmd","singleInstance":false,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":3,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_CUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_CUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst stdin=\n  await kernel32.GetStdHandle(\n    kernel32.STD_INPUT_HANDLE\n  );\n\nconst stdout=\n  await kernel32.GetStdHandle(\n    kernel32.STD_OUTPUT_HANDLE\n  );\n\nconst shell32=ExOS.LoadLibrary('shell32.dll');\n\nconst stderr=\n  await kernel32.GetStdHandle(\n    kernel32.STD_ERROR_HANDLE\n  );\n\nawait kernel32.SetConsoleTitle(\n  '命令提示字元 - ExOS cmd.xsh'\n);\n\nlet promptSpec='$P$G';\nlet errorLevel=0;\nlet echoEnabled=true;\nconst directoryStack=[];\nconst localEnvironmentStack=[];\n\nconst utf8Decoder=new TextDecoder('utf-8');\n\nconst writeHandle=async(handle,value)=>{\n  const text=String(value??'');\n\n  if(\n    handle===kernel32.STD_OUTPUT_HANDLE||\n    handle===kernel32.STD_ERROR_HANDLE\n  ){\n    return kernel32.WriteConsole(\n      handle,\n      text\n    );\n  }\n\n  if(!handle){\n    throw new Error(\n      'The standard output handle is invalid.'\n    );\n  }\n\n  return kernel32.WriteFile(\n    handle,\n    text\n  );\n};\n\nconst readHandle=async(handle,maxBytes=8192)=>{\n  if(handle===kernel32.STD_INPUT_HANDLE){\n    return String(\n      await kernel32.ReadConsole(\n        handle,\n        maxBytes\n      )||''\n    );\n  }\n\n  if(!handle){\n    throw new Error(\n      'The standard input handle is invalid.'\n    );\n  }\n\n  const r=await kernel32.ReadFile(\n    handle,\n    Math.max(\n      1,\n      Math.min(\n        1024*1024,\n        Number(maxBytes)||8192\n      )\n    )\n  );\n\n  if(r&&typeof r.text==='string'){\n    return r.text;\n  }\n\n  return utf8Decoder.decode(\n    new Uint8Array(\n      r&&Array.isArray(r.data)\n        ?r.data\n        :[]\n    )\n  );\n};\n\nconst readStd=async(maxBytes=8192)=>\n  readHandle(\n    await kernel32.GetStdHandle(\n      kernel32.STD_INPUT_HANDLE\n    ),\n    maxBytes\n  );\n\nconst readAllStdIn=async()=>{\n  const h=await kernel32.GetStdHandle(\n    kernel32.STD_INPUT_HANDLE\n  );\n\n  if(h===kernel32.STD_INPUT_HANDLE){\n    return readHandle(h,1024*1024);\n  }\n\n  let out='';\n\n  for(let guard=0;guard<1024;guard++){\n    const r=await kernel32.ReadFile(\n      h,\n      64*1024\n    );\n\n    const part=\n      r&&typeof r.text==='string'\n        ?r.text\n        :utf8Decoder.decode(\n            new Uint8Array(\n              r&&Array.isArray(r.data)\n                ?r.data\n                :[]\n            )\n          );\n\n    out+=part;\n\n    if(\n      !r||\n      r.eof||\n      !r.bytesRead\n    ){\n      break;\n    }\n\n    if(out.length>16*1024*1024){\n      throw new Error(\n        'STDIN exceeded the 16 MiB CMD stream limit.'\n      );\n    }\n  }\n\n  return out;\n};\n\nconst write=async s=>\n  writeHandle(\n    await kernel32.GetStdHandle(\n      kernel32.STD_OUTPUT_HANDLE\n    ),\n    String(s??'')\n  );\n\nconst writeln=async s=>\n  write(\n    String(s??'')+\n    '\\r\\n'\n  );\n\nconst writeError=async s=>\n  writeHandle(\n    await kernel32.GetStdHandle(\n      kernel32.STD_ERROR_HANDLE\n    ),\n    String(s??'')+\n    '\\r\\n'\n  );\n\nconst argv=s=>{\n  const out=[];\n  const re=/\"([^\"]*)\"|'([^']*)'|([^\\s]+)/g;\n  let m;\n\n  while((m=re.exec(String(s||'')))!==null){\n    out.push(\n      m[1]!==undefined\n        ?m[1]\n        :(m[2]!==undefined?m[2]:m[3])\n    );\n  }\n\n  return out;\n};\n\nconst quote=s=>{\n  s=String(s||'');\n\n  return /[\\s\"]/g.test(s)\n    ?'\"'+s.replace(/\"/g,'\\\\\"')+'\"'\n    :s;\n};\n\nconst cwd=()=>kernel32.GetCurrentDirectory();\n\nconst systemPath=name=>\n  'C:\\\\ExOS\\\\SystemApps\\\\'+name;\n\nconst startBuiltin=async(name,args=[])=>{\n  const line=args.map(quote).join(' ');\n\n  return kernel32.CreateProcess(\n    systemPath(name),\n    line\n  );\n};\n\nconst parentPath=p=>{\n  p=String(p||'').replace(/\\//g,'\\\\');\n\n  if(/^C:\\\\?$/i.test(p))return'C:\\\\';\n\n  p=p.replace(/\\\\+$/,'');\n  const pos=p.lastIndexOf('\\\\');\n\n  return pos<=2?'C:\\\\':p.substring(0,pos);\n};\n\nconst baseName=p=>{\n  p=String(p||'').replace(/\\//g,'\\\\').replace(/\\\\+$/,'');\n  const pos=p.lastIndexOf('\\\\');\n  return pos>=0?p.substring(pos+1):p;\n};\n\nconst numberText=n=>\n  Math.max(0,Number(n)||0)\n    .toLocaleString('en-US');\n\nconst sizeText=n=>{\n  n=Math.max(0,Number(n)||0);\n\n  if(n<1024)return n+' bytes';\n  if(n<1024*1024)return(n/1024).toFixed(1)+' KB';\n  if(n<1024*1024*1024)return(n/1024/1024).toFixed(1)+' MB';\n  return(n/1024/1024/1024).toFixed(2)+' GB';\n};\n\nconst dosDateTime=value=>{\n  const d=new Date(value||Date.now());\n\n  if(Number.isNaN(d.getTime())){\n    return{\n      date:'          ',\n      time:'        '\n    };\n  }\n\n  const mm=String(d.getMonth()+1).padStart(2,'0');\n  const dd=String(d.getDate()).padStart(2,'0');\n  const yy=String(d.getFullYear());\n  let hour=d.getHours();\n  const ap=hour>=12?'PM':'AM';\n  hour=hour%12;\n  if(hour===0)hour=12;\n\n  return{\n    date:mm+'/'+dd+'/'+yy,\n    time:\n      String(hour).padStart(2,' ')+\n      ':'+\n      String(d.getMinutes()).padStart(2,'0')+\n      ' '+\n      ap\n  };\n};\n\nconst wildcardRegex=pattern=>{\n  pattern=String(pattern||'*');\n\n  const escaped=pattern.replace(\n    /[.+^${}()|[\\]\\\\]/g,\n    '\\\\$&'\n  );\n\n  return new RegExp(\n    '^'+\n    escaped\n      .replace(/\\*/g,'.*')\n      .replace(/\\?/g,'.')+\n    '$',\n    'i'\n  );\n};\n\nconst splitWildcard=async spec=>{\n  spec=String(spec||'').replace(/\\//g,'\\\\');\n\n  if(!spec)return{\n    directory:await cwd(),\n    pattern:'*'\n  };\n\n  if(!/[*?]/.test(spec)){\n    try{\n      const attr=await kernel32.GetFileAttributes(spec);\n\n      if(attr.directory){\n        return{\n          directory:spec,\n          pattern:'*'\n        };\n      }\n\n      return{\n        directory:parentPath(spec),\n        pattern:baseName(spec)\n      };\n    }catch(ignoreAttr){\n      return{\n        directory:parentPath(spec),\n        pattern:baseName(spec)\n      };\n    }\n  }\n\n  const pos=spec.lastIndexOf('\\\\');\n\n  if(pos<0){\n    return{\n      directory:await cwd(),\n      pattern:spec\n    };\n  }\n\n  return{\n    directory:\n      pos===2\n        ?spec.substring(0,3)\n        :spec.substring(0,pos),\n    pattern:spec.substring(pos+1)||'*'\n  };\n};\n\nconst expandEnv=async line=>{\n  const source=String(line||'');\n  const names=[];\n  const seen={};\n  let m;\n\n  const re=/%([^%]+)%/g;\n\n  while((m=re.exec(source))!==null){\n    const name=String(m[1]||'').toUpperCase();\n\n    if(!seen[name]){\n      seen[name]=1;\n      names.push(name);\n    }\n  }\n\n  const values={};\n\n  for(const name of names){\n    if(name==='CD'){\n      values[name]=await cwd();\n    }else if(name==='ERRORLEVEL'){\n      values[name]=String(errorLevel);\n    }else if(name==='DATE'){\n      values[name]=new Date().toLocaleDateString();\n    }else if(name==='TIME'){\n      values[name]=new Date().toLocaleTimeString();\n    }else{\n      const v=await kernel32.GetEnvironmentVariable(name);\n      values[name]=v===null?'':String(v);\n    }\n  }\n\n  return source.replace(\n    /%([^%]+)%/g,\n    (all,name)=>\n      Object.prototype.hasOwnProperty.call(\n        values,\n        String(name).toUpperCase()\n      )\n        ?values[String(name).toUpperCase()]\n        :all\n  );\n};\n\nconst promptText=async()=>{\n  const p=String(promptSpec||'$P$G');\n  const now=new Date();\n  const current=await cwd();\n\n  const map={\n    '$A':'&',\n    '$B':'|',\n    '$C':'(',\n    '$D':now.toLocaleDateString(),\n    '$E':'\\x1b',\n    '$F':')',\n    '$G':'>',\n    '$L':'<',\n    '$N':String(current).charAt(0).toUpperCase(),\n    '$P':current,\n    '$Q':'=',\n    '$S':' ',\n    '$T':now.toLocaleTimeString(),\n    '$V':'ExOS 6.4.0-dev-os84',\n    '$_':'\\r\\n',\n    '$$':'$'\n  };\n\n  return p.replace(\n    /\\$[A-Z_$]/gi,\n    token=>\n      Object.prototype.hasOwnProperty.call(\n        map,\n        token.toUpperCase()\n      )\n        ?map[token.toUpperCase()]\n        :token\n  );\n};\n\nconst dosError=err=>{\n  const msg=String(\n    err&&err.message\n      ?err.message\n      :err||''\n  );\n\n  const lower=msg.toLowerCase();\n\n  if(\n    lower.includes('access denied')||\n    lower.includes('denied')\n  ){\n    return'Access is denied.';\n  }\n\n  if(\n    lower.includes('directory not found')||\n    lower.includes('path not found')\n  ){\n    return'The system cannot find the path specified.';\n  }\n\n  if(\n    lower.includes('file not found')||\n    lower.includes('object not found')||\n    lower.includes('not found')\n  ){\n    return'The system cannot find the file specified.';\n  }\n\n  if(\n    lower.includes('already exists')||\n    lower.includes('collision')\n  ){\n    return'A subdirectory or file with that name already exists.';\n  }\n\n  if(\n    lower.includes('invalid')||\n    lower.includes('syntax')\n  ){\n    return'The syntax of the command is incorrect.';\n  }\n\n  return msg||'The command failed.';\n};\n\nconst printDir=async argLine=>{\n  const tokens=argv(argLine);\n  let wide=false;\n  let bare=false;\n  let dirsOnly=false;\n  let filesOnly=false;\n  let spec='';\n\n  for(const t of tokens){\n    const u=String(t).toUpperCase();\n\n    if(u==='/W'){\n      wide=true;\n      continue;\n    }\n\n    if(u==='/B'){\n      bare=true;\n      continue;\n    }\n\n    if(u==='/A:D'){\n      dirsOnly=true;\n      continue;\n    }\n\n    if(u==='/A:-D'){\n      filesOnly=true;\n      continue;\n    }\n\n    if(/^\\/[A-Z]/.test(u)){\n      continue;\n    }\n\n    if(!spec)spec=t;\n  }\n\n  const target=await splitWildcard(spec);\n  const rx=wildcardRegex(target.pattern);\n  let list=await kernel32.ListDirectory(\n    target.directory\n  );\n\n  list=list.filter(e=>\n    rx.test(String(e.name||''))&&\n    (!dirsOnly||e.directory)&&\n    (!filesOnly||!e.directory)\n  );\n\n  if(bare){\n    for(const e of list){\n      await writeln(e.name);\n    }\n\n    if(!list.length){\n      await writeln('File Not Found');\n    }\n\n    return;\n  }\n\n  if(!wide){\n    const vdo=await exes.QuerySystemVdo();\n\n    await writeln(\n      ' Volume in drive C is '+\n      String(vdo&&vdo.label?vdo.label:'ExFS')\n    );\n    await writeln(\n      ' Volume Serial Number is EXOS-EXFS'\n    );\n    await writeln('');\n    await writeln(\n      ' Directory of '+\n      target.directory\n    );\n    await writeln('');\n  }\n\n  if(!list.length){\n    await writeln('File Not Found');\n    return;\n  }\n\n  if(wide){\n    let row=[];\n\n    for(const e of list){\n      const name=\n        e.directory\n          ?'['+e.name+']'\n          :e.name;\n\n      row.push(\n        name.length>22\n          ?name.substring(0,21)+'…'\n          :name.padEnd(22,' ')\n      );\n\n      if(row.length===4){\n        await writeln(row.join(' '));\n        row=[];\n      }\n    }\n\n    if(row.length){\n      await writeln(row.join(' '));\n    }\n\n    return;\n  }\n\n  let files=0;\n  let dirs=0;\n  let total=0;\n\n  for(const e of list){\n    const dt=dosDateTime(e.modified);\n\n    if(e.directory){\n      dirs++;\n\n      await writeln(\n        dt.date+\n        '  '+\n        dt.time+\n        '    <DIR>          '+\n        e.name\n      );\n    }else{\n      files++;\n      total+=Number(e.size)||0;\n\n      await writeln(\n        dt.date+\n        '  '+\n        dt.time+\n        '    '+\n        numberText(e.size).padStart(14,' ')+\n        ' '+\n        e.name\n      );\n    }\n  }\n\n  await writeln('');\n  await writeln(\n    '               '+\n    files+\n    ' File(s) '+\n    numberText(total)+\n    ' bytes'\n  );\n  await writeln(\n    '               '+\n    dirs+\n    ' Dir(s)'\n  );\n};\n\nconst printTree=async(path,depth=0)=>{\n  if(depth>16)return;\n\n  const list=await kernel32.ListDirectory(path);\n\n  for(const e of list){\n    await writeln(\n      (depth?'│   '.repeat(depth):'')+\n      (e.directory?'├── ':'    ')+\n      e.name\n    );\n\n    if(e.directory){\n      await printTree(\n        e.path,\n        depth+1\n      );\n    }\n  }\n};\n\nconst deletePattern=async spec=>{\n  const target=await splitWildcard(spec);\n  const rx=wildcardRegex(target.pattern);\n  const list=await kernel32.ListDirectory(\n    target.directory\n  );\n\n  let deleted=0;\n\n  for(const e of list){\n    if(\n      !e.directory&&\n      rx.test(String(e.name||''))\n    ){\n      await kernel32.DeleteFile(e.path);\n      deleted++;\n    }\n  }\n\n  if(!deleted){\n    throw new Error('File not found.');\n  }\n\n  return deleted;\n};\n\nconst removeTree=async(path,depth=0)=>{\n  if(depth>32){\n    throw new Error(\n      'Directory tree is too deep.'\n    );\n  }\n\n  const attr=\n    await kernel32.GetFileAttributes(\n      path\n    );\n\n  if(!attr.directory){\n    await kernel32.DeleteFile(path);\n    return;\n  }\n\n  const list=\n    await kernel32.ListDirectory(\n      path\n    );\n\n  for(const e of list){\n    if(e.directory){\n      await removeTree(\n        e.path,\n        depth+1\n      );\n    }else{\n      await kernel32.DeleteFile(\n        e.path\n      );\n    }\n  }\n\n  await kernel32.RemoveDirectory(\n    path\n  );\n};\n\nconst xcopyTree=async(source,dest,depth=0)=>{\n  if(depth>32){\n    throw new Error(\n      'Directory tree is too deep.'\n    );\n  }\n\n  const attr=\n    await kernel32.GetFileAttributes(\n      source\n    );\n\n  if(!attr.directory){\n    await kernel32.CopyFile(\n      source,\n      dest,\n      false\n    );\n    return 1;\n  }\n\n  try{\n    await kernel32.CreateDirectory(\n      dest\n    );\n  }catch(ignoreExists){}\n\n  const list=\n    await kernel32.ListDirectory(\n      source\n    );\n\n  let copied=0;\n\n  for(const e of list){\n    const target=\n      String(dest).replace(/\\\\+$/,'')+\n      '\\\\'+\n      e.name;\n\n    if(e.directory){\n      copied+=await xcopyTree(\n        e.path,\n        target,\n        depth+1\n      );\n    }else{\n      await kernel32.CopyFile(\n        e.path,\n        target,\n        false\n      );\n      copied++;\n    }\n  }\n\n  return copied;\n};\n\nconst findText=async(\n  needle,\n  paths,\n  regexMode=false,\n  ignoreCase=false\n)=>{\n  let found=0;\n\n  for(const p of paths){\n    const text=\n      await kernel32.ReadTextFile(p);\n\n    const lines=\n      String(text).split(/\\r?\\n/);\n\n    for(let i=0;i<lines.length;i++){\n      const line=lines[i];\n\n      let hit=false;\n\n      if(regexMode){\n        const rx=new RegExp(\n          needle,\n          ignoreCase?'i':''\n        );\n        hit=rx.test(line);\n      }else{\n        hit=ignoreCase\n          ?line.toLowerCase().includes(\n              needle.toLowerCase()\n            )\n          :line.includes(needle);\n      }\n\n      if(hit){\n        found++;\n\n        await writeln(\n          paths.length>1\n            ?(\n              '---------- '+\n              p+\n              ':'+\n              (i+1)\n            )\n            :'['+(i+1)+']'\n        );\n\n        await writeln(line);\n      }\n    }\n  }\n\n  return found;\n};\n\nconst helpCommand=async name=>{\n  const key=String(name||'')\n    .toUpperCase();\n\n  const docs={\n    DIR:'DIR [drive:][path][file] [/W] [/B] [/A:D] [/A:-D]\\\\r\\\\nDisplays a list of files and subdirectories.',\n    CD:'CD [/D] [path]\\\\r\\\\nDisplays or changes the current directory.',\n    COPY:'COPY source destination\\\\r\\\\nCopies one file.',\n    XCOPY:'XCOPY source destination [/E]\\\\r\\\\nCopies a directory tree.',\n    DEL:'DEL filespec\\\\r\\\\nDeletes files. Wildcards are supported.',\n    RD:'RD [/S] [/Q] directory\\\\r\\\\nRemoves a directory. /S removes its tree.',\n    SET:'SET [name=[value]]\\\\r\\\\nSET /P name=prompt reads a value. SET /A name=expression evaluates integer arithmetic.',\n    FIND:'FIND [/I] \"text\" file [file...]\\\\r\\\\nSearches for a literal text string.',\n    FINDSTR:'FINDSTR [/I] pattern file [file...]\\\\r\\\\nSearches using a JavaScript regular expression.',\n    FC:'FC file1 file2\\\\r\\\\nCompares two text files.',\n    WHERE:'WHERE filename\\\\r\\\\nSearches the current directory and PATH.',\n    PUSHD:'PUSHD path\\\\r\\\\nSaves the current directory and changes to path.',\n    POPD:'POPD\\\\r\\\\nReturns to the directory saved by PUSHD.',\n    CHCP:'CHCP\\\\r\\\\nDisplays the active ExOS console code page.',\n    DOSKEY:'DOSKEY /HISTORY | /LISTSIZE=n | /REINSTALL\\\\r\\\\nManages command history.',\n    START:'START path|program\\\\r\\\\nStarts an XSH application or opens a document.',\n    NOTEPAD:'NOTEPAD [file]\\\\r\\\\nStarts Notepad. Without a path, opens a new untitled document.'\n  };\n\n  if(docs[key]){\n    await writeln(\n      docs[key]\n    );\n    return true;\n  }\n\n  return false;\n};\n\nconst whereCommand=async name=>{\n  name=String(name||'').trim();\n\n  if(!name){\n    throw new Error(\n      'The syntax of the command is incorrect.'\n    );\n  }\n\n  const dirs=[\n    await cwd()\n  ];\n\n  const pathEnv=\n    await kernel32.GetEnvironmentVariable(\n      'PATH'\n    );\n\n  if(pathEnv){\n    for(const d of String(pathEnv).split(';')){\n      if(d.trim())dirs.push(d.trim());\n    }\n  }\n\n  const candidates=[name];\n\n  if(!/\\.[^\\\\]+$/.test(name)){\n    candidates.push(name+'.xsh');\n    candidates.push(name+'.txt');\n  }\n\n  const seen={};\n  let found=0;\n\n  for(const dir of dirs){\n    for(const candidate of candidates){\n      const p=\n        String(dir).replace(/\\\\+$/,'')+\n        '\\\\'+\n        candidate;\n\n      const key=p.toLowerCase();\n\n      if(seen[key])continue;\n      seen[key]=1;\n\n      try{\n        const attr=\n          await kernel32.GetFileAttributes(p);\n\n        if(!attr.directory){\n          found++;\n          await writeln(p);\n        }\n      }catch(ignoreWhere){}\n    }\n  }\n\n  if(!found){\n    throw new Error(\n      'INFO: Could not find files for the given pattern(s).'\n    );\n  }\n\n  return found;\n};\n\nconst evaluateSetA=async expression=>{\n  let expr=String(expression||'').trim();\n\n  expr=expr.replace(\n    /\\b[A-Za-z_][A-Za-z0-9_]*\\b/g,\n    name=>{\n      const key=\n        String(name).toUpperCase();\n\n      if(\n        key==='TRUE'||\n        key==='FALSE'\n      ){\n        return key==='TRUE'?'1':'0';\n      }\n\n      return '0';\n    }\n  );\n\n  if(\n    !/^[0-9A-Fa-fxX+\\-*/%()<>|&^~\\s]+$/.test(\n      expr\n    )\n  ){\n    throw new Error(\n      'Invalid arithmetic expression.'\n    );\n  }\n\n  const value=\n    Function(\n      '\"use strict\";return (('+\n      expr+\n      ')|0);'\n    )();\n\n  return Number(value)|0;\n};\n\nconst restoreEnvironment=async snapshot=>{\n  const current=await kernel32.GetEnvironmentStrings();\n  const target=snapshot||{};\n\n  for(const name of Object.keys(current||{})){\n    if(!Object.prototype.hasOwnProperty.call(target,name)){\n      await kernel32.SetEnvironmentVariable(name,null);\n    }\n  }\n\n  for(const [name,value] of Object.entries(target)){\n    await kernel32.SetEnvironmentVariable(name,String(value));\n  }\n};\n\nconst scanDirectory=async(path,depth=0,seen=new Set())=>{\n  if(depth>64)throw new Error('Directory depth limit exceeded.');\n\n  const key=String(path||'').toLowerCase();\n  if(seen.has(key))return{files:0,dirs:0,bytes:0,errors:1};\n  seen.add(key);\n\n  let list=[];\n  try{\n    list=await kernel32.ListDirectory(path);\n  }catch(err){\n    return{files:0,dirs:0,bytes:0,errors:1};\n  }\n\n  const result={files:0,dirs:0,bytes:0,errors:0};\n\n  for(const item of list){\n    if(item.directory){\n      result.dirs++;\n      const child=await scanDirectory(item.path,depth+1,seen);\n      result.files+=child.files;\n      result.dirs+=child.dirs;\n      result.bytes+=child.bytes;\n      result.errors+=child.errors;\n    }else{\n      result.files++;\n      result.bytes+=Number(item.size)||0;\n    }\n  }\n\n  return result;\n};\n\nconst parseChoiceArgs=parts=>{\n  const out={choices:'YN',message:'',defaultChoice:'',timeout:-1};\n\n  for(let i=0;i<parts.length;i++){\n    const p=String(parts[i]);\n    const u=p.toUpperCase();\n\n    if(u==='/C'&&i+1<parts.length)out.choices=String(parts[++i]).toUpperCase();\n    else if(u==='/M'&&i+1<parts.length)out.message=String(parts[++i]);\n    else if(u==='/D'&&i+1<parts.length)out.defaultChoice=String(parts[++i]).toUpperCase().charAt(0);\n    else if(u==='/T'&&i+1<parts.length)out.timeout=Math.max(0,parseInt(parts[++i],10)||0);\n  }\n\n  out.choices=Array.from(new Set(out.choices.split('').filter(Boolean))).join('');\n  if(!out.choices)out.choices='YN';\n\n  return out;\n};\n\nconst printHelp=async()=>{\n  await writeln('For more information on a specific command, type HELP command-name.');\n  await writeln('');\n  await writeln('CD       Displays or changes the current directory.');\n  await writeln('CLS      Clears the screen.');\n  await writeln('COLOR    Sets the default console foreground/background colors.');\n  await writeln('COPY     Copies a file.');\n  await writeln('ASSOC    Displays an ExOS Shell file association.');\n  await writeln('CHCP     Displays the active console code page.');\n  await writeln('CHKDSK   Scans an ExFS directory tree (read-only).');\n  await writeln('CHOICE   Waits for one key from a choice list.');\n  await writeln('DATE     Displays the current date.');\n  await writeln('DEL      Deletes one or more files. Wildcards are supported.');\n  await writeln('DIR      Displays files and subdirectories. /W /B /A:D /A:-D');\n  await writeln('FC       Compares two text files.');\n  await writeln('FIND     Searches for a text string in files.');\n  await writeln('FINDSTR  Searches files using a regular expression.');\n  await writeln('DOSKEY   /HISTORY, /LISTSIZE=n, /REINSTALL.');\n  await writeln('ECHO     Displays messages, or turns command echoing on/off.');\n  await writeln('EXIT     Exits cmd.xsh.');\n  await writeln('MD       Creates a directory.');\n  await writeln('MORE     Displays a text file.');\n  await writeln('SORT     Sorts text lines.');\n  await writeln('COMP     Compares two files.');\n  await writeln('ROBOCOPY Copies a directory tree (ExFS subset).');\n  await writeln('VERIFY   Displays ExOS verify state.');\n  await writeln('BREAK    Displays ExOS break-check state.');\n  await writeln('MOVE     Moves a file.');\n  await writeln('PATH     Displays or sets the PATH environment variable.');\n  await writeln('POPD     Restores a directory saved by PUSHD.');\n  await writeln('PUSHD    Saves the current directory and changes directory.');\n  await writeln('PAUSE    Waits for ENTER.');\n  await writeln('PROMPT   Changes the command prompt.');\n  await writeln('RD       Removes an empty directory.');\n  await writeln('REN      Renames a file or directory.');\n  await writeln('SET      Displays or sets environment variables.');\n  await writeln('START    Opens an XSH app, document, or directory.');\n  await writeln('TIME     Displays the current time.');\n  await writeln('TITLE    Sets the console window title.');\n  await writeln('TREE     Graphically displays the directory structure.');\n  await writeln('TYPE     Displays the contents of a text file.');\n  await writeln('VER      Displays the ExOS version.');\n  await writeln('VOL      Displays the ExFS volume label.');\n  await writeln('WHERE    Locates files in the current directory and PATH.');\n  await writeln('XCOPY    Copies a directory tree.');\n  await writeln('XBA      Executes ExOS batch scripts using CMD-compatible syntax.');\n  await writeln('');\n  await writeln('ExOS commands: CALC, CMD, EXPLORER, NOTEPAD, CSVEDIT, MSPAINT,');\n  await writeln('DEVMGMT, WINMINE, TASKMGR, REGEDIT, DISKMGMT, EXCONFIG, WHOAMI, MEM,');\n  await writeln('PEB, PE, CONSOLE, TASKLIST, VDO, IRP.');\n  await writeln('Aliases: LS=DIR, CAT=TYPE, CP=COPY, MV=MOVE, RM=DEL, OPEN=START.');\n  await writeln('');\n  await writeln('Line editing: ↑/↓ history, TAB/Shift+TAB completion, F3 repeat last command, F8 prefix history, Ctrl+C cancel line.');\n  await writeln('');\n  await writeln('Standard stream redirection (CMD / XBA):');\n  await writeln('  command > file       STDOUT -> file (overwrite).');\n  await writeln('  command >> file      STDOUT -> file (append).');\n  await writeln('  command < file       file -> STDIN.');\n  await writeln('  command 2> file      STDERR -> file (overwrite).');\n  await writeln('  command 2>> file     STDERR -> file (append).');\n  await writeln('  command 2>&1         STDERR uses current STDOUT handle.');\n  await writeln('  command 1>&2         STDOUT uses current STDERR handle.');\n  await writeln('  left | right         Anonymous pipe: left STDOUT -> right STDIN.');\n  await writeln('Examples:');\n  await writeln('  DIR > listing.txt');\n  await writeln('  SORT < names.txt > sorted.txt');\n  await writeln('  DIR /B | FIND \".txt\"');\n  await writeln('  TYPE missing.txt 2> error.log');\n};\n\n\n/* ---------------------------------------------------------------------\n * os79 Standard Handle / Redirection Engine\n *\n * CMD parses redirection before executing the command. It replaces the\n * process standard HANDLE values with ExFS file handles or anonymous pipe\n * handles, runs the command unchanged, then restores the console handles.\n *\n * Supported:\n *   >  >>  <  1> 1>> 2> 2>>  2>&1  1>&2  |\n * --------------------------------------------------------------------- */\nconst baseStdHandles={\n  input:stdin,\n  output:stdout,\n  error:stderr\n};\n\nconst setStdHandles=async(inputHandle,outputHandle,errorHandle)=>{\n  await kernel32.SetStdHandle(\n    kernel32.STD_INPUT_HANDLE,\n    inputHandle\n  );\n  await kernel32.SetStdHandle(\n    kernel32.STD_OUTPUT_HANDLE,\n    outputHandle\n  );\n  await kernel32.SetStdHandle(\n    kernel32.STD_ERROR_HANDLE,\n    errorHandle\n  );\n};\n\nconst restoreStdHandles=()=>setStdHandles(\n  baseStdHandles.input,\n  baseStdHandles.output,\n  baseStdHandles.error\n);\n\nconst splitPipeline=line=>{\n  const out=[];\n  let quoteChar='';\n  let buf='';\n\n  line=String(line||'');\n\n  for(let i=0;i<line.length;i++){\n    const c=line[i];\n\n    if(c==='^'&&i+1<line.length){\n      buf+=c+line[++i];\n      continue;\n    }\n\n    if(\n      (c==='\"'||c===\"'\")&&\n      (!quoteChar||quoteChar===c)\n    ){\n      quoteChar=quoteChar?'':c;\n      buf+=c;\n      continue;\n    }\n\n    if(\n      !quoteChar&&\n      c==='|'&&\n      line[i+1]!=='|'\n    ){\n      out.push(buf.trim());\n      buf='';\n      continue;\n    }\n\n    buf+=c;\n  }\n\n  out.push(buf.trim());\n\n  if(out.some(x=>!x)){\n    throw new Error(\n      'The syntax of the command is incorrect.'\n    );\n  }\n\n  return out;\n};\n\nconst parseRedirectTarget=(source,start)=>{\n  let i=start;\n  let out='';\n  let quoteChar='';\n\n  while(i<source.length&&/\\s/.test(source[i])){\n    i++;\n  }\n\n  if(i>=source.length){\n    throw new Error(\n      'The syntax of the command is incorrect.'\n    );\n  }\n\n  for(;i<source.length;i++){\n    const c=source[i];\n\n    if(c==='^'&&i+1<source.length){\n      out+=source[++i];\n      continue;\n    }\n\n    if(\n      (c==='\"'||c===\"'\")&&\n      (!quoteChar||quoteChar===c)\n    ){\n      quoteChar=quoteChar?'':c;\n      continue;\n    }\n\n    if(!quoteChar&&/\\s/.test(c)){\n      break;\n    }\n\n    if(!quoteChar&&(c==='>'||c==='<')){\n      break;\n    }\n\n    out+=c;\n  }\n\n  if(quoteChar||!out){\n    throw new Error(\n      'The syntax of the command is incorrect.'\n    );\n  }\n\n  return{\n    target:out,\n    end:i\n  };\n};\n\nconst parseRedirections=stageText=>{\n  const source=String(stageText||'');\n  const redirects=[];\n  let command='';\n  let quoteChar='';\n\n  for(let i=0;i<source.length;){\n    const c=source[i];\n\n    if(c==='^'&&i+1<source.length){\n      command+=source[i+1];\n      i+=2;\n      continue;\n    }\n\n    if(\n      (c==='\"'||c===\"'\")&&\n      (!quoteChar||quoteChar===c)\n    ){\n      quoteChar=quoteChar?'':c;\n      command+=c;\n      i++;\n      continue;\n    }\n\n    if(quoteChar){\n      command+=c;\n      i++;\n      continue;\n    }\n\n    let fd=null;\n    let pos=i;\n\n    if(\n      (c==='0'||c==='1'||c==='2')&&\n      (source[i+1]==='>'||source[i+1]==='<')\n    ){\n      fd=Number(c);\n      pos=i+1;\n    }\n\n    if(source[pos]!=='>'&&source[pos]!=='<'){\n      command+=c;\n      i++;\n      continue;\n    }\n\n    const direction=source[pos];\n    let append=false;\n    let after=pos+1;\n\n    if(direction==='>'&&source[after]==='>'){\n      append=true;\n      after++;\n    }\n\n    if(fd===null){\n      fd=direction==='<'?0:1;\n    }\n\n    if(\n      direction==='>'&&\n      source[after]==='&'&&\n      /[012]/.test(source[after+1]||'')\n    ){\n      redirects.push({\n        kind:'dup',\n        fd,\n        from:Number(source[after+1])\n      });\n      i=after+2;\n      continue;\n    }\n\n    if(direction==='<'&&fd!==0){\n      throw new Error(\n        'Only STDIN (0<) can use input redirection.'\n      );\n    }\n\n    const target=parseRedirectTarget(\n      source,\n      after\n    );\n\n    redirects.push({\n      kind:'file',\n      fd,\n      direction,\n      append,\n      target:target.target\n    });\n\n    i=target.end;\n  }\n\n  return{\n    command:command.trim(),\n    redirects\n  };\n};\n\nconst openRedirectOutput=async(path,append)=>{\n  const h=await kernel32.CreateFile(\n    path,\n    kernel32.GENERIC_WRITE,\n    append\n      ?kernel32.OPEN_ALWAYS\n      :kernel32.CREATE_ALWAYS\n  );\n\n  if(append){\n    const size=await kernel32.GetFileSize(h);\n\n    /*\n     * A zero-byte WriteFile at EOF advances the ExOS file object's current\n     * position without changing its contents — equivalent to the seek CMD\n     * needs before implementing >>.\n     */\n    await kernel32.WriteFile(\n      h,\n      '',\n      size\n    );\n  }\n\n  return h;\n};\n\nconst openRedirectInput=path=>\n  kernel32.CreateFile(\n    path,\n    kernel32.GENERIC_READ,\n    kernel32.OPEN_EXISTING\n  );\n\nconst execute=async rawLine=>{\n  const stages=\n    splitPipeline(rawLine).map(\n      parseRedirections\n    );\n\n  let incomingPipe=0;\n\n  for(let index=0;index<stages.length;index++){\n    const stage=stages[index];\n    const hasNext=index<stages.length-1;\n    const pipe=hasNext\n      ?await kernel32.CreatePipe()\n      :null;\n    const owned=[];\n    const stageIn=\n      incomingPipe||\n      baseStdHandles.input;\n    const stageOut=\n      pipe\n        ?pipe.writeHandle\n        :baseStdHandles.output;\n\n    if(incomingPipe){\n      owned.push(incomingPipe);\n    }\n\n    if(pipe){\n      owned.push(pipe.writeHandle);\n    }\n\n    try{\n      await setStdHandles(\n        stageIn,\n        stageOut,\n        baseStdHandles.error\n      );\n\n      for(const r of stage.redirects){\n        if(r.kind==='dup'){\n          const sourceHandle=\n            r.from===0\n              ?await kernel32.GetStdHandle(kernel32.STD_INPUT_HANDLE)\n              :(r.from===1\n                ?await kernel32.GetStdHandle(kernel32.STD_OUTPUT_HANDLE)\n                :await kernel32.GetStdHandle(kernel32.STD_ERROR_HANDLE));\n\n          await kernel32.SetStdHandle(\n            r.fd===0\n              ?kernel32.STD_INPUT_HANDLE\n              :(r.fd===1\n                ?kernel32.STD_OUTPUT_HANDLE\n                :kernel32.STD_ERROR_HANDLE),\n            sourceHandle\n          );\n          continue;\n        }\n\n        if(r.direction==='<'){\n          const h=await openRedirectInput(\n            r.target\n          );\n          owned.push(h);\n\n          await kernel32.SetStdHandle(\n            kernel32.STD_INPUT_HANDLE,\n            h\n          );\n          continue;\n        }\n\n        const h=await openRedirectOutput(\n          r.target,\n          r.append\n        );\n        owned.push(h);\n\n        await kernel32.SetStdHandle(\n          r.fd===2\n            ?kernel32.STD_ERROR_HANDLE\n            :kernel32.STD_OUTPUT_HANDLE,\n          h\n        );\n      }\n\n      errorLevel=0;\n\n      try{\n        await executeCommand(\n          stage.command\n        );\n      }catch(err){\n        if(err&&err.xshExit)throw err;\n\n        errorLevel=1;\n        await writeError(\n          dosError(err)\n        );\n      }\n    }finally{\n      await restoreStdHandles();\n\n      /*\n       * Writer closure publishes EOF to the next stage. File redirection\n       * handles and the previous stage's read endpoint are also process-local\n       * and are closed here after command completion.\n       */\n      for(const h of owned){\n        try{\n          await kernel32.CloseHandle(h);\n        }catch(ignoreClose){}\n      }\n    }\n\n    incomingPipe=\n      pipe\n        ?pipe.readHandle\n        :0;\n  }\n\n  if(incomingPipe){\n    try{\n      await kernel32.CloseHandle(\n        incomingPipe\n      );\n    }catch(ignoreFinalPipe){}\n  }\n};\n\n/* ---------------------------------------------------------------------\n * ExOS Batch Architecture (.xba)\n * DOS/Windows CMD-compatible text batch runtime.  The extension is new,\n * while the command language deliberately follows common .BAT semantics.\n * --------------------------------------------------------------------- */\nconst batchJoin=(base,name)=>String(base||'').replace(/\\\\+$/,'')+'\\\\'+String(name||'').replace(/^\\\\+/, '');\nconst batchFrames=[];\n\nconst resolveBatchPath=async spec=>{\n  spec=String(spec||'').trim().replace(/^\"|\"$/g,'').replace(/\\//g,'\\\\');\n  if(!spec)return null;\n  if(!/\\.xba$/i.test(spec))spec+='.xba';\n  const candidates=[];\n  if(/^[A-Za-z]:\\\\/.test(spec)||spec.includes('\\\\'))candidates.push(spec);\n  else{\n    candidates.push(batchJoin(await cwd(),spec));\n    const pathValue=String(await kernel32.GetEnvironmentVariable('PATH')||'');\n    for(const d of pathValue.split(';').filter(Boolean))candidates.push(batchJoin(d,spec));\n  }\n  for(const c of candidates){\n    try{const a=await kernel32.GetFileAttributes(c);if(a&&!a.directory)return c;}catch(ignore){}\n  }\n  return null;\n};\n\nconst batchArgModified=(value,mods)=>{\n  let v=String(value||'').replace(/\\//g,'\\\\');\n  const quoted=/^\".*\"$/.test(v);\n  if(quoted)v=v.slice(1,-1);\n  mods=String(mods||'').toLowerCase();\n  const full=/^[A-Za-z]:\\\\/.test(v)?v:batchJoin(batchFrames.length?batchFrames[batchFrames.length-1].cwd:'C:\\\\',v);\n  const pp=parentPath(full),bn=baseName(full),dot=bn.lastIndexOf('.'),name=dot>0?bn.slice(0,dot):bn,ext=dot>0?bn.slice(dot):'';\n  if(!mods)return v;\n  let out='';\n  if(mods.includes('f'))return full;\n  if(mods.includes('d'))out+=full.slice(0,2);\n  if(mods.includes('p'))out+=(pp.slice(2).replace(/\\\\?$/,'\\\\'));\n  if(mods.includes('n'))out+=name;\n  if(mods.includes('x'))out+=ext;\n  return out||v;\n};\n\nconst expandBatchParameters=(line,frame)=>{\n  let out=String(line||'');\n  out=out.replace(/%%([A-Za-z])/g,(m,n)=>Object.prototype.hasOwnProperty.call(frame.forVars,n.toUpperCase())?String(frame.forVars[n.toUpperCase()]):m);\n  out=out.replace(/%~([fdpnx]*)([0-9])/gi,(m,mods,n)=>batchArgModified(frame.args[Number(n)]||'',mods));\n  out=out.replace(/%([0-9])/g,(m,n)=>String(frame.args[Number(n)]||''));\n  out=out.replace(/%\\*/g,()=>frame.args.slice(1).map(quote).join(' '));\n  return out;\n};\n\nconst expandBatchEnvironment=async line=>{\n  const protectedFor=[];\n  let src=String(line||'').replace(/%%([A-Za-z])/g,(m,n)=>{const token='\\uE000'+protectedFor.length+'\\uE001';protectedFor.push('%%'+n);return token;});\n  const names=[];\n  src.replace(/%([^%\\r\\n]+)%/g,(m,n)=>{names.push(String(n));return m;});\n  const values={};\n  for(const rawName of [...new Set(names)]){\n    const key=String(rawName).toUpperCase();\n    if(key==='ERRORLEVEL')values[rawName]=String(errorLevel||0);\n    else if(key==='CD')values[rawName]=String(await cwd());\n    else if(key==='DATE')values[rawName]=new Date().toLocaleDateString('en-US');\n    else if(key==='TIME')values[rawName]=new Date().toLocaleTimeString('en-US',{hour12:false});\n    else if(key==='RANDOM')values[rawName]=String(Math.floor(Math.random()*32768));\n    else{\n      const v=await kernel32.GetEnvironmentVariable(rawName);\n      values[rawName]=v===null||v===undefined?'':String(v);\n    }\n  }\n  src=src.replace(/%([^%\\r\\n]+)%/g,(m,n)=>Object.prototype.hasOwnProperty.call(values,n)?values[n]:'');\n  return src.replace(/\\uE000(\\d+)\\uE001/g,(m,n)=>protectedFor[Number(n)]||'');\n};\n\nconst expandDelayed=async(line,enabled)=>{\n  if(!enabled)return String(line||'');\n  const names=[];String(line||'').replace(/!([^!]+)!/g,(m,n)=>{names.push(String(n).toUpperCase());return m;});\n  const values={};for(const n of [...new Set(names)]){const v=await kernel32.GetEnvironmentVariable(n);values[n]=v===null||v===undefined?'':String(v);}\n  return String(line||'').replace(/!([^!]+)!/g,(m,n)=>values[String(n).toUpperCase()]??'');\n};\n\nconst splitBatchChain=line=>{\n  const out=[];let q='',buf='',op='';const s=String(line||'');\n  for(let i=0;i<s.length;i++){\n    const c=s[i];if((c==='\"'||c===\"'\")&&(!q||q===c)){q=q?'' : c;buf+=c;continue;}\n    if(!q&&c==='&'&&s[i-1]!=='>'){const dbl=s[i+1]==='&';out.push({op,command:buf.trim()});buf='';op=dbl?'&&':'&';if(dbl)i++;continue;}\n    if(!q&&c==='|'&&s[i+1]==='|'){out.push({op,command:buf.trim()});buf='';op='||';i++;continue;}\n    buf+=c;\n  }\n  if(buf.trim()||!out.length)out.push({op,command:buf.trim()});\n  return out.filter(x=>x.command);\n};\n\nconst batchRunSimple=async(frame,line)=>{\n  let raw=String(line||'').trim();if(!raw)return;\n  const quiet=raw.startsWith('@');if(quiet)raw=raw.slice(1).trim();\n  if(!raw||/^rem(?:\\s|$)/i.test(raw)||/^::/.test(raw)||/^:/.test(raw))return;\n  raw=expandBatchParameters(raw,frame);\n  raw=await expandBatchEnvironment(raw);\n  raw=await expandDelayed(raw,frame.delayedExpansion);\n  if(!quiet&&echoEnabled)await writeln((await cwd())+'>'+raw);\n\n  let m;\n  if(/^echo\\s+off\\s*$/i.test(raw)){echoEnabled=false;return;}\n  if(/^echo\\s+on\\s*$/i.test(raw)){echoEnabled=true;return;}\n  if((m=/^goto\\s+(.+)$/i.exec(raw))){\n    const label=String(m[1]).trim().replace(/^:/,'').toLowerCase();\n    if(label==='eof'){frame.returnRequested=true;return;}\n    if(!Object.prototype.hasOwnProperty.call(frame.labels,label))throw new Error('The system cannot find the batch label specified - '+label);\n    frame.pc=frame.labels[label]+1;return;\n  }\n  if((m=/^shift(?:\\s+\\/([0-8]))?\\s*$/i.exec(raw))){const start=m[1]!==undefined?Number(m[1]):1;if(start<frame.args.length)frame.args.splice(start,1);return;}\n  if((m=/^exit\\s+\\/b(?:\\s+(-?\\d+))?\\s*$/i.exec(raw))){if(m[1]!==undefined)errorLevel=Number(m[1])||0;frame.returnRequested=true;return;}\n  if((m=/^call\\s+:([^\\s]+)(?:\\s+(.*))?$/i.exec(raw))){\n    const label=String(m[1]).toLowerCase();if(!Object.prototype.hasOwnProperty.call(frame.labels,label))throw new Error('The system cannot find the batch label specified - '+label);\n    frame.callStack.push({pc:frame.pc,args:frame.args.slice()});frame.args=[frame.path,...argv(m[2]||'')];frame.pc=frame.labels[label]+1;return;\n  }\n  if((m=/^call\\s+(.+)$/i.exec(raw))){const a=argv(m[1]);const target=await resolveBatchPath(a.shift()||'');if(!target)throw new Error('The system cannot find the batch file specified.');await runBatch(target,a);return;}\n  if((m=/^if\\s+(\\/i\\s+)?(not\\s+)?errorlevel\\s+(\\d+)\\s+(.+)$/i.exec(raw))){const ok=errorLevel>=Number(m[3]);if((m[2]?!ok:ok))await batchRunLine(frame,m[4]);return;}\n  if((m=/^if\\s+(\\/i\\s+)?(not\\s+)?exist\\s+(\"[^\"]+\"|\\S+)\\s+(.+)$/i.exec(raw))){let exists=false;try{await kernel32.GetFileAttributes(m[3].replace(/^\"|\"$/g,''));exists=true;}catch(ignore){}if((m[2]?!exists:exists))await batchRunLine(frame,m[4]);return;}\n  if((m=/^if\\s+(\\/i\\s+)?(not\\s+)?defined\\s+([^\\s]+)\\s+(.+)$/i.exec(raw))){const defined=(await kernel32.GetEnvironmentVariable(m[3]))!==null;if((m[2]?!defined:defined))await batchRunLine(frame,m[4]);return;}\n  if((m=/^if\\s+(\\/i\\s+)?(not\\s+)?(\"[^\"]*\"|[^=\\s]+)==(\"[^\"]*\"|\\S+)\\s+(.+)$/i.exec(raw))){let a=m[3].replace(/^\"|\"$/g,''),b=m[4].replace(/^\"|\"$/g,'');if(m[1]){a=a.toLowerCase();b=b.toLowerCase();}const ok=a===b;if((m[2]?!ok:ok))await batchRunLine(frame,m[5]);return;}\n  if((m=/^for\\s+\\/l\\s+%%([A-Za-z])\\s+in\\s*\\((-?\\d+)\\s*,\\s*(-?\\d+)\\s*,\\s*(-?\\d+)\\)\\s+do\\s+(.+)$/i.exec(raw))){const key=m[1].toUpperCase(),start=Number(m[2]),step=Number(m[3])||1,end=Number(m[4]);for(let n=start;(step>0?n<=end:n>=end);n+=step){frame.forVars[key]=String(n);await batchRunLine(frame,m[5]);if(frame.returnRequested)break;}delete frame.forVars[key];return;}\n  if((m=/^for\\s+%%([A-Za-z])\\s+in\\s*\\((.*)\\)\\s+do\\s+(.+)$/i.exec(raw))){const key=m[1].toUpperCase(),items=argv(m[2]);for(const item of items){frame.forVars[key]=item;await batchRunLine(frame,m[3]);if(frame.returnRequested)break;}delete frame.forVars[key];return;}\n  if(/^setlocal(?:\\s|$)/i.test(raw)){frame.delayedExpansion=/enabledelayedexpansion/i.test(raw);await execute('setlocal');return;}\n  if(/^endlocal\\s*$/i.test(raw)){frame.delayedExpansion=false;await execute('endlocal');return;}\n\n  const maybe=argv(raw)[0]||'';\n  if(/\\.xba$/i.test(maybe)){\n    const a=argv(raw),target=await resolveBatchPath(a.shift());if(!target)throw new Error('The system cannot find the batch file specified.');await runBatch(target,a);return;\n  }\n  try{await execute(raw);if(errorLevel===undefined)errorLevel=0;}catch(err){errorLevel=1;throw err;}\n};\n\nconst batchRunLine=async(frame,line)=>{\n  const chain=splitBatchChain(line);let previous=errorLevel;\n  for(const part of chain){if(part.op==='&&'&&previous!==0)continue;if(part.op==='||'&&previous===0)continue;try{await batchRunSimple(frame,part.command);}catch(err){errorLevel=1;throw err;}previous=errorLevel;if(frame.returnRequested)break;}\n};\n\nconst runBatch=async(path,args=[])=>{\n  if(batchFrames.length>=16)throw new Error('Batch recursion limit exceeded.');\n  const resolved=await resolveBatchPath(path);if(!resolved)throw new Error('The system cannot find the batch file specified.');\n  const text=String(await kernel32.ReadTextFile(resolved));if(text.length>1024*1024)throw new Error('.xba exceeds the 1 MiB batch source limit.');\n  const lines=text.replace(/^\\uFEFF/,'').split(/\\r?\\n/);if(lines.length>50000)throw new Error('.xba contains too many lines.');\n  const labels={};for(let i=0;i<lines.length;i++){const mm=/^\\s*:([^:\\s][^\\s]*)\\s*$/.exec(lines[i]);if(mm)labels[String(mm[1]).toLowerCase()]=i;}\n  const frame={path:resolved,cwd:await cwd(),args:[resolved,...args.map(String)],lines,labels,pc:0,callStack:[],forVars:{},delayedExpansion:false,returnRequested:false};\n  batchFrames.push(frame);\n  try{\n    while(frame.pc<lines.length){const line=lines[frame.pc++];frame.returnRequested=false;await batchRunLine(frame,line);if(frame.returnRequested){if(frame.callStack.length){const ret=frame.callStack.pop();frame.pc=ret.pc;frame.args=ret.args;frame.returnRequested=false;}else break;}}\n  }finally{batchFrames.pop();}\n  return errorLevel;\n};\n\nconst executeCommand=async rawLine=>{\n  let line=String(rawLine||'').replace(/[\\r\\n]+$/,'');\n\n  if(!line.trim())return;\n\n  if(line.charAt(0)==='@'){\n    line=line.substring(1);\n  }\n\n  const compact=line.trim();\n\n  if(/^ECHO\\.\\s*$/i.test(compact)){\n    await writeln('');\n    return;\n  }\n\n  if(/^DIR\\//i.test(compact)){\n    await printDir(\n      compact.substring(3)\n    );\n    return;\n  }\n\n  if(/^CD\\.\\s*$/i.test(compact)){\n    return;\n  }\n\n  if(/^C:$/i.test(compact)){\n    return;\n  }\n\n  if(/^CD\\.\\.$/i.test(compact)){\n    await kernel32.SetCurrentDirectory(\n      parentPath(await cwd())\n    );\n    return;\n  }\n\n  if(/^CD\\\\$/i.test(compact)){\n    await kernel32.SetCurrentDirectory('C:\\\\');\n    return;\n  }\n\n  line=await expandEnv(line);\n\n  const parts=argv(line);\n  const commandRaw=String(parts.shift()||'');\n  const command=commandRaw.toLowerCase();\n\n  if(command==='ls'){\n    await printDir(parts.map(quote).join(' '));\n    return;\n  }\n  if(command==='cat'){\n    if(!parts.length)throw new Error('The syntax of the command is incorrect.');\n    for(const p of parts)await write(await kernel32.ReadTextFile(p));\n    return;\n  }\n  if(command==='cp'){\n    if(parts.length<2)throw new Error('The syntax of the command is incorrect.');\n    await kernel32.CopyFile(parts[0],parts[1],false);await writeln('        1 file(s) copied.');return;\n  }\n  if(command==='mv'){\n    if(parts.length<2)throw new Error('The syntax of the command is incorrect.');\n    await kernel32.MoveFile(parts[0],parts[1]);await writeln('        1 file(s) moved.');return;\n  }\n  if(command==='rm'){\n    if(!parts.length)throw new Error('The syntax of the command is incorrect.');\n    for(const p of parts){if(/[*?]/.test(p))await deletePattern(p);else await kernel32.DeleteFile(p);}return;\n  }\n  if(command==='open'){\n    await execute('start '+parts.map(quote).join(' '));\n    return;\n  }\n\n  if(command==='rem'){\n    return;\n  }\n\n  if(command==='cls'||command==='clear'){\n    await kernel32.ClearConsole();\n    return;\n  }\n\n  if(command==='help'||command==='?'||command==='man'){\n    if(\n      parts.length&&\n      await helpCommand(parts[0])\n    ){\n      return;\n    }\n\n    await printHelp();\n    return;\n  }\n\n  if(command==='ver'||command==='winver'){\n    await writeln(\n      'ExOS [Version 6.4.0-dev-os84]'\n    );\n    return;\n  }\n\n  if(command==='chcp'){\n    const inputCp=\n      await kernel32.GetConsoleCP();\n\n    const outputCp=\n      await kernel32.GetConsoleOutputCP();\n\n    await writeln(\n      'Active code page: '+\n      inputCp+\n      (\n        inputCp===outputCp\n          ?''\n          :' (output '+outputCp+')'\n      )\n    );\n    return;\n  }\n\n  if(command==='assoc'){\n    if(!parts.length){\n      await writeln('Usage: ASSOC .extension');\n      return;\n    }\n\n    let ext=String(parts[0]||'').trim().toLowerCase();\n    if(!ext.startsWith('.'))ext='.'+ext;\n\n    const assoc=await shell32.SHGetFileAssociation('C:\\\\dummy'+ext);\n\n    if(!assoc||!assoc.registered){\n      await writeln('File association not found for '+ext);\n      errorLevel=1;\n      return;\n    }\n\n    const rec=assoc.association||{};\n    await writeln(\n      ext+'='+(rec.progId||rec.fileType||rec.app||rec.application||'ExOS registered type')\n    );\n    errorLevel=0;\n    return;\n  }\n\n  if(command==='chkdsk'){\n    const target=parts.find(x=>!String(x).startsWith('/'))||await cwd();\n    const start=Date.now();\n\n    await writeln('The type of the file system is ExFS.');\n    await writeln('ExOS is scanning '+target+' (read-only)...');\n\n    const result=await scanDirectory(target);\n\n    await writeln('');\n    await writeln(numberText(result.files)+' files');\n    await writeln(numberText(result.dirs)+' directories');\n    await writeln(numberText(result.bytes)+' bytes in files');\n    await writeln(numberText(result.errors)+' traversal errors');\n    await writeln('Elapsed: '+(Date.now()-start)+' ms');\n    await writeln('ExFS VDO check completed. No repair was performed.');\n\n    errorLevel=result.errors?1:0;\n    return;\n  }\n\n  if(command==='choice'){\n    const opt=parseChoiceArgs(parts);\n    const prompt=(opt.message?opt.message+' ':'')+'['+opt.choices.split('').join(',')+']? ';\n    await write(prompt);\n\n    let answer='';\n\n    if(opt.timeout>=0&&opt.defaultChoice){\n      answer=await Promise.race([\n        readStd(32).then(x=>String(x||'').trim().toUpperCase().charAt(0)),\n        kernel32.Sleep(opt.timeout*1000).then(()=>opt.defaultChoice)\n      ]);\n    }else{\n      answer=String(await readStd(32)).trim().toUpperCase().charAt(0);\n    }\n\n    if(!opt.choices.includes(answer)){\n      await writeln('');\n      errorLevel=255;\n      return;\n    }\n\n    await writeln(answer);\n    errorLevel=opt.choices.indexOf(answer)+1;\n    return;\n  }\n\n  if(command==='timeout'){\n    let seconds=0;\n    for(let i=0;i<parts.length;i++){\n      if(String(parts[i]).toUpperCase()==='/T'&&i+1<parts.length){\n        seconds=parseInt(parts[++i],10)||0;\n      }else if(/^\\d+$/.test(String(parts[i]))){\n        seconds=parseInt(parts[i],10)||0;\n      }\n    }\n\n    seconds=Math.max(0,Math.min(86400,seconds));\n    await writeln('Waiting for '+seconds+' second(s)...');\n    await kernel32.Sleep(seconds*1000);\n    errorLevel=0;\n    return;\n  }\n\n  if(command==='hostname'){\n    const name=\n      await kernel32.GetEnvironmentVariable(\n        'COMPUTERNAME'\n      );\n\n    await writeln(\n      name||'EXOS'\n    );\n    return;\n  }\n\n  if(command==='vol'){\n    const vdo=await exes.QuerySystemVdo();\n\n    await writeln(\n      ' Volume in drive C is '+\n      String(vdo&&vdo.label?vdo.label:'ExFS')\n    );\n    await writeln(\n      ' Volume Serial Number is EXOS-EXFS'\n    );\n    return;\n  }\n\n  if(command==='title'){\n    const title=line.substring(\n      line.toLowerCase().indexOf('title')+5\n    ).trim();\n\n    await kernel32.SetConsoleTitle(\n      title||'命令提示字元 - ExOS cmd.xsh'\n    );\n    return;\n  }\n\n  if(command==='color'){\n    const value=String(parts[0]||'07').toUpperCase();\n\n    if(!/^[0-9A-F]{1,2}$/.test(value)){\n      throw new Error('Invalid COLOR attribute.');\n    }\n\n    const attr=parseInt(value,16);\n\n    if((attr&15)===((attr>>4)&15)){\n      throw new Error('Foreground and background colors cannot be the same.');\n    }\n\n    await kernel32.SetConsoleTextAttribute(\n      stdout,\n      attr\n    );\n    return;\n  }\n\n  if(command==='prompt'){\n    promptSpec=line.substring(\n      line.toLowerCase().indexOf('prompt')+6\n    ).trim()||'$P$G';\n    return;\n  }\n\n  if(command==='date'){\n    await writeln(\n      'The current date is: '+\n      new Date().toLocaleDateString()\n    );\n    return;\n  }\n\n  if(command==='time'){\n    await writeln(\n      'The current time is: '+\n      new Date().toLocaleTimeString()\n    );\n    return;\n  }\n\n  if(command==='pause'){\n    await write(\n      'Press ENTER to continue . . .'\n    );\n\n    await readStd(2);\n\n    return;\n  }\n\n  if(command==='doskey'||command==='history'){\n    const option=String(parts[0]||'/HISTORY').toUpperCase();\n\n    if(option==='/HISTORY'){\n      const history=\n        await kernel32.GetConsoleCommandHistory();\n\n      for(const item of history){\n        await writeln(item);\n      }\n\n      return;\n    }\n\n    if(option==='/REINSTALL'){\n      await kernel32.ExpungeConsoleCommandHistory();\n      return;\n    }\n\n    if(option.startsWith('/LISTSIZE=')){\n      const count=parseInt(\n        option.substring('/LISTSIZE='.length),\n        10\n      );\n\n      await kernel32.SetConsoleNumberOfCommands(\n        count\n      );\n      return;\n    }\n\n    throw new Error('Invalid DOSKEY option.');\n  }\n\n  if(command==='pe'){\n    await writeln(\n      JSON.stringify(\n        process.peHeader,\n        null,\n        2\n      )\n    );\n    return;\n  }\n\n  if(command==='console'){\n    await writeln(\n      JSON.stringify(\n        await kernel32.GetConsoleScreenBufferInfo(stdout),\n        null,\n        2\n      )\n    );\n    return;\n  }\n\n  if(command==='whoami'){\n    const u=\n      await kernel32.GetEnvironmentVariable('USERNAME');\n\n    await writeln(u||'administrator');\n    return;\n  }\n\n  if(command==='peb'){\n    const env=await kernel32.GetEnvironmentStrings();\n\n    await writeln('PID '+process.pid);\n    await writeln('PPID '+process.ppid);\n    await writeln('Image '+process.imagePath);\n    await writeln('Subsystem '+process.subsystemName);\n    await writeln('Console '+(process.consoleAttached?'ATTACHED':'NONE'));\n    await writeln('CWD '+await cwd());\n    await writeln('Environment '+Object.keys(env).length);\n    return;\n  }\n\n  if(command==='tasklist'){\n    const q=\n      await ntdll.NtQuerySystemInformation(\n        'SystemProcessInformation'\n      );\n\n    const rows=Array.isArray(q)\n      ?q\n      :(q&&Array.isArray(q.information)?q.information:[]);\n\n    await writeln('Image Name                     PID  Subsystem');\n    await writeln('========================= ========  ===========================');\n\n    for(const p of rows){\n      await writeln(\n        String(p.imageName||p.name||'').padEnd(25,' ').substring(0,25)+\n        ' '+\n        String(p.pid??'').padStart(8,' ')+\n        '  '+\n        String(p.imageSubsystemName||'')\n      );\n    }\n\n    return;\n  }\n\n  if(command==='pwd'){\n    await writeln(await cwd());\n    return;\n  }\n\n  if(command==='cd'||command==='chdir'){\n    if(!parts.length){\n      await writeln(await cwd());\n      return;\n    }\n\n    if(\n      String(parts[0]).toLowerCase()==='/d'\n    ){\n      parts.shift();\n    }\n\n    if(!parts[0]){\n      await writeln(await cwd());\n      return;\n    }\n\n    await kernel32.SetCurrentDirectory(parts[0]);\n    return;\n  }\n\n  if(command==='pushd'){\n    if(!parts.length){\n      throw new Error(\n        'The syntax of the command is incorrect.'\n      );\n    }\n\n    directoryStack.push(\n      await cwd()\n    );\n\n    try{\n      await kernel32.SetCurrentDirectory(\n        parts[0]\n      );\n    }catch(err){\n      directoryStack.pop();\n      throw err;\n    }\n\n    return;\n  }\n\n  if(command==='popd'){\n    if(!directoryStack.length){\n      throw new Error(\n        'There are no entries in the directory stack.'\n      );\n    }\n\n    await kernel32.SetCurrentDirectory(\n      directoryStack.pop()\n    );\n\n    return;\n  }\n\n  if(command==='dir'||command==='ls'){\n    const rest=line.substring(\n      line.toLowerCase().indexOf(command)+command.length\n    ).trim();\n\n    await printDir(rest);\n    return;\n  }\n\n  if(command==='tree'){\n    const p=parts[0]||await cwd();\n\n    await writeln(\n      'Folder PATH listing for volume ExFS'\n    );\n    await writeln(p);\n    await printTree(p,0);\n    return;\n  }\n\n  if(command==='type'||command==='cat'){\n    if(!parts.length){\n      throw new Error('The syntax of the command is incorrect.');\n    }\n\n    for(const p of parts){\n      await write(\n        await kernel32.ReadTextFile(p)\n      );\n      await writeln('');\n    }\n\n    return;\n  }\n\n  if(command==='more'){\n    if(!parts.length){\n      await write(\n        await readAllStdIn()\n      );\n      return;\n    }\n\n    for(const p of parts){\n      await write(\n        await kernel32.ReadTextFile(p)\n      );\n\n      await writeln('');\n    }\n\n    return;\n  }\n\n  if(command==='find'||command==='findstr'){\n    let ignoreCase=false;\n    const args=parts.slice();\n\n    if(\n      args.length&&\n      String(args[0]).toUpperCase()==='/I'\n    ){\n      ignoreCase=true;\n      args.shift();\n    }\n\n    if(args.length<1){\n      throw new Error(\n        'The syntax of the command is incorrect.'\n      );\n    }\n\n    const needle=args.shift();\n    let count=0;\n\n    if(args.length){\n      count=\n        await findText(\n          needle,\n          args,\n          command==='findstr',\n          ignoreCase\n        );\n    }else{\n      const text=String(\n        await readAllStdIn()\n      );\n\n      for(const line of text.split(/\\r?\\n/)){\n        let hit=false;\n\n        if(command==='findstr'){\n          hit=new RegExp(\n            needle,\n            ignoreCase?'i':''\n          ).test(line);\n        }else{\n          hit=ignoreCase\n            ?line.toLowerCase().includes(\n                needle.toLowerCase()\n              )\n            :line.includes(needle);\n        }\n\n        if(hit){\n          count++;\n          await writeln(line);\n        }\n      }\n    }\n\n    errorLevel=count?0:1;\n    return;\n  }\n\n  if(command==='fc'){\n    if(parts.length<2){\n      throw new Error(\n        'The syntax of the command is incorrect.'\n      );\n    }\n\n    const a=String(\n      await kernel32.ReadTextFile(parts[0])\n    ).split(/\\r?\\n/);\n\n    const b=String(\n      await kernel32.ReadTextFile(parts[1])\n    ).split(/\\r?\\n/);\n\n    const max=Math.max(\n      a.length,\n      b.length\n    );\n\n    let diff=0;\n\n    for(let i=0;i<max;i++){\n      if(\n        String(a[i]??'')!==\n        String(b[i]??'')\n      ){\n        diff++;\n\n        await writeln(\n          '***** '+\n          parts[0]+\n          ':'+\n          (i+1)\n        );\n        await writeln(\n          String(a[i]??'')\n        );\n        await writeln(\n          '***** '+\n          parts[1]+\n          ':'+\n          (i+1)\n        );\n        await writeln(\n          String(b[i]??'')\n        );\n      }\n    }\n\n    if(!diff){\n      await writeln(\n        'FC: no differences encountered'\n      );\n    }\n\n    errorLevel=diff?1:0;\n    return;\n  }\n\n  if(command==='echo'){\n    const rest=line.substring(\n      line.toLowerCase().indexOf('echo')+4\n    );\n\n    if(!rest.trim()){\n      await writeln('ECHO is on.');\n      return;\n    }\n\n    if(/^\\.?\\s*$/i.test(rest)){\n      await writeln('');\n      return;\n    }\n\n    if(/^\\s+off\\s*$/i.test(rest)){\n      return;\n    }\n\n    if(/^\\s+on\\s*$/i.test(rest)){\n      return;\n    }\n\n    if(rest.charAt(0)==='.'){\n      await writeln(rest.substring(1));\n      return;\n    }\n\n    await writeln(rest.replace(/^\\s+/,''));\n    return;\n  }\n\n  if(command==='setlocal'){\n    localEnvironmentStack.push(\n      await kernel32.GetEnvironmentStrings()\n    );\n    errorLevel=0;\n    return;\n  }\n\n  if(command==='endlocal'){\n    if(!localEnvironmentStack.length){\n      await writeError('ENDLOCAL was called without a matching SETLOCAL.');\n      errorLevel=1;\n      return;\n    }\n\n    await restoreEnvironment(\n      localEnvironmentStack.pop()\n    );\n    errorLevel=0;\n    return;\n  }\n\n  if(command==='set'){\n    const rest=line.substring(\n      line.toLowerCase().indexOf('set')+3\n    ).trim();\n\n    if(/^\\/P\\s+/i.test(rest)){\n      const spec=\n        rest.replace(\n          /^\\/P\\s+/i,\n          ''\n        );\n\n      const pos=spec.indexOf('=');\n\n      if(pos<1){\n        throw new Error(\n          'The syntax of the command is incorrect.'\n        );\n      }\n\n      const name=\n        spec.substring(0,pos).trim();\n\n      const prompt=\n        spec.substring(pos+1);\n\n      if(prompt)await write(prompt);\n\n      const value=\n        String(\n          await readStd(\n            8192\n          )\n        ).replace(\n          /[\\r\\n]+$/,\n          ''\n        );\n\n      await kernel32.SetEnvironmentVariable(\n        name,\n        value\n      );\n\n      return;\n    }\n\n    if(/^\\/A\\s+/i.test(rest)){\n      const spec=\n        rest.replace(\n          /^\\/A\\s+/i,\n          ''\n        );\n\n      const pos=spec.indexOf('=');\n\n      let name='';\n      let expression=spec;\n\n      if(pos>0){\n        name=\n          spec.substring(0,pos).trim();\n        expression=\n          spec.substring(pos+1);\n      }\n\n      const value=\n        await evaluateSetA(\n          expression\n        );\n\n      if(name){\n        await kernel32.SetEnvironmentVariable(\n          name,\n          String(value)\n        );\n      }\n\n      await writeln(\n        String(value)\n      );\n\n      return;\n    }\n\n    if(!rest){\n      const env=await kernel32.GetEnvironmentStrings();\n\n      for(const k of Object.keys(env).sort()){\n        await writeln(k+'='+env[k]);\n      }\n\n      return;\n    }\n\n    const pos=rest.indexOf('=');\n\n    if(pos<1){\n      const v=\n        await kernel32.GetEnvironmentVariable(rest);\n\n      if(v!==null){\n        await writeln(\n          rest.toUpperCase()+'='+v\n        );\n      }\n\n      return;\n    }\n\n    await kernel32.SetEnvironmentVariable(\n      rest.substring(0,pos).trim(),\n      rest.substring(pos+1)\n    );\n    return;\n  }\n\n  if(command==='path'){\n    if(!parts.length){\n      const value=\n        await kernel32.GetEnvironmentVariable('PATH');\n\n      await writeln(\n        'PATH='+\n        String(value||'')\n      );\n      return;\n    }\n\n    const value=line.substring(\n      line.toLowerCase().indexOf('path')+4\n    ).trim();\n\n    await kernel32.SetEnvironmentVariable(\n      'PATH',\n      value\n    );\n    return;\n  }\n\n  if(command==='md'||command==='mkdir'){\n    if(!parts.length){\n      throw new Error('The syntax of the command is incorrect.');\n    }\n\n    for(const p of parts){\n      await kernel32.CreateDirectory(p);\n    }\n\n    return;\n  }\n\n  if(command==='rd'||command==='rmdir'){\n    if(!parts.length){\n      throw new Error(\n        'The syntax of the command is incorrect.'\n      );\n    }\n\n    const recursive=\n      parts.some(\n        x=>String(x).toUpperCase()==='/S'\n      );\n\n    const targets=\n      parts.filter(\n        x=>!/^\\/[SQ]$/i.test(String(x))\n      );\n\n    if(!targets.length){\n      throw new Error(\n        'The syntax of the command is incorrect.'\n      );\n    }\n\n    for(const p of targets){\n      if(recursive){\n        await removeTree(p,0);\n      }else{\n        await kernel32.RemoveDirectory(p);\n      }\n    }\n\n    return;\n  }\n\n  if(command==='del'||command==='erase'){\n    if(!parts.length){\n      throw new Error('The syntax of the command is incorrect.');\n    }\n\n    for(const p of parts){\n      if(/[*?]/.test(p)){\n        await deletePattern(p);\n      }else{\n        await kernel32.DeleteFile(p);\n      }\n    }\n\n    return;\n  }\n\n  if(command==='ren'||command==='rename'){\n    if(parts.length<2){\n      throw new Error('The syntax of the command is incorrect.');\n    }\n\n    await kernel32.MoveFile(\n      parts[0],\n      parts[1]\n    );\n    return;\n  }\n\n  if(command==='copy'){\n    if(parts.length<2){\n      throw new Error('The syntax of the command is incorrect.');\n    }\n\n    await kernel32.CopyFile(\n      parts[0],\n      parts[1],\n      false\n    );\n\n    await writeln('        1 file(s) copied.');\n    return;\n  }\n\n  if(command==='xcopy'){\n    const args=\n      parts.filter(\n        x=>!/^\\/[EISYQH]$/i.test(String(x))\n      );\n\n    if(args.length<2){\n      throw new Error(\n        'The syntax of the command is incorrect.'\n      );\n    }\n\n    const copied=\n      await xcopyTree(\n        args[0],\n        args[1],\n        0\n      );\n\n    await writeln(\n      '        '+\n      copied+\n      ' File(s) copied'\n    );\n\n    return;\n  }\n\n  if(command==='sort'){\n    const sources=[];\n\n    if(parts.length){\n      for(const p of parts){\n        sources.push(\n          String(\n            await kernel32.ReadTextFile(p)\n          )\n        );\n      }\n    }else{\n      sources.push(\n        String(\n          await readAllStdIn()\n        )\n      );\n    }\n\n    for(const sourceText of sources){\n      const lines=\n        sourceText.split(/\\r?\\n/);\n\n      lines.sort(\n        (a,b)=>a.localeCompare(\n          b,\n          'en',\n          {\n            numeric:true,\n            sensitivity:'base'\n          }\n        )\n      );\n\n      await writeln(\n        lines.join('\\r\\n')\n      );\n    }\n\n    return;\n  }\n\n  if(command==='comp'){\n    if(parts.length<2){\n      throw new Error(\n        'The syntax of the command is incorrect.'\n      );\n    }\n\n    const a=String(\n      await kernel32.ReadTextFile(parts[0])\n    );\n\n    const b=String(\n      await kernel32.ReadTextFile(parts[1])\n    );\n\n    if(a===b){\n      await writeln(\n        'Files compare OK'\n      );\n      errorLevel=0;\n    }else{\n      await writeln(\n        'Files differ'\n      );\n      errorLevel=1;\n    }\n\n    return;\n  }\n\n  if(command==='robocopy'){\n    const args=\n      parts.filter(\n        x=>!/^\\/[EISYQH]$/i.test(String(x))\n      );\n\n    if(args.length<2){\n      throw new Error(\n        'The syntax of the command is incorrect.'\n      );\n    }\n\n    const copied=\n      await xcopyTree(\n        args[0],\n        args[1],\n        0\n      );\n\n    await writeln(\n      '  Files : '+\n      copied+\n      ' copied'\n    );\n    await writeln(\n      '  ExOS ROBOCOPY subset completed.'\n    );\n    return;\n  }\n\n  if(command==='verify'){\n    await writeln(\n      'VERIFY is off.'\n    );\n    return;\n  }\n\n  if(command==='break'){\n    await writeln(\n      'BREAK is off.'\n    );\n    return;\n  }\n\n  if(command==='move'){\n    if(parts.length<2){\n      throw new Error('The syntax of the command is incorrect.');\n    }\n\n    await kernel32.MoveFile(\n      parts[0],\n      parts[1]\n    );\n\n    await writeln('        1 file(s) moved.');\n    return;\n  }\n\n  if(command==='where'){\n    await whereCommand(\n      parts[0]||''\n    );\n    return;\n  }\n\n  if(command==='calc'){\n    await startBuiltin('calc.xsh');\n    return;\n  }\n\n  if(command==='cmd'||command==='command'){\n    await startBuiltin(\n      'cmd.xsh',\n      parts\n    );\n    return;\n  }\n\n  if(command==='notepad'){\n    await startBuiltin(\n      'notepad.xsh',\n      parts.length\n        ?[parts[0]]\n        :[]\n    );\n    return;\n  }\n\n  if(command==='csvedit'){\n    await startBuiltin(\n      'csvedit.xsh',\n      parts.length?[parts[0]]:[]\n    );\n    return;\n  }\n\n  if(command==='devmgmt'||command==='devmgmt.msc'||command==='devices'){\n    await ExOS.LaunchSystemApp('devmgmt');\n    return;\n  }\n\n  if(command==='mspaint'||command==='paint'){\n    await ExOS.LaunchSystemApp('paint',parts.length?[parts[0]]:[]);\n    return;\n  }\n\n  if(command==='winmine'||command==='minesweeper'||command==='mine'){\n    await ExOS.LaunchSystemApp('winmine',[]);\n    return;\n  }\n\n  if(command==='mem'||command==='memory'){\n    const m=await kernel32.QueryVmmStatistics();\n    await writeln('ExOS Virtual Memory Manager');\n    await writeln('  VAS size       '+sizeText(m.virtualAddressSpaceBytes||m.vasBytes||0));\n    await writeln('  Commit         '+sizeText(m.committedBytes||0));\n    await writeln('  Resident       '+sizeText(m.residentBytes||0));\n    await writeln('  Pagefile       '+sizeText(m.pagefileUsedBytes||0));\n    await writeln('  Page faults    '+String(m.pageFaults||0));\n    await writeln('  Hard faults    '+String(m.hardFaults||0));\n    return;\n  }\n\n  if(command==='taskmgr'){\n    await ExOS.LaunchSystemApp('taskmgr');\n    return;\n  }\n\n  if(command==='regedit'){\n    await ExOS.LaunchSystemApp('regedit');\n    return;\n  }\n\n  if(command==='diskmgmt'){\n    await ExOS.LaunchSystemApp('diskmgmt');\n    return;\n  }\n\n  if(command==='exconfig'||command==='msconfig'||command==='control'){\n    await ExOS.LaunchSystemApp('exconfig');\n    return;\n  }\n\n  if(command==='explorer'){\n    await ExOS.LaunchSystemApp(\n      'explorer',\n      parts.length?[parts[0]]:[]\n    );\n    return;\n  }\n\n  if(command==='vdo'){\n    await writeln(\n      JSON.stringify(\n        await exes.QuerySystemVdo(),\n        null,\n        2\n      )\n    );\n    return;\n  }\n\n  if(command==='irp'){\n    await writeln(\n      JSON.stringify(\n        await io.GetIrpTrace(30),\n        null,\n        2\n      )\n    );\n    return;\n  }\n\n  if(command==='start'){\n    if(!parts[0]){\n      await ExOS.LaunchSystemApp('explorer');\n      return;\n    }\n\n    const p=parts[0];\n    const attr=await kernel32.GetFileAttributes(p);\n\n    if(attr.directory){\n      await ExOS.LaunchSystemApp(\n        'explorer',\n        [p]\n      );\n      return;\n    }\n\n    const ext=(\n      p.split('.').pop()||''\n    ).toLowerCase();\n\n    if(ext==='xsh'){\n      await kernel32.CreateProcess(\n        p,\n        parts.slice(1).map(quote).join(' ')\n      );\n      return;\n    }\n\n    if(ext==='xba'){\n      await runBatch(p,parts.slice(1));\n      return;\n    }\n\n    if(ext==='csv'){\n      await startBuiltin('csvedit.xsh',[p]);\n      return;\n    }\n\n    if(ext==='html'||ext==='htm'){\n      await startBuiltin('html_editor.xsh',[p]);\n      return;\n    }\n\n    if(\n      ['jpg','jpeg','png','gif','webp','bmp'].includes(ext)\n    ){\n      await startBuiltin('image_viewer.xsh',[p]);\n      return;\n    }\n\n    await startBuiltin('notepad.xsh',[p]);\n    return;\n  }\n\n  if(command==='exit'||command==='quit'){\n    await process.ExitProcess(0);\n    return;\n  }\n\n  if(command.endsWith('.xba')){\n    const target=await resolveBatchPath(commandRaw);\n    if(!target)throw new Error('The system cannot find the batch file specified.');\n    await runBatch(target,parts);\n    return;\n  }\n\n  /* CMD also searches for a same-name .xba when no extension is supplied. */\n  if(!command.includes('.')){\n    const target=await resolveBatchPath(commandRaw);\n    if(target){await runBatch(target,parts);return;}\n  }\n\n  throw new Error(\n    \"'\"+\n    command+\n    \"' is not recognized as an internal or external command,\\r\\n\"+\n    'operable program or batch file.'\n  );\n};\n\nawait writeln(\n  'ExOS [Version 6.4.0-dev-os84]'\n);\nawait writeln(\n  '(c) ExOS Project. All rights reserved.'\n);\nawait writeln('');\n\nconst launchArgs=process.argv.slice(1);\nlet interactive=true;\n\nif(\n  launchArgs.length&&\n  String(launchArgs[0]).toLowerCase()==='/c'\n){\n  interactive=false;\n\n  const commandLine=\n    launchArgs.slice(1).join(' ');\n\n  if(commandLine){\n    try{\n      await execute(commandLine);\n    }catch(err){\n      errorLevel=1;\n      await writeError(dosError(err));\n    }\n  }\n\n  await process.ExitProcess(errorLevel);\n}\n\nif(\n  launchArgs.length&&\n  String(launchArgs[0]).toLowerCase()==='/k'\n){\n  const commandLine=\n    launchArgs.slice(1).join(' ');\n\n  if(commandLine){\n    try{\n      await execute(commandLine);\n    }catch(err){\n      errorLevel=1;\n      await writeError(dosError(err));\n    }\n  }\n}\n\nwhile(interactive){\n  await write(await promptText());\n\n  const raw=\n    await readStd(\n      8192\n    );\n\n  const line=String(raw||'')\n    .replace(/[\\r\\n]+$/,'');\n\n  if(!line.trim())continue;\n\n  try{\n    await execute(line);\n  }catch(err){\n    if(err&&err.xshExit)throw err;\n\n    errorLevel=1;\n    await writeError(\n      dosError(err)\n    );\n  }\n}\n"},"notepad":{"id":"notepad","fileName":"notepad.xsh","title":"記事本","description":"ExOS Notepad (.xsh / find-replace / line endings / cleanup tools)","icon":"txt","singleInstance":false,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst comdlg32=ExOS.LoadLibrary('comdlg32.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.TOOLBARCLASSNAME,comctl32.STATUSCLASSNAME]});\nlet path=process.argv[1]?String(process.argv[1]):'';\nconst leaf=p=>{p=String(p||'').replace(/\\//g,'\\\\');const i=p.lastIndexOf('\\\\');return i>=0?p.substring(i+1):p;};\nconst joinPath=(base,name)=>{base=String(base||'C:\\\\').replace(/\\//g,'\\\\').replace(/\\\\+$/,'');if(/^C:$/i.test(base))base+='\\\\';return base==='C:\\\\'?base+name:base+'\\\\'+name;};\nconst parentPath=p=>{p=String(p||'').replace(/\\//g,'\\\\');const i=p.lastIndexOf('\\\\');return i>2?p.substring(0,i):'C:\\\\';};\nlet fileName=path?(leaf(path)||'未命名.txt'):'未命名.txt';\nlet src=path?await kernel32.ReadTextFile(path):'';\nlet dirty=false,wrap=true,fontSize=14,lastFind='',lineEnding=/\\r\\n/.test(src)?'CRLF':'LF';\nlet history=[src],historyIndex=0,historyMute=false;\nconst hwnd=await user32.CreateWindow({title:'記事本 - '+fileName,icon:'txt',width:980,height:720});\nawait uxtheme.SetWindowTheme(hwnd,'TextEditor','Document');\nawait dwmapi.DwmSetWindowAttribute(hwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',overflow:'hidden'}});\nawait comctl32.CreateToolbar(hwnd,{id:'toolbar',parentId:'root',buttons:[\n  {id:'new',text:'新增',title:'新增文件 (Ctrl+N)'},{id:'open',text:'開啟',title:'開啟 (Ctrl+O)'},{id:'save',text:'儲存',title:'儲存 (Ctrl+S)'},{id:'saveas',text:'另存新檔',title:'另存新檔 (Ctrl+Shift+S)'},{separator:true},\n  {id:'undo',text:'復原',title:'復原 (Ctrl+Z)'},{id:'redo',text:'取消復原',title:'取消復原 (Ctrl+Y)'},{separator:true},{id:'cut',text:'剪下',title:'Ctrl+X'},{id:'copy',text:'複製',title:'Ctrl+C'},{id:'paste',text:'貼上',title:'Ctrl+V'},{separator:true},\n  {id:'find',text:'尋找',title:'尋找 (Ctrl+F)'},{id:'replace',text:'取代',title:'取代 (Ctrl+H)'},{id:'goto',text:'跳到行',title:'跳到行 (Ctrl+G)'},{separator:true},\n  {id:'timestamp',text:'日期時間',title:'在游標位置插入日期與時間'},{id:'duplicate',text:'複製行',title:'複製目前行 (Ctrl+D)'},{id:'upper',text:'大寫',title:'選取文字轉大寫'},{id:'lower',text:'小寫',title:'選取文字轉小寫'},{id:'sortlines',text:'排序行',title:'排序選取行；沒有選取時排序全文'},{separator:true},\n  {id:'trim',text:'清除行尾空白',title:'移除每行尾端空白'},{id:'stats',text:'統計',title:'文件統計'},{id:'lineending',text:'CRLF',title:'切換 CRLF / LF 行尾'},{separator:true},\n  {id:'selectall',text:'全選',title:'全選 (Ctrl+A)'},{id:'wrap',text:'自動換行',checkable:true,checked:true,title:'切換自動換行'},{id:'fontminus',text:'A−',title:'縮小字體'},{id:'fontreset',text:'100%',title:'字體回到 14px'},{id:'fontplus',text:'A＋',title:'放大字體'}\n],style:{width:'100%',margin:'0 0 6px'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'searchPanel',parentId:'root',style:{display:'none',alignItems:'center',gap:'6px',width:'100%',margin:'0 0 6px',padding:'6px',boxSizing:'border-box',border:'1px solid #dbe3ec',background:'#f8fafc'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'findText',parentId:'searchPanel',placeholder:'尋找內容',spellcheck:false,style:{flex:'1',width:'auto',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'replaceText',parentId:'searchPanel',placeholder:'取代為',spellcheck:false,style:{flex:'1',width:'auto',margin:'0',display:'none'}});\nfor(const [id,text] of [['findPrev','上一個'],['findNext','下一個'],['replaceOne','取代'],['replaceAll','全部取代'],['closeSearch','×']]){\n  await user32.CreateControl(hwnd,{type:'button',id,parentId:'searchPanel',text,style:{margin:'0',minWidth:id==='closeSearch'?'34px':'70px',padding:id==='closeSearch'?'0':'0 8px',display:(id==='replaceOne'||id==='replaceAll')?'none':'block'}});\n}\nawait user32.CreateControl(hwnd,{type:'textarea',id:'text',parentId:'root',text:src,spellcheck:true,style:{flex:'1',width:'100%',height:'100%',minHeight:'0',resize:'none',margin:'0',padding:'10px 12px',boxSizing:'border-box',fontFamily:'Consolas,\"Microsoft JhengHei\",monospace',fontSize:'14px',lineHeight:'1.5',whiteSpace:'pre-wrap',overflow:'auto'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'statusbar',parentId:'root',parts:[{text:path?'已載入':'新文件',width:0},{text:'行 1，欄 1',width:130},{text:`${src.length} 字元`,width:110},{text:'UTF-8 ｜ 自動換行',width:150}],style:{width:'100%',margin:'6px 0 0'}});\nconst updateTitle=()=>user32.SetWindowText(hwnd,(dirty?'*':'')+'記事本 - '+fileName);\nconst markDirty=async()=>{if(!dirty){dirty=true;await updateTitle();}await comctl32.StatusBar_SetText('statusbar',0,path?'尚未儲存':'未命名文件');};\nconst updateStatus=async(value,pos)=>{\n  value=String(value??await user32.GetControlText('text'));\n  if(typeof pos!=='number'){const sel=await user32.GetControlProperty('text','selection');pos=Number(sel&&sel.start)||0;}\n  const before=value.slice(0,pos),lines=before.split('\\n');\n  await comctl32.StatusBar_SetText('statusbar',1,`行 ${lines.length}，欄 ${lines[lines.length-1].length+1}`);\n  const words=(value.trim().match(/\\S+/g)||[]).length;\n  await comctl32.StatusBar_SetText('statusbar',2,`${value.length} 字元 ｜ ${words} 字詞`);\n  await comctl32.StatusBar_SetText('statusbar',3,`UTF-8 ｜ ${lineEnding} ｜ ${wrap?'換行':'不換行'}`);\n};\nconst newDocument=async()=>{if(dirty&&!await user32.ConfirmBox('目前文件尚未儲存，仍要建立新文件嗎？','記事本'))return false;path='';fileName='未命名.txt';src='';dirty=false;lineEnding='CRLF';history=[''];historyIndex=0;historyMute=true;await user32.SetControlText('text','');historyMute=false;await updateTitle();await updateStatus('',0);await comctl32.StatusBar_SetText('statusbar',0,'新文件');await user32.FocusControl('text');return true;};\nconst clipboardCommand=async cmd=>{try{await user32.FocusControl('text');const text=String(await user32.GetControlText('text')),sel=await user32.GetControlProperty('text','selection')||{start:0,end:0},start=Math.max(0,Number(sel.start)||0),end=Math.max(start,Number(sel.end)||0);if(cmd==='copy'||cmd==='cut'){const value=text.slice(start,end);if(!value)return;await user32.OpenClipboard(hwnd);await user32.EmptyClipboard();await user32.SetClipboardData(user32.CF_UNICODETEXT,value);await user32.CloseClipboard();if(cmd==='cut'){await user32.InsertControlText('text','');await markDirty();await updateStatus();}}else if(cmd==='paste'){await user32.OpenClipboard(hwnd);const v=String(await user32.GetClipboardData(user32.CF_UNICODETEXT)||'');await user32.CloseClipboard();if(v){await user32.InsertControlText('text',v);await markDirty();await updateStatus();}}}catch(err){try{await user32.CloseClipboard();}catch(ignore){}await comctl32.StatusBar_SetText('statusbar',0,'剪貼簿：'+String(err&&err.message?err.message:err));}};\nconst choosePath=async()=>{\n  const cwd=await kernel32.GetCurrentDirectory();\n  const r=await comdlg32.GetSaveFileName({\n    hwndOwner:hwnd,title:'另存新檔',\n    initialDir:path?parentPath(path):cwd,\n    file:path?leaf(path):'未命名.txt',defaultExt:'txt',overwritePrompt:true,\n    filter:[{name:'文字文件',pattern:'*.txt;*.log;*.md'},{name:'所有檔案',pattern:'*.*'}]\n  });\n  return r?String(r.file||''):'';\n};\nconst openFile=async()=>{\n  if(dirty&&!await user32.ConfirmBox('目前文件尚未儲存，仍要開啟其他檔案嗎？','記事本'))return false;\n  const cwd=await kernel32.GetCurrentDirectory();\n  const r=await comdlg32.GetOpenFileName({\n    hwndOwner:hwnd,title:'開啟文字文件',initialDir:path?parentPath(path):cwd,\n    filter:[{name:'文字文件',pattern:'*.txt;*.log;*.md;*.csv;*.html;*.htm'},{name:'所有檔案',pattern:'*.*'}]\n  });\n  if(!r)return false;\n  const next=String(r.file||''),text=await kernel32.ReadTextFile(next);\n  path=next;fileName=leaf(path)||'未命名.txt';src=text;lineEnding=/\\r\\n/.test(text)?'CRLF':'LF';\n  history=[text];historyIndex=0;historyMute=true;await user32.SetControlText('text',text);historyMute=false;dirty=false;\n  await updateTitle();await updateStatus(text,0);await comctl32.StatusBar_SetText('statusbar',0,'已開啟 ｜ '+path);await user32.FocusControl('text');return true;\n};\nconst save=async forceAs=>{\n  if(forceAs||!path){const chosen=await choosePath();if(!chosen)return false;path=chosen;fileName=leaf(path)||'未命名.txt';}\n  let text=String(await user32.GetControlText('text'));text=text.replace(/\\r?\\n/g,lineEnding==='CRLF'?'\\r\\n':'\\n');const bytes=await kernel32.WriteTextFile(path,text);dirty=false;await updateTitle();await comctl32.StatusBar_SetText('statusbar',0,`已儲存 ｜ ${bytes} bytes ｜ ${path}`);await updateStatus(text);return true;\n};\nconst showSearch=async replaceMode=>{\n  await user32.SetControlStyle('searchPanel',{display:'flex'});\n  await user32.SetControlStyle('replaceText',{display:replaceMode?'block':'none'});\n  await user32.SetControlStyle('replaceOne',{display:replaceMode?'block':'none'});\n  await user32.SetControlStyle('replaceAll',{display:replaceMode?'block':'none'});\n  await user32.FocusControl('findText');\n};\nconst hideSearch=async()=>{await user32.SetControlStyle('searchPanel',{display:'none'});await user32.FocusControl('text');};\nconst findNext=async backwards=>{\n  const needle=String(await user32.GetControlText('findText')||lastFind||'');if(!needle)return false;lastFind=needle;\n  const text=String(await user32.GetControlText('text'));const sel=await user32.GetControlProperty('text','selection')||{start:0,end:0};\n  let at;\n  if(backwards){const from=Math.max(0,Number(sel.start)||0)-1;at=text.lastIndexOf(needle,from);if(at<0)at=text.lastIndexOf(needle);}else{const from=Math.max(Number(sel.end)||0,0);at=text.indexOf(needle,from);if(at<0)at=text.indexOf(needle);}\n  if(at<0){await comctl32.StatusBar_SetText('statusbar',0,`找不到「${needle}」`);return false;}\n  await user32.SetControlProperty('text','selection',{start:at,end:at+needle.length});await updateStatus(text,at);await comctl32.StatusBar_SetText('statusbar',0,`找到「${needle}」`);return true;\n};\nconst replaceOne=async()=>{\n  const needle=String(await user32.GetControlText('findText'));if(!needle)return;\n  const replacement=String(await user32.GetControlText('replaceText'));const text=String(await user32.GetControlText('text'));const sel=await user32.GetControlProperty('text','selection')||{start:0,end:0};\n  if(text.slice(sel.start,sel.end)!==needle){if(!await findNext(false))return;return replaceOne();}\n  await user32.InsertControlText('text',replacement);await markDirty();await updateStatus();await findNext(false);\n};\nconst replaceAll=async()=>{\n  const needle=String(await user32.GetControlText('findText'));if(!needle)return;\n  const replacement=String(await user32.GetControlText('replaceText'));const text=String(await user32.GetControlText('text'));const count=text.split(needle).length-1;\n  if(!count){await comctl32.StatusBar_SetText('statusbar',0,`找不到「${needle}」`);return;}\n  const out=text.split(needle).join(replacement);await user32.SetControlText('text',out);await markDirty();await updateStatus(out);await comctl32.StatusBar_SetText('statusbar',0,`已取代 ${count} 處`);\n};\nconst goToLine=async()=>{\n  const text=String(await user32.GetControlText('text'));const lines=text.split('\\n');const raw=await user32.PromptBox(`請輸入行號 (1-${lines.length})：`,'跳到行','1');if(raw===null)return;\n  const line=Math.max(1,Math.min(lines.length,parseInt(raw,10)||1));let pos=0;for(let i=0;i<line-1;i++)pos+=lines[i].length+1;await user32.SetControlProperty('text','selection',{start:pos,end:pos});await updateStatus(text,pos);\n};\nconst pushHistory=async value=>{\n  if(historyMute)return;\n  value=String(value??await user32.GetControlText('text'));\n  if(history[historyIndex]===value)return;\n  history=history.slice(0,historyIndex+1);\n  history.push(value);\n  if(history.length>120)history.shift();\n  historyIndex=history.length-1;\n};\nconst restoreHistory=async index=>{\n  if(index<0||index>=history.length||index===historyIndex)return false;\n  historyIndex=index;historyMute=true;\n  const value=history[historyIndex];\n  await user32.SetControlText('text',value);\n  historyMute=false;await markDirty();await updateStatus(value);await user32.FocusControl('text');return true;\n};\nconst undo=()=>restoreHistory(historyIndex-1);\nconst redo=()=>restoreHistory(historyIndex+1);\nconst transformSelection=async fn=>{\n  const text=String(await user32.GetControlText('text'));\n  const sel=await user32.GetControlProperty('text','selection')||{start:0,end:0};\n  if(Number(sel.end)<=Number(sel.start)){await comctl32.StatusBar_SetText('statusbar',0,'請先選取文字');return false;}\n  const out=text.slice(0,sel.start)+fn(text.slice(sel.start,sel.end))+text.slice(sel.end);\n  historyMute=true;await user32.SetControlText('text',out);historyMute=false;\n  await user32.SetControlProperty('text','selection',{start:sel.start,end:sel.start+fn(text.slice(sel.start,sel.end)).length});\n  await pushHistory(out);await markDirty();await updateStatus(out,sel.start);return true;\n};\nconst duplicateLine=async()=>{\n  const text=String(await user32.GetControlText('text'));const sel=await user32.GetControlProperty('text','selection')||{start:0,end:0};const pos=Number(sel.start)||0;\n  const start=text.lastIndexOf('\\n',Math.max(0,pos-1))+1;let end=text.indexOf('\\n',pos);if(end<0)end=text.length;\n  const line=text.slice(start,end);const insertion=(end<text.length?'\\n':'')+line;\n  const out=text.slice(0,end)+insertion+text.slice(end);historyMute=true;await user32.SetControlText('text',out);historyMute=false;await pushHistory(out);await markDirty();await user32.SetControlProperty('text','selection',{start:end+1,end:end+1+line.length});await updateStatus(out,end+1);\n};\nconst sortLines=async()=>{\n  const text=String(await user32.GetControlText('text'));const sel=await user32.GetControlProperty('text','selection')||{start:0,end:0};let start=Number(sel.start)||0,end=Number(sel.end)||0;\n  if(end<=start){start=0;end=text.length;}else{start=text.lastIndexOf('\\n',Math.max(0,start-1))+1;const n=text.indexOf('\\n',end);end=n<0?text.length:n;}\n  const block=text.slice(start,end);const outBlock=block.split(/\\r?\\n/).sort((a,b)=>a.localeCompare(b,undefined,{numeric:true,sensitivity:'base'})).join('\\n');\n  const out=text.slice(0,start)+outBlock+text.slice(end);historyMute=true;await user32.SetControlText('text',out);historyMute=false;await pushHistory(out);await markDirty();await user32.SetControlProperty('text','selection',{start,end:start+outBlock.length});await updateStatus(out,start);\n};\nconst showStats=async()=>{\n  const text=String(await user32.GetControlText('text'));const lines=text.length?text.split(/\\r?\\n/).length:1;const words=(text.trim().match(/\\S+/g)||[]).length;const nonspace=(text.match(/\\S/g)||[]).length;\n  await user32.MessageBox(`行數：${lines}\\n字元：${text.length}\\n非空白字元：${nonspace}\\n字詞：${words}\\nUTF-8 預估 bytes：${new TextEncoder().encode(text).length}\\n行尾：${lineEnding}`,'文件統計');\n};\nconst setWrap=async enabled=>{wrap=!!enabled;await user32.SetControlProperty('text','wrap',wrap?'soft':false);await user32.SetControlStyle('text',{whiteSpace:wrap?'pre-wrap':'pre',overflowX:wrap?'hidden':'auto'});await updateStatus();};\nconst setFont=async delta=>{fontSize=Math.max(10,Math.min(28,fontSize+delta));await user32.SetControlStyle('text',{fontSize:fontSize+'px'});await comctl32.StatusBar_SetText('statusbar',0,`字體大小 ${fontSize}px`);};\nconst resetFont=async()=>{fontSize=14;await user32.SetControlStyle('text',{fontSize:'14px'});await comctl32.StatusBar_SetText('statusbar',0,'字體大小 14px');};\nconst insertTimestamp=async()=>{const now=new Date();const stamp=now.toLocaleString('zh-TW',{hour12:false});await user32.InsertControlText('text',stamp);await markDirty();await updateStatus();};\nconst trimTrailing=async()=>{const text=String(await user32.GetControlText('text'));const out=text.split(/\\r?\\n/).map(x=>x.replace(/[ \\t]+$/,'')).join(lineEnding==='CRLF'?'\\r\\n':'\\n');if(out!==text){await user32.SetControlText('text',out);await markDirty();await updateStatus(out);await comctl32.StatusBar_SetText('statusbar',0,'已清除行尾空白');}else await comctl32.StatusBar_SetText('statusbar',0,'沒有可清除的行尾空白');};\nconst toggleLineEnding=async()=>{lineEnding=lineEnding==='CRLF'?'LF':'CRLF';await comctl32.Toolbar_SetButtonText('toolbar','lineending',lineEnding);await updateStatus();await comctl32.StatusBar_SetText('statusbar',0,`儲存時使用 ${lineEnding} 行尾`);};\nconst selectAll=async()=>{const text=String(await user32.GetControlText('text'));await user32.SetControlProperty('text','selection',{start:0,end:text.length});await user32.FocusControl('text');};\ncomctl32.OnNotify('toolbar',async e=>{if(e.code!=='TBN_CLICK')return;const b=e.data&&e.data.button;if(!b)return;switch(b.id){case'new':await newDocument();break;case'open':await openFile();break;case'save':await save(false);break;case'saveas':await save(true);break;case'undo':await undo();break;case'redo':await redo();break;case'cut':await clipboardCommand('cut');break;case'copy':await clipboardCommand('copy');break;case'paste':await clipboardCommand('paste');break;case'find':await showSearch(false);break;case'replace':await showSearch(true);break;case'goto':await goToLine();break;case'timestamp':await insertTimestamp();break;case'duplicate':await duplicateLine();break;case'upper':await transformSelection(x=>x.toUpperCase());break;case'lower':await transformSelection(x=>x.toLowerCase());break;case'sortlines':await sortLines();break;case'trim':await trimTrailing();break;case'stats':await showStats();break;case'lineending':await toggleLineEnding();break;case'selectall':await selectAll();break;case'wrap':await setWrap(!!b.checked);break;case'fontminus':await setFont(-1);break;case'fontreset':await resetFont();break;case'fontplus':await setFont(1);break;}});\nuser32.OnControl('findPrev',async e=>{if(e.action==='click')await findNext(true);});\nuser32.OnControl('findNext',async e=>{if(e.action==='click')await findNext(false);});\nuser32.OnControl('replaceOne',async e=>{if(e.action==='click')await replaceOne();});\nuser32.OnControl('replaceAll',async e=>{if(e.action==='click')await replaceAll();});\nuser32.OnControl('closeSearch',async e=>{if(e.action==='click')await hideSearch();});\nuser32.OnControl('findText',async e=>{if(e.action==='keydown'&&e.key==='Enter')await findNext(!!e.shiftKey);if(e.action==='keydown'&&e.key==='Escape')await hideSearch();});\nuser32.OnControl('text',async e=>{\n  if(e.action==='input'){await pushHistory(e.value);await markDirty();await updateStatus(e.value,e.selectionStart);return;}\n  if(e.action==='click'||e.action==='keyup'||e.action==='focus'){await updateStatus(e.value,e.selectionStart);}\n  if(e.action==='keydown'&&!e.ctrlKey&&String(e.key||'')==='F5'){await insertTimestamp();return;}if(e.action==='keydown'&&e.ctrlKey){const k=String(e.key||'').toLowerCase();if(k==='o')return openFile();if(k==='s')return save(!!e.shiftKey);if(k==='n')return newDocument();if(k==='f')return showSearch(false);if(k==='h')return showSearch(true);if(k==='g')return goToLine();if(k==='z')return undo();if(k==='y')return redo();if(k==='d')return duplicateLine();}\n  if(e.action==='keydown'&&String(e.key||'')==='F3')await findNext(!!e.shiftKey);\n});\nawait updateStatus(src,0);await user32.FocusControl('text');\n"},"csvedit":{"id":"csvedit","fileName":"csvedit.xsh","title":"CSV 編輯器","description":"ExOS CSV Editor (.xsh / paged grid / row-column operations / sort-search)","icon":"csv","singleInstance":false,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst comdlg32=ExOS.LoadLibrary('comdlg32.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.TOOLBARCLASSNAME,comctl32.STATUSCLASSNAME]});\nlet path=String(process.argv[1]||'');\nconst leaf=p=>{p=String(p||'').replace(/\\//g,'\\\\');const i=p.lastIndexOf('\\\\');return i>=0?p.substring(i+1):p;};\nconst joinPath=(base,name)=>{base=String(base||'C:\\\\').replace(/\\//g,'\\\\').replace(/\\\\+$/,'');if(/^C:$/i.test(base))base+='\\\\';return base==='C:\\\\'?base+name:base+'\\\\'+name;};\nconst parentPath=p=>{p=String(p||'').replace(/\\//g,'\\\\');const i=p.lastIndexOf('\\\\');return i>2?p.substring(0,i):'C:\\\\';};\nlet fileName=path?(leaf(path)||'data.csv'):'data.csv';\nconst source=path?await kernel32.ReadTextFile(path):'';\nconst parseCsv=text=>{text=String(text||'').replace(/^\\uFEFF/,'');const rows=[];let row=[],cell='',q=false;for(let i=0;i<text.length;i++){const ch=text[i];if(q){if(ch==='\"'){if(text[i+1]==='\"'){cell+='\"';i++;}else q=false;}else cell+=ch;continue;}if(ch==='\"')q=true;else if(ch===','){row.push(cell);cell='';}else if(ch==='\\r'){if(text[i+1]==='\\n')i++;row.push(cell);rows.push(row);row=[];cell='';}else if(ch==='\\n'){row.push(cell);rows.push(row);row=[];cell='';}else cell+=ch;}if(cell!==''||row.length||!rows.length){row.push(cell);rows.push(row);}return rows;};\nconst esc=v=>{v=String(v??'');return /[\",\\r\\n]/.test(v)?'\"'+v.replace(/\"/g,'\"\"')+'\"':v;};\nconst serialize=rows=>rows.map(r=>r.map(esc).join(',')).join('\\r\\n');\nlet data=parseCsv(source);if(!data.length)data=[['']];\nconst colCount=()=>{let n=1;for(const r of data)n=Math.max(n,r.length);return n;};\nconst normalize=()=>{const cols=colCount();for(const r of data)while(r.length<cols)r.push('');return cols;};\nconst colName=index=>{let n=index+1,out='';while(n>0){n--;out=String.fromCharCode(65+(n%26))+out;n=Math.floor(n/26);}return out;};\nlet dirty=false,selectedRow=0,selectedCol=0,rowPage=0,colPage=0,headerMode=true;\nconst ROW_PAGE=60,COL_PAGE=20;\nconst hwnd=await user32.CreateWindow({title:'CSV 編輯器 - '+fileName,icon:'csv',width:1220,height:780});\nawait uxtheme.SetWindowTheme(hwnd,'CsvEditor','Document');\nawait dwmapi.DwmSetWindowAttribute(hwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',overflow:'hidden'}});\nawait comctl32.CreateToolbar(hwnd,{id:'toolbar',parentId:'root',buttons:[\n  {id:'new',text:'新增',title:'新增 CSV (Ctrl+N)'},{id:'open',text:'開啟',title:'開啟 CSV (Ctrl+O)'},{id:'save',text:'儲存',title:'儲存 (Ctrl+S)'},{id:'saveas',text:'另存新檔'},{separator:true},\n  {id:'addrowbefore',text:'列↑',title:'在目前列前插入'},{id:'addrow',text:'列↓',title:'在目前列後插入'},{id:'duprow',text:'複製列',title:'複製目前列'},{id:'delrow',text:'－列'},{separator:true},\n  {id:'addcolbefore',text:'欄←',title:'在目前欄前插入'},{id:'addcol',text:'欄→',title:'在目前欄後插入'},{id:'delcol',text:'－欄'},{id:'clearcell',text:'清除格',title:'清除目前儲存格'},{id:'copycell',text:'複製格',title:'複製目前儲存格到剪貼簿'},{id:'pastecell',text:'貼上格',title:'從剪貼簿貼到目前儲存格'},{separator:true},\n  {id:'find',text:'尋找',title:'尋找 (Ctrl+F)'},{id:'sortasc',text:'A→Z',title:'依目前欄升冪'},{id:'sortdesc',text:'Z→A',title:'依目前欄降冪'},{id:'colstats',text:'欄統計',title:'統計目前欄的唯一值與數值資料'},{id:'trimall',text:'清理空白',title:'移除所有儲存格前後空白'},{id:'dedupe',text:'移除重複',title:'移除重複資料列'},{id:'transpose',text:'轉置',title:'列欄互換'},{id:'header',text:'首列為標題',checkable:true,checked:true},{separator:true},\n  {id:'prevRows',text:'上一頁列'},{id:'nextRows',text:'下一頁列'},{id:'prevCols',text:'←欄'},{id:'nextCols',text:'欄→'}\n],style:{width:'100%',margin:'0 0 6px'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'formula',parentId:'root',style:{display:'flex',alignItems:'center',gap:'6px',width:'100%',margin:'0 0 6px'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'cellRef',parentId:'formula',text:'A1',style:{width:'64px',margin:'0',padding:'6px',boxSizing:'border-box',textAlign:'center',fontFamily:'Consolas,monospace',fontWeight:'600',border:'1px solid #cbd5e1',background:'#f8fafc'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'cellValue',parentId:'formula',text:String(data[0][0]??''),spellcheck:false,style:{flex:'1',width:'auto',margin:'0',fontFamily:'Consolas,monospace'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'applyCell',parentId:'formula',text:'套用',style:{margin:'0',minWidth:'64px'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'findPanel',parentId:'root',style:{display:'none',alignItems:'center',gap:'6px',width:'100%',margin:'0 0 6px',padding:'6px',boxSizing:'border-box',border:'1px solid #dbe3ec',background:'#f8fafc'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'findText',parentId:'findPanel',placeholder:'尋找儲存格內容',spellcheck:false,style:{flex:'1',width:'auto',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'findNext',parentId:'findPanel',text:'下一個',style:{margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'closeFind',parentId:'findPanel',text:'×',style:{margin:'0',minWidth:'34px'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'viewport',parentId:'root',style:{flex:'1',minHeight:'0',width:'100%',overflow:'auto',border:'1px solid #cbd5e1',background:'#ffffff',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'grid',parentId:'viewport',style:{display:'grid',gap:'1px',width:'100%',minWidth:'720px',margin:'0',padding:'4px',boxSizing:'border-box',background:'#e2e8f0'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'statusbar',parentId:'root',parts:[{text:'已載入',width:0},{text:'A1',width:90},{text:'',width:170},{text:'',width:160}],style:{width:'100%',margin:'6px 0 0'}});\nconst updateTitle=()=>user32.SetWindowText(hwnd,(dirty?'*':'')+'CSV 編輯器 - '+fileName);\nconst markDirty=async()=>{if(!dirty){dirty=true;await updateTitle();}await comctl32.StatusBar_SetText('statusbar',0,'尚未儲存');};\nconst clampSelection=()=>{selectedRow=Math.max(0,Math.min(selectedRow,data.length-1));selectedCol=Math.max(0,Math.min(selectedCol,colCount()-1));};\nconst updateSelectionUi=async()=>{clampSelection();normalize();await user32.SetControlText('cellRef',`${colName(selectedCol)}${selectedRow+1}`);await user32.SetControlText('cellValue',String(data[selectedRow][selectedCol]??''));await comctl32.StatusBar_SetText('statusbar',1,`${colName(selectedCol)}${selectedRow+1}`);await comctl32.StatusBar_SetText('statusbar',2,`${data.length} 列 × ${colCount()} 欄`);const rp=Math.max(1,Math.ceil(data.length/ROW_PAGE)),cp=Math.max(1,Math.ceil(colCount()/COL_PAGE));await comctl32.StatusBar_SetText('statusbar',3,`列頁 ${rowPage+1}/${rp} ｜ 欄頁 ${colPage+1}/${cp}`);};\nconst render=async focusSelected=>{\n  normalize();const cols=colCount();const rowStart=rowPage*ROW_PAGE,rowEnd=Math.min(data.length,rowStart+ROW_PAGE),colStart=colPage*COL_PAGE,colEnd=Math.min(cols,colStart+COL_PAGE),visibleCols=Math.max(1,colEnd-colStart);\n  if(rowStart>=data.length){rowPage=Math.max(0,Math.ceil(data.length/ROW_PAGE)-1);return render(focusSelected);}if(colStart>=cols){colPage=Math.max(0,Math.ceil(cols/COL_PAGE)-1);return render(focusSelected);}\n  await user32.ClearControlChildren('grid');await user32.SetControlStyle('grid',{gridTemplateColumns:`52px repeat(${visibleCols},minmax(110px,1fr))`});\n  await user32.CreateControl(hwnd,{type:'label',id:'corner',parentId:'grid',text:'#',style:{margin:'0',padding:'6px',textAlign:'center',fontWeight:'600',background:'#f1f5f9'}});\n  for(let c=colStart;c<colEnd;c++)await user32.CreateControl(hwnd,{type:'label',id:`head_${c}`,parentId:'grid',text:colName(c),style:{margin:'0',padding:'6px',textAlign:'center',fontWeight:'600',background:'#f1f5f9',overflow:'hidden'}});\n  for(let r=rowStart;r<rowEnd;r++){\n    await user32.CreateControl(hwnd,{type:'label',id:`row_${r}`,parentId:'grid',text:String(r+1),style:{margin:'0',padding:'6px',textAlign:'center',fontFamily:'Consolas,monospace',background:'#f8fafc'}});\n    for(let c=colStart;c<colEnd;c++){\n      const id=`cell_${r-rowStart}_${c-colStart}`;\n      await user32.CreateControl(hwnd,{type:'input',id,parentId:'grid',text:String(data[r][c]??''),spellcheck:false,style:{width:'100%',minWidth:'0',margin:'0',padding:'6px',boxSizing:'border-box',fontFamily:'Consolas,\"Microsoft JhengHei\",monospace',fontSize:'12px',borderRadius:'0'}});\n      user32.OnControl(id,async e=>{\n        if(e.action==='focus'||e.action==='click'){selectedRow=r;selectedCol=c;await updateSelectionUi();}\n        if(e.action==='input'){selectedRow=r;selectedCol=c;data[r][c]=String(e.value??'');await user32.SetControlText('cellValue',data[r][c]);await markDirty();await updateSelectionUi();}\n        if(e.action==='keydown'&&String(e.key||'')==='Delete'){selectedRow=r;selectedCol=c;data[r][c]='';await markDirty();await render(true);return;}\n        if(e.action==='keydown'&&e.ctrlKey){const k=String(e.key||'').toLowerCase();if(k==='o')return openFile();if(k==='s')return save(!!e.shiftKey);if(k==='n')return newSheet();if(k==='f')return showFind();if(k==='d')return duplicateRow();}\n        if(e.action==='keydown'&&!e.ctrlKey&&!e.altKey){const k=String(e.key||'');if(k==='ArrowUp')return moveSelection(-1,0);if(k==='ArrowDown'||k==='Enter')return moveSelection(1,0);if(k==='ArrowLeft')return moveSelection(0,-1);if(k==='ArrowRight'||k==='Tab')return moveSelection(0,1);}\n      });\n    }\n  }\n  await updateSelectionUi();\n  if(focusSelected&&selectedRow>=rowStart&&selectedRow<rowEnd&&selectedCol>=colStart&&selectedCol<colEnd)await user32.FocusControl(`cell_${selectedRow-rowStart}_${selectedCol-colStart}`);\n};\nconst newSheet=async()=>{if(dirty&&!await user32.ConfirmBox('目前 CSV 尚未儲存，仍要建立新試算表嗎？','CSV 編輯器'))return false;path='';fileName='data.csv';data=[['']];dirty=false;selectedRow=0;selectedCol=0;rowPage=0;colPage=0;await updateTitle();await render(true);await comctl32.StatusBar_SetText('statusbar',0,'新 CSV');return true;};\nconst copyCell=async()=>{clampSelection();normalize();try{await user32.OpenClipboard(hwnd);await user32.EmptyClipboard();await user32.SetClipboardData(user32.CF_UNICODETEXT,String(data[selectedRow][selectedCol]??''));await user32.CloseClipboard();await comctl32.StatusBar_SetText('statusbar',0,'已複製 '+colName(selectedCol)+(selectedRow+1));}catch(err){try{await user32.CloseClipboard();}catch(ignore){}}};\nconst pasteCell=async()=>{clampSelection();try{await user32.OpenClipboard(hwnd);const v=String(await user32.GetClipboardData(user32.CF_UNICODETEXT)||'');await user32.CloseClipboard();data[selectedRow][selectedCol]=v;await markDirty();await render(true);}catch(err){try{await user32.CloseClipboard();}catch(ignore){}}};\nconst choosePath=async()=>{\n  const cwd=await kernel32.GetCurrentDirectory();\n  const r=await comdlg32.GetSaveFileName({hwndOwner:hwnd,title:'CSV 另存新檔',initialDir:path?parentPath(path):cwd,file:path?leaf(path):'data.csv',defaultExt:'csv',overwritePrompt:true,filter:[{name:'CSV (Comma delimited)',pattern:'*.csv'},{name:'所有檔案',pattern:'*.*'}]});\n  return r?String(r.file||''):'';\n};\nconst openFile=async()=>{\n  if(dirty&&!await user32.ConfirmBox('目前 CSV 尚未儲存，仍要開啟其他檔案嗎？','CSV 編輯器'))return false;\n  const cwd=await kernel32.GetCurrentDirectory();\n  const r=await comdlg32.GetOpenFileName({hwndOwner:hwnd,title:'開啟 CSV',initialDir:path?parentPath(path):cwd,filter:[{name:'CSV 檔案',pattern:'*.csv'},{name:'所有檔案',pattern:'*.*'}]});\n  if(!r)return false;\n  path=String(r.file||'');fileName=leaf(path)||'data.csv';data=parseCsv(await kernel32.ReadTextFile(path));if(!data.length)data=[['']];\n  selectedRow=0;selectedCol=0;rowPage=0;colPage=0;dirty=false;await updateTitle();await render(true);await comctl32.StatusBar_SetText('statusbar',0,'已開啟 ｜ '+path);return true;\n};\nconst save=async forceAs=>{if(forceAs||!path){const chosen=await choosePath();if(!chosen)return false;path=chosen;fileName=leaf(path)||'data.csv';}normalize();const text=serialize(data);const bytes=await kernel32.WriteTextFile(path,text);dirty=false;await updateTitle();await comctl32.StatusBar_SetText('statusbar',0,`已儲存 ｜ ${bytes} bytes ｜ ${path}`);return true;};\nconst applyCell=async()=>{clampSelection();normalize();data[selectedRow][selectedCol]=String(await user32.GetControlText('cellValue'));await markDirty();await render(true);};\nconst showFind=async()=>{await user32.SetControlStyle('findPanel',{display:'flex'});await user32.FocusControl('findText');};\nconst hideFind=async()=>{await user32.SetControlStyle('findPanel',{display:'none'});await render(true);};\nconst findNext=async()=>{const needle=String(await user32.GetControlText('findText'));if(!needle)return false;const rows=data.length,cols=colCount(),start=selectedRow*cols+selectedCol+1,total=rows*cols;for(let step=0;step<total;step++){const flat=(start+step)%total,r=Math.floor(flat/cols),c=flat%cols;if(String(data[r][c]??'').includes(needle)){selectedRow=r;selectedCol=c;rowPage=Math.floor(r/ROW_PAGE);colPage=Math.floor(c/COL_PAGE);await render(true);await comctl32.StatusBar_SetText('statusbar',0,`找到「${needle}」`);return true;}}await comctl32.StatusBar_SetText('statusbar',0,`找不到「${needle}」`);return false;};\nconst insertRow=async before=>{normalize();const row=Array(colCount()).fill('');const at=Math.max(0,Math.min(data.length,before?selectedRow:selectedRow+1));data.splice(at,0,row);selectedRow=at;rowPage=Math.floor(selectedRow/ROW_PAGE);await markDirty();await render(true);};\nconst addRow=async()=>insertRow(false);\nconst addRowBefore=async()=>insertRow(true);\nconst duplicateRow=async()=>{normalize();const row=data[selectedRow].slice();const at=Math.min(data.length,selectedRow+1);data.splice(at,0,row);selectedRow=at;rowPage=Math.floor(selectedRow/ROW_PAGE);await markDirty();await render(true);};\nconst insertCol=async before=>{normalize();const at=Math.max(0,Math.min(colCount(),before?selectedCol:selectedCol+1));for(const r of data)r.splice(at,0,'');selectedCol=at;colPage=Math.floor(selectedCol/COL_PAGE);await markDirty();await render(true);};\nconst addCol=async()=>insertCol(false);\nconst addColBefore=async()=>insertCol(true);\nconst clearCell=async()=>{normalize();data[selectedRow][selectedCol]='';await markDirty();await render(true);};\nconst delRow=async()=>{if(data.length<=1)data=[Array(colCount()).fill('')];else data.splice(selectedRow,1);selectedRow=Math.max(0,Math.min(selectedRow,data.length-1));rowPage=Math.floor(selectedRow/ROW_PAGE);await markDirty();await render(true);};\nconst delCol=async()=>{normalize();if(colCount()<=1){for(const r of data)r[0]='';selectedCol=0;}else{for(const r of data)r.splice(selectedCol,1);selectedCol=Math.max(0,Math.min(selectedCol,colCount()-1));}colPage=Math.floor(selectedCol/COL_PAGE);await markDirty();await render(true);};\nconst moveSelection=async(dr,dc)=>{normalize();selectedRow=Math.max(0,Math.min(data.length-1,selectedRow+dr));selectedCol=Math.max(0,Math.min(colCount()-1,selectedCol+dc));rowPage=Math.floor(selectedRow/ROW_PAGE);colPage=Math.floor(selectedCol/COL_PAGE);await render(true);};\nconst columnStats=async()=>{normalize();const start=headerMode&&data.length>1?1:0;const vals=data.slice(start).map(r=>String(r[selectedCol]??''));const nonempty=vals.filter(v=>v!=='');const unique=new Set(nonempty);const nums=nonempty.map(v=>Number(String(v).replace(/,/g,''))).filter(v=>Number.isFinite(v));let numeric='沒有可統計的數值';if(nums.length){const sum=nums.reduce((a,b)=>a+b,0),avg=sum/nums.length,min=Math.min(...nums),max=Math.max(...nums);numeric=`數值筆數：${nums.length}\\n合計：${sum}\\n平均：${avg}\\n最小：${min}\\n最大：${max}`;}await user32.MessageBox(`欄位：${colName(selectedCol)}\\n資料列：${vals.length}\\n非空白：${nonempty.length}\\n唯一值：${unique.size}\\n\\n${numeric}`,'CSV 欄統計');};\nconst trimAll=async()=>{let changed=0;for(const row of data)for(let c=0;c<row.length;c++){const old=String(row[c]??''),next=old.trim();if(old!==next){row[c]=next;changed++;}}if(changed){await markDirty();await render(false);}await comctl32.StatusBar_SetText('statusbar',0,changed?`已清理 ${changed} 個儲存格空白`:'沒有需要清理的空白');};\nconst removeDuplicates=async()=>{normalize();const head=headerMode&&data.length?data[0]:null;const rows=headerMode?data.slice(1):data.slice();const seen=new Set(),out=[];let removed=0;for(const row of rows){const key=JSON.stringify(row);if(seen.has(key)){removed++;continue;}seen.add(key);out.push(row);}if(!removed){await comctl32.StatusBar_SetText('statusbar',0,'沒有重複資料列');return;}const ok=await user32.ConfirmBox(`找到 ${removed} 個重複資料列，確定移除？`,'CSV 編輯器');if(!ok)return;data=head?[head,...out]:out;if(!data.length)data=[['']];selectedRow=Math.min(selectedRow,data.length-1);await markDirty();await render(true);await comctl32.StatusBar_SetText('statusbar',0,`已移除 ${removed} 個重複資料列`);};\nconst transpose=async()=>{normalize();const rows=data.length,cols=colCount();if(rows*cols>200000){await user32.MessageBox('資料量過大，轉置限制為 200,000 個儲存格。','CSV 編輯器');return;}const ok=await user32.ConfirmBox(`將 ${rows} 列 × ${cols} 欄轉置成 ${cols} 列 × ${rows} 欄？`,'CSV 轉置');if(!ok)return;const out=Array.from({length:cols},(_,c)=>Array.from({length:rows},(_,r)=>String(data[r][c]??'')));data=out.length?out:[['']];selectedRow=0;selectedCol=0;rowPage=0;colPage=0;await markDirty();await render(true);};\nconst sortCurrent=async asc=>{normalize();const head=headerMode&&data.length>1?data.shift():null;const c=selectedCol;data.sort((a,b)=>String(a[c]??'').localeCompare(String(b[c]??''),undefined,{numeric:true,sensitivity:'base'})*(asc?1:-1));if(head)data.unshift(head);await markDirty();await render(false);};\nconst movePage=async(dr,dc)=>{rowPage=Math.max(0,Math.min(Math.max(0,Math.ceil(data.length/ROW_PAGE)-1),rowPage+dr));colPage=Math.max(0,Math.min(Math.max(0,Math.ceil(colCount()/COL_PAGE)-1),colPage+dc));selectedRow=Math.min(data.length-1,rowPage*ROW_PAGE);selectedCol=Math.min(colCount()-1,colPage*COL_PAGE);await render(true);};\ncomctl32.OnNotify('toolbar',async e=>{if(e.code!=='TBN_CLICK')return;const b=e.data&&e.data.button;if(!b)return;switch(b.id){case'new':await newSheet();break;case'open':await openFile();break;case'save':await save(false);break;case'saveas':await save(true);break;case'addrowbefore':await addRowBefore();break;case'addrow':await addRow();break;case'duprow':await duplicateRow();break;case'addcolbefore':await addColBefore();break;case'addcol':await addCol();break;case'clearcell':await clearCell();break;case'copycell':await copyCell();break;case'pastecell':await pasteCell();break;case'delrow':await delRow();break;case'delcol':await delCol();break;case'find':await showFind();break;case'sortasc':await sortCurrent(true);break;case'sortdesc':await sortCurrent(false);break;case'colstats':await columnStats();break;case'trimall':await trimAll();break;case'dedupe':await removeDuplicates();break;case'transpose':await transpose();break;case'header':headerMode=!!b.checked;await comctl32.StatusBar_SetText('statusbar',0,headerMode?'排序時保留首列':'排序包含首列');break;case'prevRows':await movePage(-1,0);break;case'nextRows':await movePage(1,0);break;case'prevCols':await movePage(0,-1);break;case'nextCols':await movePage(0,1);break;}});\nuser32.OnControl('applyCell',async e=>{if(e.action==='click')await applyCell();});\nuser32.OnControl('cellValue',async e=>{if(e.action==='keydown'&&e.key==='Enter')await applyCell();if(e.action==='keydown'&&e.ctrlKey){const k=String(e.key||'').toLowerCase();if(k==='o')await openFile();if(k==='s')await save(!!e.shiftKey);if(k==='n')await newSheet();}});\nuser32.OnControl('findNext',async e=>{if(e.action==='click')await findNext();});user32.OnControl('closeFind',async e=>{if(e.action==='click')await hideFind();});user32.OnControl('findText',async e=>{if(e.action==='keydown'&&e.key==='Enter')await findNext();if(e.action==='keydown'&&e.key==='Escape')await hideFind();});\nawait render(false);\n"},"image_viewer":{"id":"image_viewer","fileName":"image_viewer.xsh","title":"圖片預覽","description":"ExOS Image Viewer (.xsh)","icon":"file","singleInstance":false,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst path=process.argv[1];if(!path)throw new Error('Image viewer requires a file path.');const h=await kernel32.CreateFile(path,kernel32.GENERIC_READ,kernel32.OPEN_EXISTING);const attr=await kernel32.GetFileAttributes(path);const r=await kernel32.ReadFile(h,Math.min((attr&&attr.size)||8388608,8388608),0);await kernel32.CloseHandle(h);const bytes=new Uint8Array(r.data||[]);const ext=(path.split('.').pop()||'').toLowerCase();const mime=ext==='png'?'image/png':ext==='gif'?'image/gif':ext==='webp'?'image/webp':'image/jpeg';let bin='';for(let i=0;i<bytes.length;i+=0x8000)bin+=String.fromCharCode(...bytes.subarray(i,i+0x8000));const url=`data:${mime};base64,${btoa(bin)}`;const hwnd=await user32.CreateWindow({title:'圖片預覽 - '+path.split('\\\\').pop(),icon:'file',width:900,height:680});await user32.CreateControl(hwnd,{type:'image',id:'image',src:url,alt:path,style:{display:'block',maxWidth:'100%',maxHeight:'calc(100% - 34px)',margin:'0 auto'}});await user32.CreateControl(hwnd,{type:'label',id:'info',text:`${path} ｜ ${bytes.length} bytes ｜ ${mime}`,style:{textAlign:'center',marginTop:'6px'}});"},"diskmgmt":{"id":"diskmgmt","fileName":"diskmgmt.xsh","title":"磁碟管理員","description":"ExOS Disk Management (.xsh / ExFS VDO)","icon":"disk","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\n\nawait comctl32.InitCommonControlsEx({\n  classes:[\n    comctl32.PROGRESS_CLASS,\n    comctl32.STATUSCLASSNAME\n  ]\n});\n\nconst hwnd=await user32.CreateWindow({\n  title:'磁碟管理員',\n  icon:'disk',\n  width:920,\n  height:620\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'root',\n  style:{\n    display:'flex',\n    flexDirection:'column',\n    width:'100%',\n    height:'100%',\n    gap:'8px'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'toolbar',\n  parentId:'root',\n  style:{\n    display:'flex',\n    gap:'6px',\n    padding:'5px',\n    border:'1px solid #d7dee8',\n    background:'#f8fafc'\n  }\n});\n\nfor(const [id,text] of [\n  ['refresh','重新整理'],\n  ['topology','3D 拓樸']\n]){\n  await user32.CreateControl(hwnd,{\n    type:'button',\n    id,\n    parentId:'toolbar',\n    text\n  });\n}\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'summary',\n  parentId:'root',\n  style:{\n    padding:'12px',\n    border:'1px solid #d7dee8',\n    background:'#ffffff'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'volume',\n  parentId:'root',\n  style:{\n    padding:'12px',\n    border:'1px solid #d7dee8',\n    background:'#ffffff'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'volumeText',\n  parentId:'volume',\n  text:'',\n  style:{\n    whiteSpace:'pre-wrap'\n  }\n});\n\nawait comctl32.CreateProgressBar(hwnd,{\n  id:'quota',\n  parentId:'volume',\n  min:0,\n  max:100,\n  position:0,\n  style:{\n    width:'100%',\n    height:'20px',\n    margin:'12px 0'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'details',\n  parentId:'root',\n  style:{\n    flex:'1',\n    minHeight:'0',\n    overflow:'auto',\n    padding:'12px',\n    whiteSpace:'pre-wrap',\n    fontFamily:'Consolas,monospace',\n    border:'1px solid #d7dee8',\n    background:'#ffffff'\n  }\n});\n\nawait comctl32.CreateStatusBar(hwnd,{\n  id:'status',\n  parentId:'root',\n  parts:[\n    {text:'',width:0},\n    {text:'XSH / ExFS VDO',width:180}\n  ]\n});\n\nconst fmt=n=>{\n  n=Math.max(0,Number(n)||0);\n  if(n<1024)return n+' B';\n  if(n<1024*1024)return(n/1024).toFixed(1)+' KB';\n  if(n<1024*1024*1024)return(n/1024/1024).toFixed(1)+' MB';\n  return(n/1024/1024/1024).toFixed(2)+' GB';\n};\n\nconst refresh=async()=>{\n  await comctl32.StatusBar_SetText(\n    'status',\n    0,\n    '正在讀取磁碟資訊…'\n  );\n\n  const [info,vdo,bridge,halInfo]=await Promise.all([\n    exes.QueryDiskInfo(),\n    exes.QuerySystemVdo(),\n    io.GetVdoBridge(),\n    hal.QueryCapabilities()\n  ]);\n\n  const quotaEnabled=\n    Number(info.quota_enabled)===1;\n\n  const quota=\n    Number(info.user_quota_bytes)||0;\n\n  const used=\n    Number(info.user_quota_used_bytes)||0;\n\n  const free=\n    Number(info.user_quota_free_bytes)||0;\n\n  const pct=\n    quotaEnabled&&quota>0\n      ?Math.max(\n          0,\n          Math.min(\n            100,\n            used/quota*100\n          )\n        )\n      :0;\n\n  await user32.SetControlText(\n    'summary',\n    '磁碟 0 — ExFS 虛擬磁碟\\n'+\n    String(info.drive||vdo.drive||'C:')+\n    '  '+\n    String(info.label||vdo.label||'ExFS')+\n    '\\n檔案系統：'+\n    String(info.filesystem||vdo.filesystem||'ExFS')+\n    ' ｜ 健康狀態：正常 ｜ 動態配置'\n  );\n\n  await comctl32.ProgressBar_SetPos(\n    'quota',\n    Math.round(pct)\n  );\n\n  await user32.SetControlText(\n    'volumeText',\n    'C:  '+String(info.label||'ExFS')+\n    '\\n'+(\n      quotaEnabled\n        ?(\n          '使用者配額：'+fmt(quota)+\n          ' ｜ 已使用 '+fmt(used)+\n          ' ｜ 可用 '+fmt(free)+\n          ' ｜ '+pct.toFixed(1)+'%'\n        )\n        :'使用者配額：未啟用；ExFS VDO 沒有固定 1 GB 容量。'\n    )\n  );\n\n  await user32.SetControlText(\n    'details',\n    [\n      'Device       : '+String(vdo.deviceName||bridge.device||'\\\\Device\\\\ExFSVdo0'),\n      'DOS link     : '+String(vdo.symbolicLink||'\\\\DosDevices\\\\C:'),\n      'Backing VDO  : '+String(vdo.backingStore||bridge.backing||'PHP /_exfs/'),\n      'Bridge       : '+String(vdo.bridge||bridge.bridge||'PhpExfsBridge.sys'),\n      'Encryption   : '+String(vdo.encryption||'ExES V6'),\n      'Host disk    : 不公開 / not exposed',\n      'HAL          : '+String(halInfo.architecture||'ExOS Browser HAL'),\n      'VolumeSerial : '+String(info.volume_serial||info.volumeSerial||''),\n      'Files        : '+String(info.file_count||info.files||''),\n      'Folders      : '+String(info.folder_count||info.folders||''),\n      'Metadata     : '+String(info.metadata_bytes||info.metadata_size||''),\n      '',\n      '這是 ExOS 的 C: → ExFS → PHP /_exfs/ VDO。',\n      '不代表 Web Server 的實體硬碟，也不直接公開 host filesystem。'\n    ].join('\\n')\n  );\n\n  await comctl32.StatusBar_SetText(\n    'status',\n    0,\n    '磁碟資訊已更新'\n  );\n};\n\nuser32.OnControl('refresh',async e=>{\n  if(e.action==='click'){\n    await refresh();\n  }\n});\n\nuser32.OnControl('topology',async e=>{\n  if(e.action==='click'){\n    await ExOS.LaunchSystemApp(\n      'volume3d'\n    );\n  }\n});\n\nawait refresh();\n"},"control":{"id":"control","fileName":"control.xsh","title":"控制台","description":"ExOS Control Panel (.xsh / system configuration and tools)","icon":"control","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.STATUSCLASSNAME]});\nconst hwnd=await user32.CreateWindow({title:'控制台',icon:'control',width:980,height:700});\nawait uxtheme.SetWindowTheme(hwnd,'ControlPanel','Explorer');\nawait dwmapi.DwmSetWindowAttribute(hwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',background:'#f8fafc'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'head',parentId:'root',style:{display:'flex',alignItems:'center',gap:'8px',padding:'10px 12px',background:'#fff',borderBottom:'1px solid #dbe3ec'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'title',parentId:'head',text:'控制台',style:{fontSize:'22px',fontWeight:'700',margin:'0 12px 0 0'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'search',parentId:'head',placeholder:'搜尋系統設定',spellcheck:false,style:{flex:'1',width:'auto',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'refresh',parentId:'head',text:'重新整理',style:{margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'body',parentId:'root',style:{display:'grid',gridTemplateColumns:'220px minmax(0,1fr)',flex:'1',minHeight:'0',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'nav',parentId:'body',style:{display:'flex',flexDirection:'column',gap:'4px',padding:'10px',overflow:'auto',borderRight:'1px solid #dbe3ec',background:'#f1f5f9'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'navLabel',parentId:'nav',text:'系統設定',style:{fontSize:'11px',fontWeight:'700',color:'#64748b',margin:'4px 6px 2px'}});\nfor(const [id,text] of [['general','一般'],['php','PHP'],['exfs','ExFS'],['environment','環境']]){\n  await user32.CreateControl(hwnd,{type:'button',id:'nav_'+id,parentId:'nav',text,style:{width:'100%',textAlign:'left',margin:'0',padding:'8px 10px'}});\n}\nawait user32.CreateControl(hwnd,{type:'label',id:'toolsLabel',parentId:'nav',text:'系統工具',style:{fontSize:'11px',fontWeight:'700',color:'#64748b',margin:'12px 6px 2px'}});\nfor(const [id,text] of [['accounts','帳號'],['devmgmt','裝置管理員'],['eventvwr','事件檢視器'],['diskmgmt','磁碟管理員'],['volume3d','3D 拓譜'],['trash','資源回收桶'],['regedit','登錄編輯程式'],['taskmgr','工作管理員'],['security','ExOS 安全性']]){\n  await user32.CreateControl(hwnd,{type:'button',id:'tool_'+id,parentId:'nav',text,style:{width:'100%',textAlign:'left',margin:'0',padding:'8px 10px'}});\n}\nawait user32.CreateControl(hwnd,{type:'div',id:'content',parentId:'body',style:{minWidth:'0',minHeight:'0',overflow:'auto',padding:'18px 20px',background:'#fff'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'sectionTitle',parentId:'content',text:'一般',style:{display:'block',fontSize:'20px',fontWeight:'700',margin:'0 0 12px'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'rows',parentId:'content',style:{display:'grid',gridTemplateColumns:'190px minmax(0,1fr)',gap:'0',border:'1px solid #dbe3ec',borderRadius:'7px',overflow:'hidden',background:'#fff'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'status',parentId:'root',parts:[{text:'正在讀取系統設定…',width:0},{text:'ExConfig / XSH',width:160}],style:{width:'100%',margin:'0'}});\nlet config={},section='general',query='';\nconst labels={general:'一般',php:'PHP',exfs:'ExFS',environment:'環境'};\nconst render=async()=>{\n  await user32.SetControlText('sectionTitle',labels[section]||section);\n  await user32.ClearControlChildren('rows');\n  const rows=Array.isArray(config[section])?config[section]:[];\n  let shown=0;\n  for(let i=0;i<rows.length;i++){\n    const r=rows[i]||{},hay=(String(r.name||'')+' '+String(r.value??'')+' '+String(r.note||'')).toLowerCase();\n    if(query&&!hay.includes(query))continue;\n    const base='r'+i+'_';\n    await user32.CreateControl(hwnd,{type:'label',id:base+'k',parentId:'rows',text:String(r.name||''),style:{margin:'0',padding:'10px 12px',fontWeight:'600',background:'#f8fafc',borderBottom:'1px solid #eef2f7'}});\n    await user32.CreateControl(hwnd,{type:'div',id:base+'v',parentId:'rows',text:String(r.value??'')+(r.note?'\\n'+String(r.note):''),style:{margin:'0',padding:'10px 12px',whiteSpace:'pre-wrap',wordBreak:'break-word',borderBottom:'1px solid #eef2f7',color:'#334155'}});\n    shown++;\n  }\n  if(!shown){\n    await user32.CreateControl(hwnd,{type:'div',id:'empty',parentId:'rows',text:query?'沒有符合搜尋條件的設定。':'這個分類目前沒有資料。',style:{gridColumn:'1 / -1',padding:'28px',textAlign:'center',color:'#64748b'}});\n  }\n  await comctl32.StatusBar_SetText('status',0,`${labels[section]||section} ｜ ${shown} 項設定`);\n};\nconst load=async()=>{\n  await comctl32.StatusBar_SetText('status',0,'正在讀取系統設定…');\n  try{\n    const out=await ExOS.QuerySystemConfig();\n    config=out&&out.config?out.config:{};\n    await render();\n  }catch(e){\n    config={};\n    await user32.ClearControlChildren('rows');\n    await user32.CreateControl(hwnd,{type:'div',id:'loaderr',parentId:'rows',text:'系統設定讀取失敗：'+String(e&&e.message?e.message:e),style:{gridColumn:'1 / -1',padding:'28px',color:'#b91c1c'}});\n    await comctl32.StatusBar_SetText('status',0,'讀取失敗');\n  }\n};\nfor(const id of ['general','php','exfs','environment'])user32.OnControl('nav_'+id,async e=>{if(e.action==='click'){section=id;await render();}});\nfor(const id of ['accounts','devmgmt','eventvwr','diskmgmt','volume3d','trash','regedit','taskmgr','security'])user32.OnControl('tool_'+id,async e=>{if(e.action==='click')await ExOS.LaunchSystemApp(id);});\nuser32.OnControl('refresh',async e=>{if(e.action==='click')await load();});\nuser32.OnControl('search',async e=>{if(e.action==='input'){query=String(e.value||'').trim().toLowerCase();await render();}if(e.action==='keydown'&&e.key==='Escape'){await user32.SetControlText('search','');query='';await render();}});\nawait load();\n"},"trash":{"id":"trash","fileName":"recyclebin.xsh","title":"資源回收桶","description":"ExOS Recycle Bin (.xsh / restore / permanent delete / search)","icon":"trash","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst shell32=ExOS.LoadLibrary('shell32.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.WC_LISTVIEW,comctl32.TOOLBARCLASSNAME,comctl32.STATUSCLASSNAME]});\nconst hwnd=await user32.CreateWindow({title:'資源回收桶',icon:'trash',width:1040,height:700});\nawait uxtheme.SetWindowTheme(hwnd,'Explorer','RecycleBin');\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',background:'#fff'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'head',parentId:'root',style:{display:'flex',alignItems:'center',gap:'8px',padding:'8px',background:'#f8fafc',borderBottom:'1px solid #dbe3ec'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'title',parentId:'head',text:'資源回收桶',style:{fontSize:'20px',fontWeight:'700',margin:'0 10px 0 2px'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'search',parentId:'head',placeholder:'搜尋已刪除的項目',spellcheck:false,style:{flex:'1',width:'auto',margin:'0'}});\nawait comctl32.CreateToolbar(hwnd,{id:'toolbar',parentId:'root',buttons:[\n  {id:'restore',text:'還原',icon:'history',enabled:false},{id:'delete',text:'永久刪除',icon:'delete',enabled:false},{separator:true},\n  {id:'empty',text:'清空資源回收桶',icon:'trash'},{separator:true},{id:'refresh',text:'重新整理',icon:'refresh'}\n],style:{width:'100%',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'hint',parentId:'root',text:'一般刪除只會移到資源回收桶；永久刪除與清空才會移除 metadata、版本與加密資料。',style:{padding:'7px 10px',fontSize:'12px',color:'#64748b',background:'#fffbea',borderBottom:'1px solid #fde68a'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'listHost',parentId:'root',style:{flex:'1',minHeight:'0',margin:'0'}});\nawait comctl32.CreateListView(hwnd,{id:'list',parentId:'listHost',view:comctl32.LV_VIEW_DETAILS,columns:[\n  {id:'name',text:'名稱',width:250,minWidth:120},{id:'original',text:'原始位置',width:310,minWidth:150},\n  {id:'deleted',text:'刪除時間',width:180,minWidth:120},{id:'type',text:'類型',width:110,minWidth:80},{id:'count',text:'內容數',width:90,minWidth:70,align:'right',sortType:'number'}\n],items:[],style:{width:'100%',height:'100%',border:'0'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'status',parentId:'root',parts:[{text:'正在讀取…',width:0},{text:'0 個項目',width:130}],style:{width:'100%',margin:'0'}});\nlet all=[],selected=[],query='';\nconst fmtDate=s=>{const d=new Date(s);return Number.isNaN(d.getTime())?String(s||''):d.toLocaleString();};\nconst filtered=()=>all.filter(x=>!query||(String(x.name||'')+' '+String(x.originalPath||'')+' '+String(x.type||'')).toLowerCase().includes(query));\nconst render=async()=>{\n  const rows=filtered().map(x=>({id:String(x.id),text:String(x.name||('#'+x.id)),subItems:[String(x.originalPath||''),fmtDate(x.trashedAt),x.type==='folder'?'資料夾':'檔案',String(x.itemCount||1)],icon:x.type==='folder'?'folder':'file',data:x,sortValues:[String(x.name||''),String(x.originalPath||''),String(x.trashedAt||''),String(x.type||''),Number(x.itemCount)||1]}));\n  await comctl32.ListView_SetItems('list',rows);\n  selected=[];\n  await comctl32.Toolbar_EnableButton('toolbar','restore',false);\n  await comctl32.Toolbar_EnableButton('toolbar','delete',false);\n  await comctl32.Toolbar_EnableButton('toolbar','empty',all.length>0);\n  await comctl32.StatusBar_SetText('status',0,query?`搜尋結果 ${rows.length} / ${all.length}`:(all.length?'選取項目以還原或永久刪除。':'資源回收桶是空的。'));\n  await comctl32.StatusBar_SetText('status',1,`${all.length} 個項目`);\n};\nconst load=async()=>{await comctl32.StatusBar_SetText('status',0,'正在讀取資源回收桶…');all=await shell32.SHQueryRecycleBin();await render();};\nconst selectedIds=()=>selected.map(x=>Number(x.id)).filter(Boolean);\nconst restore=async()=>{const ids=selectedIds();if(!ids.length)return;if(!await user32.ConfirmBox(`確定還原 ${ids.length} 個項目嗎？\\n會優先回原資料夾；原資料夾不存在時回到 C:\\\\。`,'資源回收桶'))return;for(const id of ids)await shell32.SHRestoreFromRecycleBin(id);await load();};\nconst remove=async()=>{const ids=selectedIds();if(!ids.length)return;if(!await user32.ConfirmBox(`確定永久刪除 ${ids.length} 個項目嗎？\\n此操作無法復原。`,'資源回收桶'))return;for(const id of ids)await shell32.SHDeleteFromRecycleBin(id);await load();};\nconst empty=async()=>{if(!all.length)return;if(!await user32.ConfirmBox(`確定永久清空資源回收桶嗎？\\n目前共有 ${all.length} 個根項目。`,'資源回收桶'))return;if(!await user32.ConfirmBox('最後確認：清空後無法還原。','資源回收桶'))return;await shell32.SHEmptyRecycleBin();await load();};\ncomctl32.OnNotify('list',async e=>{\n  if(e.code==='LVN_ITEMCHANGED'){selected=Array.isArray(e.data&&e.data.items)?e.data.items.map(x=>x.data||x):[];const on=selected.length>0;await comctl32.Toolbar_EnableButton('toolbar','restore',on);await comctl32.Toolbar_EnableButton('toolbar','delete',on);await comctl32.StatusBar_SetText('status',0,on?`已選取 ${selected.length} 個項目`:(all.length?'選取項目以還原或永久刪除。':'資源回收桶是空的。'));}\n  if(e.code==='LVN_ITEMACTIVATE'&&selected.length)await restore();\n});\ncomctl32.OnNotify('toolbar',async e=>{if(e.code!=='TBN_CLICK')return;const id=e.data&&e.data.button&&e.data.button.id;if(id==='restore')await restore();else if(id==='delete')await remove();else if(id==='empty')await empty();else if(id==='refresh')await load();});\nuser32.OnControl('search',async e=>{if(e.action==='input'){query=String(e.value||'').trim().toLowerCase();await render();}if(e.action==='keydown'&&e.key==='Escape'){query='';await user32.SetControlText('search','');await render();}});\nawait load();\n"},"volume3d":{"id":"volume3d","fileName":"volume3d.xsh","title":"ExFS 3D 拓譜","description":"ExOS 3D Topology (.xsh / USER32 topology3d host control)","icon":"disk","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.TOOLBARCLASSNAME,comctl32.STATUSCLASSNAME]});\nconst hwnd=await user32.CreateWindow({title:'ExFS 3D 拓譜',icon:'disk',width:1180,height:780});\nawait uxtheme.SetWindowTheme(hwnd,'Explorer','Topology');\nawait dwmapi.DwmSetWindowAttribute(hwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',background:'#0b1220'}});\nawait comctl32.CreateToolbar(hwnd,{id:'toolbar',parentId:'root',buttons:[\n  {id:'physical',text:'實體拓譜',checkable:true,checked:true,group:'mode'},{id:'sandbox',text:'沙盒拓譜',checkable:true,group:'mode'},\n  {separator:true},{id:'refresh',text:'重新整理',icon:'refresh'},{id:'reset',text:'重設視角'},{separator:true},{id:'open',text:'開啟選取位置',icon:'explorer',enabled:false}\n],style:{width:'100%',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'workspace',parentId:'root',style:{display:'grid',gridTemplateColumns:'minmax(0,1fr) 290px',flex:'1',minHeight:'0',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'topology3d',id:'graph',parentId:'workspace',topologyMode:'physical',style:{width:'100%',height:'100%',minHeight:'0',margin:'0',background:'#07101e'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'details',parentId:'workspace',style:{display:'flex',flexDirection:'column',minWidth:'0',padding:'14px',overflow:'auto',borderLeft:'1px solid #26354b',background:'#0f172a',color:'#e2e8f0'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'detailTitle',parentId:'details',text:'ExFS 拓譜',style:{fontSize:'17px',fontWeight:'700',margin:'0 0 10px',color:'#fff'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'detailBody',parentId:'details',text:'拖曳旋轉、滾輪縮放。\\n點選節點可查看詳細資料。',style:{whiteSpace:'pre-wrap',fontFamily:'Consolas,\"Microsoft JhengHei\",monospace',fontSize:'12px',lineHeight:'1.6',color:'#cbd5e1'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'status',parentId:'root',parts:[{text:'正在初始化 Three.js…',width:0},{text:'實體拓譜',width:120}],style:{width:'100%',margin:'0'}});\nlet mode='physical',selected=null;\nconst setMode=async m=>{mode=m;selected=null;await user32.SetControlProperty('graph','topologyMode',m);await comctl32.Toolbar_EnableButton('toolbar','open',false);await user32.SetControlText('detailTitle',m==='physical'?'ExFS 實體拓譜':'使用者沙盒拓譜');await user32.SetControlText('detailBody','拖曳旋轉、滾輪縮放。\\n點選節點可查看詳細資料。');await comctl32.StatusBar_SetText('status',1,m==='physical'?'實體拓譜':'沙盒拓譜');};\ncomctl32.OnNotify('toolbar',async e=>{if(e.code!=='TBN_CLICK')return;const id=e.data&&e.data.button&&e.data.button.id;if(id==='physical')await setMode('physical');else if(id==='sandbox')await setMode('sandbox');else if(id==='refresh'){await user32.SetControlProperty('graph','refreshTopology',true);await comctl32.StatusBar_SetText('status',0,'拓譜已重新整理');}else if(id==='reset')await user32.SetControlProperty('graph','resetView',true);else if(id==='open'&&selected&&selected.path)await ExOS.OpenPath(selected.path);});\nuser32.OnControl('graph',async e=>{\n  if(e.action==='topologyready'){await comctl32.StatusBar_SetText('status',0,`Three.js READY ｜ ${e.nodeCount||0} 節點`);return;}\n  if(e.action==='topologyerror'){await comctl32.StatusBar_SetText('status',0,'3D 初始化失敗：'+String(e.message||''));return;}\n  if(e.action==='topologynode'){\n    selected=e.data||null;const d=selected||{};await user32.SetControlText('detailTitle',String(d.label||d.name||'節點'));await user32.SetControlText('detailBody',[`類型：${d.kind||d.type||''}`,d.path?`路徑：${d.path}`:'',d.detail?`說明：${d.detail}`:'',d.size?`大小：${d.size}`:''].filter(Boolean).join('\\n'));await comctl32.Toolbar_EnableButton('toolbar','open',!!d.path);await comctl32.StatusBar_SetText('status',0,'已選取：'+String(d.label||d.name||'節點'));\n  }\n});\n"},"regedit":{"id":"regedit","fileName":"regedit.xsh","title":"登錄編輯程式","description":"ExOS Registry Editor (.xsh / advapi32.dll)","icon":"regedit","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\nIcon=res://shell32.dll/regedit\n*/\nconst advapi32=ExOS.LoadLibrary('advapi32.dll');\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.WC_TREEVIEW,comctl32.WC_LISTVIEW,comctl32.TOOLBARCLASSNAME,comctl32.STATUSCLASSNAME]});\n\nconst ROOTS={\n  HKEY_CLASSES_ROOT:advapi32.HKEY_CLASSES_ROOT,\n  HKEY_CURRENT_USER:advapi32.HKEY_CURRENT_USER,\n  HKEY_LOCAL_MACHINE:advapi32.HKEY_LOCAL_MACHINE\n};\nconst SHORT={HKCR:'HKEY_CLASSES_ROOT',HKCU:'HKEY_CURRENT_USER',HKLM:'HKEY_LOCAL_MACHINE'};\nconst normalizeTarget=value=>{\n  let s=String(value||'HKEY_CURRENT_USER').trim().replace(/\\//g,'\\\\').replace(/\\\\+/g,'\\\\').replace(/\\\\+$/,'');\n  const m=/^(HKCR|HKCU|HKLM)(?:\\\\(.*))?$/i.exec(s);\n  if(m)s=SHORT[m[1].toUpperCase()]+(m[2]?'\\\\'+m[2]:'');\n  const n=/^(HKEY_CLASSES_ROOT|HKEY_CURRENT_USER|HKEY_LOCAL_MACHINE)(?:\\\\(.*))?$/i.exec(s);\n  if(!n)throw new Error('無效的 Registry 路徑。');\n  return n[1].toUpperCase()+(n[2]?'\\\\'+n[2]:'');\n};\nconst splitTarget=value=>{\n  const s=normalizeTarget(value),i=s.indexOf('\\\\');\n  return i<0?{root:s,sub:''}:{root:s.substring(0,i),sub:s.substring(i+1)};\n};\nconst targetId=t=>'reg:'+normalizeTarget(t);\nconst parentTarget=t=>{const p=splitTarget(t);if(!p.sub)return'';const i=p.sub.lastIndexOf('\\\\');return p.root+(i<0?'':'\\\\'+p.sub.substring(0,i));};\nconst leafTarget=t=>{const p=splitTarget(t);if(!p.sub)return p.root;const i=p.sub.lastIndexOf('\\\\');return i<0?p.sub:p.sub.substring(i+1);};\nconst joinTarget=(t,name)=>normalizeTarget(t)+'\\\\'+String(name||'').replace(/[\\\\/]+/g,'');\nconst rootHandle=name=>ROOTS[String(name||'').toUpperCase()];\nconst withKey=async(target,write,fn)=>{\n  const p=splitTarget(target),root=rootHandle(p.root);let h=root;\n  if(p.sub)h=await advapi32.RegOpenKeyEx(root,p.sub,write?advapi32.KEY_ALL_ACCESS:advapi32.KEY_READ);\n  try{return await fn(h);}finally{if(p.sub)try{await advapi32.RegCloseKey(h);}catch(ignoreClose){}}\n};\nconst enumKeys=target=>withKey(target,false,async h=>{const out=[];for(let i=0;i<4096;i++){const k=await advapi32.RegEnumKeyEx(h,i);if(!k)break;out.push(k);}return out;});\nconst enumValues=target=>withKey(target,false,async h=>{const out=[];for(let i=0;i<8192;i++){const v=await advapi32.RegEnumValue(h,i);if(!v)break;out.push(v);}return out;});\nconst dataText=v=>{if(!v)return'';if(v.type==='REG_BINARY')return Array.isArray(v.data)?v.data.map(x=>Number(x).toString(16).padStart(2,'0').toUpperCase()).join(' '):String(v.data||'');if(v.type==='REG_MULTI_SZ')return Array.isArray(v.data)?v.data.join('  |  '):String(v.data||'');return String(v.data===undefined?'':v.data);};\n\nconst hwnd=await user32.CreateWindow({title:'登錄編輯程式',icon:'regedit',width:1120,height:760});\nawait uxtheme.SetWindowTheme(hwnd,'Explorer','RegistryEditor');\nawait dwmapi.DwmSetWindowAttribute(hwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',background:'#fff'}});\nawait comctl32.CreateToolbar(hwnd,{id:'toolbar',parentId:'root',buttons:[\n  {id:'refresh',text:'重新整理',icon:'refresh'},\n  {separator:true},{id:'newkey',text:'新增機碼',icon:'folder'},\n  {id:'newstr',text:'新增字串值',icon:'txt'},\n  {id:'newdword',text:'新增 DWORD',icon:'file'},\n  {separator:true},{id:'edit',text:'修改',icon:'edit',enabled:false},{id:'delete',text:'刪除',icon:'delete',enabled:false}\n],style:{width:'100%',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'addressRow',parentId:'root',style:{display:'flex',gap:'6px',alignItems:'center',padding:'6px 8px',margin:'0',background:'#f8fafc',borderBottom:'1px solid #dbe3ec'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'addressLabel',parentId:'addressRow',text:'位置',style:{margin:'0',fontWeight:'600'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'address',parentId:'addressRow',text:'HKEY_CURRENT_USER',spellcheck:false,style:{flex:'1',width:'auto',margin:'0',fontFamily:'Consolas,monospace'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'workspace',parentId:'root',style:{display:'grid',gridTemplateColumns:'360px minmax(0,1fr)',flex:'1',minHeight:'0',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'treeHost',parentId:'workspace',style:{minWidth:'0',minHeight:'0',borderRight:'1px solid #dbe3ec',background:'#f8fafc'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'listHost',parentId:'workspace',style:{minWidth:'0',minHeight:'0',background:'#fff'}});\nawait comctl32.CreateTreeView(hwnd,{id:'tree',parentId:'treeHost',items:[\n  {id:targetId('HKEY_CLASSES_ROOT'),text:'HKEY_CLASSES_ROOT',icon:'regedit',data:{target:'HKEY_CLASSES_ROOT'},hasChildren:true},\n  {id:targetId('HKEY_CURRENT_USER'),text:'HKEY_CURRENT_USER',icon:'regedit',data:{target:'HKEY_CURRENT_USER'},hasChildren:true,expanded:true},\n  {id:targetId('HKEY_LOCAL_MACHINE'),text:'HKEY_LOCAL_MACHINE',icon:'regedit',data:{target:'HKEY_LOCAL_MACHINE'},hasChildren:true}\n],style:{width:'100%',height:'100%',border:'0'}});\nawait comctl32.CreateListView(hwnd,{id:'values',parentId:'listHost',view:comctl32.LV_VIEW_DETAILS,columns:[\n  {id:'name',text:'名稱',width:240,minWidth:120},{id:'type',text:'類型',width:150,minWidth:100},{id:'data',text:'資料',width:430,minWidth:160}\n],items:[],style:{width:'100%',height:'100%',border:'0'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'status',parentId:'root',parts:[{text:'就緒',width:0},{text:'EXOS_ADVAPI32_V2',width:180}],style:{width:'100%',margin:'0'}});\n\nlet current='HKEY_CURRENT_USER',selectedValue=null,loaded=new Set();\nconst setStatus=t=>comctl32.StatusBar_SetText('status',0,String(t||''));\nconst loadChildren=async target=>{\n  target=normalizeTarget(target);if(loaded.has(target))return;\n  const keys=await enumKeys(target);\n  const rows=keys.map(k=>({id:targetId(joinTarget(target,k.name)),text:k.name,icon:'folder',data:{target:joinTarget(target,k.name)},hasChildren:true}));\n  await comctl32.TreeView_SetChildren('tree',targetId(target),rows);loaded.add(target);\n};\nconst loadValues=async target=>{\n  current=normalizeTarget(target);selectedValue=null;\n  await user32.SetControlText('address',current);\n  await user32.SetWindowText(hwnd,'登錄編輯程式 — '+current);\n  await comctl32.Toolbar_EnableButton('toolbar','edit',false);await comctl32.Toolbar_EnableButton('toolbar','delete',false);\n  await setStatus('正在讀取 '+current+' ...');\n  const values=await enumValues(current);\n  const rows=values.map((v,i)=>({id:'v:'+i+':'+v.name,text:v.name===''?'(預設)':v.name,subItems:[v.type,dataText(v)],icon:'file',data:v,sortValues:[v.name,v.type,dataText(v)]}));\n  await comctl32.ListView_SetItems('values',rows);\n  await setStatus(current+' ｜ '+rows.length+' 個值');\n};\nconst navigate=async target=>{target=normalizeTarget(target);await loadValues(target);try{await comctl32.TreeView_SelectItem('tree',targetId(target));}catch(ignoreSelect){}};\nconst parseValueInput=(type,text)=>{if(type==='REG_DWORD'){const n=/^0x/i.test(text)?parseInt(text,16):parseInt(text,10);if(!Number.isFinite(n))throw new Error('DWORD 必須是數字。');return n>>>0;}if(type==='REG_QWORD')return String(text||'0');if(type==='REG_MULTI_SZ')return String(text||'').split(/\\r?\\n/);if(type==='REG_BINARY'){const clean=String(text||'').replace(/[^0-9a-f]/gi,'');if(clean.length%2)throw new Error('REG_BINARY 必須是偶數個十六進位字元。');const a=[];for(let i=0;i<clean.length;i+=2)a.push(parseInt(clean.substr(i,2),16));return a;}return String(text||'');};\nconst editValue=async()=>{if(!selectedValue)return;const v=selectedValue;const shown=v.type==='REG_MULTI_SZ'&&Array.isArray(v.data)?v.data.join('\\n'):dataText(v);const input=await user32.PromptBox('請輸入新的資料：\\n類型：'+v.type,'修改值 — '+(v.name||'(預設)'),shown);if(input===null)return;const data=parseValueInput(v.type,input);await withKey(current,true,h=>advapi32.RegSetValueEx(h,v.name,v.type,data));await loadValues(current);};\nconst newValue=async type=>{const name=await user32.PromptBox('值名稱（留空代表預設值）：','新增 '+type,'');if(name===null)return;const input=await user32.PromptBox('資料：','新增 '+type,type==='REG_DWORD'?'0':'');if(input===null)return;await withKey(current,true,h=>advapi32.RegSetValueEx(h,String(name),type,parseValueInput(type,input)));await loadValues(current);};\nconst newKey=async()=>{const name=await user32.PromptBox('新機碼名稱：','新增機碼','新機碼 #1');if(name===null)return;const clean=String(name).trim();if(!clean||/[\\\\/]/.test(clean))throw new Error('機碼名稱無效。');await withKey(current,true,async h=>{const o=await advapi32.RegCreateKeyEx(h,clean,advapi32.KEY_ALL_ACCESS);if(o&&o.hKey)await advapi32.RegCloseKey(o.hKey);});loaded.delete(current);await loadChildren(current);};\nconst deleteSelected=async()=>{if(selectedValue){if(!await user32.ConfirmBox('確定刪除值「'+(selectedValue.name||'(預設)')+'」嗎？','登錄編輯程式'))return;await withKey(current,true,h=>advapi32.RegDeleteValue(h,selectedValue.name));await loadValues(current);return;}const p=splitTarget(current);if(!p.sub){await user32.MessageBox('無法刪除預先定義的 Registry 根機碼。','登錄編輯程式');return;}if(!await user32.ConfirmBox('確定刪除機碼與其內容嗎？\\n\\n'+current,'登錄編輯程式'))return;const parent=parentTarget(current),leaf=leafTarget(current);await withKey(parent,true,h=>advapi32.RegDeleteKey(h,leaf));loaded.delete(parent);current=parent;await loadChildren(parent);await navigate(parent);};\n\ncomctl32.OnNotify('tree',async e=>{if(e.action!=='notify')return;const item=e.data&&e.data.item;if(!item||!item.data||!item.data.target)return;if(e.code==='TVN_ITEMEXPANDING'&&e.data.expanding){await loadChildren(item.data.target);return;}if(e.code==='TVN_SELCHANGED'){await loadValues(item.data.target);return;}});\ncomctl32.OnNotify('values',async e=>{if(e.action!=='notify')return;if(e.code==='LVN_ITEMCHANGED'){const rows=Array.isArray(e.data&&e.data.items)?e.data.items:[];selectedValue=rows.length?(rows[0].data||null):null;await comctl32.Toolbar_EnableButton('toolbar','edit',!!selectedValue);await comctl32.Toolbar_EnableButton('toolbar','delete',!!selectedValue);}if(e.code==='LVN_ITEMACTIVATE'&&selectedValue)await editValue();});\ncomctl32.OnNotify('toolbar',async e=>{if(e.code!=='TBN_CLICK')return;const id=e.data&&e.data.button&&e.data.button.id;if(id==='refresh'){loaded.clear();await loadChildren(current);await loadValues(current);}else if(id==='newkey')await newKey();else if(id==='newstr')await newValue('REG_SZ');else if(id==='newdword')await newValue('REG_DWORD');else if(id==='edit')await editValue();else if(id==='delete')await deleteSelected();});\nuser32.OnControl('address',async e=>{if(e.action==='keydown'&&e.key==='Enter'){try{await navigate(String(e.value||''));}catch(err){await user32.MessageBox(String(err&&err.message||err),'登錄編輯程式');await user32.SetControlText('address',current);}}});\n\nawait loadChildren('HKEY_CURRENT_USER');\nawait navigate('HKEY_CURRENT_USER');\n"},"accounts":{"id":"accounts","fileName":"accounts.xsh","title":"帳號","description":"ExOS Accounts (.xsh / local users and administrators)","icon":"user","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\nIcon=res://shell32.dll/user\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.WC_LISTVIEW,comctl32.STATUSCLASSNAME,comctl32.TOOLBARCLASSNAME]});\nconst hwnd=await user32.CreateWindow({title:'帳號',icon:'user',width:1060,height:720});\nawait uxtheme.SetWindowTheme(hwnd,'ControlPanel','Accounts');\nawait dwmapi.DwmSetWindowAttribute(hwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',background:'#f5f7fa'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'head',parentId:'root',style:{display:'flex',alignItems:'center',gap:'12px',padding:'16px 18px',background:'#fff',borderBottom:'1px solid #dbe3ec'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'title',parentId:'head',text:'帳號',style:{fontSize:'24px',fontWeight:'700',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'sub',parentId:'head',text:'管理本機使用者、帳號類型與登入密碼',style:{fontSize:'12px',color:'#64748b',margin:'4px 0 0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'body',parentId:'root',style:{display:'grid',gridTemplateColumns:'minmax(420px,1.25fr) minmax(330px,.75fr)',flex:'1',minHeight:'0',gap:'12px',padding:'12px'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'left',parentId:'body',style:{display:'flex',flexDirection:'column',minHeight:'0',background:'#fff',border:'1px solid #dbe3ec',borderRadius:'8px',overflow:'hidden'}});\nawait comctl32.CreateToolbar(hwnd,{id:'toolbar',parentId:'left',buttons:[{id:'refresh',text:'重新整理',icon:'refresh'},{id:'add',text:'新增帳號',icon:'setup_user'}],style:{width:'100%',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'listHost',parentId:'left',style:{flex:'1',minHeight:'0',margin:'0'}});\nawait comctl32.CreateListView(hwnd,{id:'accounts',parentId:'listHost',view:comctl32.LV_VIEW_DETAILS,columns:[\n  {id:'user',text:'帳號',width:185,minWidth:130},{id:'type',text:'類型',width:150,minWidth:120},{id:'status',text:'狀態',width:90,minWidth:70},{id:'last',text:'上次登入',width:205,minWidth:130}\n],items:[],style:{width:'100%',height:'100%',border:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'right',parentId:'body',style:{display:'flex',flexDirection:'column',gap:'10px',minHeight:'0',overflow:'auto',background:'#fff',border:'1px solid #dbe3ec',borderRadius:'8px',padding:'16px'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'detailTitle',parentId:'right',text:'請選擇帳號',style:{fontSize:'20px',fontWeight:'700',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'pre',id:'detail',parentId:'right',text:'從左側選擇本機帳號。',style:{margin:'0',padding:'10px',whiteSpace:'pre-wrap',fontFamily:'Segoe UI,Microsoft JhengHei,sans-serif',fontSize:'12px',lineHeight:'1.6',background:'#f8fafc',border:'1px solid #e5e7eb',borderRadius:'6px'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'actions',parentId:'right',style:{display:'grid',gridTemplateColumns:'1fr 1fr',gap:'8px',margin:'0'}});\nfor(const [id,text] of [['toggle','啟用 / 停用'],['type','變更帳號類型'],['reset','重設密碼'],['delete','刪除帳號']])await user32.CreateControl(hwnd,{type:'button',id:'act_'+id,parentId:'actions',text,style:{margin:'0',minHeight:'38px'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'addPanel',parentId:'right',style:{display:'none',flexDirection:'column',gap:'8px',padding:'12px',border:'1px solid #cbd5e1',borderRadius:'7px',background:'#f8fafc'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'addTitle',parentId:'addPanel',text:'新增本機帳號',style:{fontSize:'16px',fontWeight:'700',margin:'0 0 4px'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'newUser',parentId:'addPanel',placeholder:'帳號名稱，例如 operator',spellcheck:false,style:{width:'100%',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'newPass',parentId:'addPanel',inputType:'password',placeholder:'密碼（至少 8 個字元）',style:{width:'100%',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'newPass2',parentId:'addPanel',inputType:'password',placeholder:'再次輸入密碼',style:{width:'100%',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'newAdmin',parentId:'addPanel',text:'帳號類型：標準使用者',style:{margin:'0',textAlign:'left'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'addButtons',parentId:'addPanel',style:{display:'flex',gap:'8px',justifyContent:'flex-end',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'addCancel',parentId:'addButtons',text:'取消',style:{margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'addCreate',parentId:'addButtons',text:'建立帳號',style:{margin:'0',fontWeight:'600'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'resetPanel',parentId:'right',style:{display:'none',flexDirection:'column',gap:'8px',padding:'12px',border:'1px solid #cbd5e1',borderRadius:'7px',background:'#fff7ed'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'resetTitle',parentId:'resetPanel',text:'重設密碼',style:{fontSize:'16px',fontWeight:'700',margin:'0 0 4px'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'resetPass',parentId:'resetPanel',inputType:'password',placeholder:'新密碼（至少 8 個字元）',style:{width:'100%',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'resetPass2',parentId:'resetPanel',inputType:'password',placeholder:'再次輸入新密碼',style:{width:'100%',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'resetButtons',parentId:'resetPanel',style:{display:'flex',gap:'8px',justifyContent:'flex-end',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'resetCancel',parentId:'resetButtons',text:'取消',style:{margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'resetApply',parentId:'resetButtons',text:'套用新密碼',style:{margin:'0',fontWeight:'600'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'status',parentId:'root',parts:[{text:'正在讀取帳號…',width:0},{text:'WINDOWS_NT_SAM_V3',width:190}],style:{width:'100%',margin:'0'}});\n\nlet rows=[],selected=null,currentUsername='',newIsAdmin=false;\nconst status=t=>comctl32.StatusBar_SetText('status',0,String(t||''));\nconst accountType=a=>a&&a.administrator?'系統管理員':'標準使用者';\nconst setPanel=async(id,visible)=>user32.SetControlStyle(id,{display:visible?'flex':'none'});\nconst updateDetail=async()=>{\n  if(!selected){await user32.SetControlText('detailTitle','請選擇帳號');await user32.SetControlText('detail','從左側選擇本機帳號。');return;}\n  await user32.SetControlText('detailTitle',selected.username+(selected.username===currentUsername?'（目前登入）':''));\n  await user32.SetControlText('detail',[\n    '帳號類型：'+accountType(selected),\n    '狀態：'+(selected.enabled?'已啟用':'已停用'),\n    'SID：'+selected.sid,\n    'RID：'+selected.rid,\n    '設定檔：'+(selected.profile_path||''),\n    '建立時間：'+(selected.created_at||'—'),\n    '密碼變更：'+(selected.password_changed_at||'—'),\n    '上次登入：'+(selected.last_logon_at||'—'),\n    '登入次數：'+String(selected.logon_count||0)\n  ].join('\\n'));\n};\nconst load=async()=>{\n  await status('正在讀取本機 SAM 帳號…');\n  try{\n    const out=await ExOS.QueryLocalAccounts();rows=Array.isArray(out.accounts)?out.accounts:[];currentUsername=String(out.current_username||'');\n    await comctl32.ListView_SetItems('accounts',rows.map((a,i)=>({id:'acct:'+a.username,text:a.username,subItems:[accountType(a),a.enabled?'已啟用':'已停用',a.last_logon_at||'—'],icon:'user',data:a,sortValues:[a.username,accountType(a),a.enabled?1:0,a.last_logon_at||'']})));\n    if(selected){selected=rows.find(x=>x.username===selected.username)||null;await updateDetail();}\n    await status('本機帳號 '+rows.length+' 個');\n  }catch(e){rows=[];selected=null;await comctl32.ListView_SetItems('accounts',[]);await user32.SetControlText('detailTitle','無法讀取帳號');await user32.SetControlText('detail',String(e&&e.message||e));await status('存取遭拒或讀取失敗');}\n};\ncomctl32.OnNotify('accounts',async e=>{if(e.code==='LVN_ITEMCHANGED'){const a=Array.isArray(e.data&&e.data.items)?e.data.items:[];selected=a.length?(a[0].data||null):null;await updateDetail();}if(e.code==='LVN_ITEMACTIVATE'&&selected)await updateDetail();});\ncomctl32.OnNotify('toolbar',async e=>{if(e.code!=='TBN_CLICK')return;const id=e.data&&e.data.button&&e.data.button.id;if(id==='refresh')await load();if(id==='add'){newIsAdmin=false;await user32.SetControlText('newAdmin','帳號類型：標準使用者');await setPanel('resetPanel',false);await setPanel('addPanel',true);await user32.FocusControl('newUser');}});\nuser32.OnControl('newAdmin',async e=>{if(e.action==='click'){newIsAdmin=!newIsAdmin;await user32.SetControlText('newAdmin','帳號類型：'+(newIsAdmin?'系統管理員':'標準使用者'));}});\nuser32.OnControl('addCancel',async e=>{if(e.action==='click')await setPanel('addPanel',false);});\nuser32.OnControl('addCreate',async e=>{if(e.action!=='click')return;const u=String(await user32.GetControlText('newUser')||'').trim().toLowerCase(),p=String(await user32.GetControlText('newPass')||''),p2=String(await user32.GetControlText('newPass2')||'');if(!u)return user32.MessageBox('請輸入帳號名稱。','帳號');if(p.length<8)return user32.MessageBox('密碼至少需要 8 個字元。','帳號');if(p!==p2)return user32.MessageBox('兩次密碼不一致。','帳號');try{await status('正在建立 '+u+'…');await ExOS.CreateLocalAccount({username:u,password:p,administrator:newIsAdmin});await user32.SetControlText('newPass','');await user32.SetControlText('newPass2','');await setPanel('addPanel',false);await load();}catch(err){await user32.MessageBox(String(err&&err.message||err),'建立帳號失敗');}});\nuser32.OnControl('act_toggle',async e=>{if(e.action!=='click'||!selected)return;try{await ExOS.SetLocalAccountEnabled(selected.username,!selected.enabled);await load();}catch(err){await user32.MessageBox(String(err&&err.message||err),'帳號');}});\nuser32.OnControl('act_type',async e=>{if(e.action!=='click'||!selected)return;const toAdmin=!selected.administrator;if(!await user32.ConfirmBox('將「'+selected.username+'」變更為'+(toAdmin?'系統管理員':'標準使用者')+'嗎？','變更帳號類型'))return;try{await ExOS.SetLocalAccountType(selected.username,toAdmin);await load();}catch(err){await user32.MessageBox(String(err&&err.message||err),'帳號');}});\nuser32.OnControl('act_reset',async e=>{if(e.action!=='click'||!selected)return;if(selected.username===currentUsername){await user32.MessageBox('目前登入帳號請從 Ctrl+Alt+S 的 ExOS 安全性畫面使用「變更密碼」，避免目前 Session 與 SAM 狀態不同步。','帳號');return;}await user32.SetControlText('resetTitle','重設 '+selected.username+' 的密碼');await user32.SetControlText('resetPass','');await user32.SetControlText('resetPass2','');await setPanel('addPanel',false);await setPanel('resetPanel',true);await user32.FocusControl('resetPass');});\nuser32.OnControl('resetCancel',async e=>{if(e.action==='click')await setPanel('resetPanel',false);});\nuser32.OnControl('resetApply',async e=>{if(e.action!=='click'||!selected)return;const p=String(await user32.GetControlText('resetPass')||''),p2=String(await user32.GetControlText('resetPass2')||'');if(p.length<8)return user32.MessageBox('新密碼至少需要 8 個字元。','帳號');if(p!==p2)return user32.MessageBox('兩次密碼不一致。','帳號');try{await ExOS.ResetLocalAccountPassword(selected.username,p);await user32.SetControlText('resetPass','');await user32.SetControlText('resetPass2','');await setPanel('resetPanel',false);await load();await user32.MessageBox('密碼已重設。','帳號');}catch(err){await user32.MessageBox(String(err&&err.message||err),'重設密碼失敗');}});\nuser32.OnControl('act_delete',async e=>{if(e.action!=='click'||!selected)return;if(!await user32.ConfirmBox('確定刪除帳號「'+selected.username+'」及其使用者設定檔嗎？\\n\\n此動作無法復原。','刪除帳號'))return;try{await ExOS.DeleteLocalAccount(selected.username);selected=null;await load();}catch(err){await user32.MessageBox(String(err&&err.message||err),'刪除帳號失敗');}});\nawait load();\n"},"eventvwr":{"id":"eventvwr","fileName":"eventvwr.xsh","title":"事件檢視器","description":"ExOS Event Viewer (.xsh / structured EXFS_EVTX1 event log)","icon":"history","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\nIcon=res://shell32.dll/history\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.WC_LISTVIEW,comctl32.STATUSCLASSNAME,comctl32.TOOLBARCLASSNAME]});\nconst hwnd=await user32.CreateWindow({title:'事件檢視器',icon:'history',width:1180,height:760});\nawait uxtheme.SetWindowTheme(hwnd,'Explorer','EventViewer');\nawait dwmapi.DwmSetWindowAttribute(hwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',background:'#fff'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'head',parentId:'root',style:{display:'flex',alignItems:'center',gap:'10px',padding:'10px 12px',background:'#f8fafc',borderBottom:'1px solid #dbe3ec'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'title',parentId:'head',text:'事件檢視器',style:{fontSize:'21px',fontWeight:'700',margin:'0 12px 0 0'}});\nawait user32.CreateControl(hwnd,{type:'input',id:'search',parentId:'head',placeholder:'搜尋 Event ID、事件名稱、使用者或內容',spellcheck:false,style:{flex:'1',width:'auto',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'level',parentId:'head',text:'層級：全部',style:{margin:'0',minWidth:'105px'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'refresh',parentId:'head',text:'重新整理',style:{margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'body',parentId:'root',style:{display:'grid',gridTemplateRows:'minmax(250px,1fr) 220px',flex:'1',minHeight:'0',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'listHost',parentId:'body',style:{minHeight:'0',margin:'0'}});\nawait comctl32.CreateListView(hwnd,{id:'events',parentId:'listHost',view:comctl32.LV_VIEW_DETAILS,columns:[\n {id:'time',text:'日期與時間',width:205,minWidth:145},{id:'level',text:'層級',width:105,minWidth:80},{id:'id',text:'事件 ID',width:90,minWidth:70},{id:'name',text:'來源 / 事件',width:220,minWidth:130},{id:'user',text:'使用者',width:140,minWidth:90},{id:'process',text:'處理程序',width:260,minWidth:140}\n],items:[],style:{width:'100%',height:'100%',border:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'detailPane',parentId:'body',style:{display:'flex',flexDirection:'column',minHeight:'0',padding:'10px 12px',borderTop:'1px solid #dbe3ec',background:'#f8fafc'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'detailTitle',parentId:'detailPane',text:'事件詳細資料',style:{fontSize:'15px',fontWeight:'700',margin:'0 0 6px'}});\nawait user32.CreateControl(hwnd,{type:'pre',id:'detail',parentId:'detailPane',text:'選取事件後顯示詳細資料。',style:{flex:'1',minHeight:'0',overflow:'auto',margin:'0',padding:'10px',background:'#fff',border:'1px solid #dbe3ec',borderRadius:'5px',fontFamily:'Consolas,monospace',fontSize:'12px',whiteSpace:'pre-wrap'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'status',parentId:'root',parts:[{text:'正在載入事件…',width:0},{text:'EXFS_EVTX1',width:140}],style:{width:'100%',margin:'0'}});\nlet all=[],shown=[],query='',levelMode='ALL',logInfo=null;\nconst levels=['ALL','INFORMATION','WARNING'];\nconst levelText=v=>v==='WARNING'?'警告':v==='INFORMATION'?'資訊':'全部';\nconst status=t=>comctl32.StatusBar_SetText('status',0,String(t||''));\nconst processText=r=>{const p=String(r&&r.process||'');if(!p)return'—';try{const o=JSON.parse(p);return String(o.image||o.run||p);}catch(_){return p;}};\nconst render=async()=>{\n  shown=all.filter(r=>{\n    if(levelMode!=='ALL'&&String(r.level||'').toUpperCase()!==levelMode)return false;\n    if(!query)return true;\n    const h=[r.time,r.level,r.event_id,r.event_name,r.user,r.user_sid,r.integrity,r.detail,processText(r)].join(' ').toLowerCase();return h.includes(query);\n  });\n  await comctl32.ListView_SetItems('events',shown.slice().reverse().map((r,i)=>({id:'evt:'+String(r.record_id||i),text:String(r.time||''),subItems:[levelText(String(r.level||'').toUpperCase()),String(r.event_id||''),String(r.event_name||''),String(r.user||'—'),processText(r)],icon:String(r.level||'').toUpperCase()==='WARNING'?'security':'history',data:r,sortValues:[r.time,r.level,r.event_id,r.event_name,r.user,processText(r)]})));\n  await status('顯示 '+shown.length+' / '+all.length+' 筆事件'+(logInfo?' ｜ '+String(logInfo.virtual_path||'C:\\\\logs\\\\sys.log'):'') );\n};\nconst load=async()=>{\n  await status('正在讀取 EXFS_EVTX1…');\n  try{const [q,i]=await Promise.all([ExOS.QueryEvents({limit:3000}),ExOS.QueryEventLogInfo()]);all=Array.isArray(q.events)?q.events:[];logInfo=i||null;await render();}catch(e){all=[];await comctl32.ListView_SetItems('events',[]);await user32.SetControlText('detail','事件記錄讀取失敗：\\n'+String(e&&e.message||e));await status('讀取失敗');}\n};\ncomctl32.OnNotify('events',async e=>{if(e.code!=='LVN_ITEMCHANGED')return;const a=Array.isArray(e.data&&e.data.items)?e.data.items:[],r=a.length?(a[0].data||null):null;if(!r){await user32.SetControlText('detailTitle','事件詳細資料');await user32.SetControlText('detail','選取事件後顯示詳細資料。');return;}await user32.SetControlText('detailTitle','事件 '+String(r.event_id||'')+' — '+String(r.event_name||''));await user32.SetControlText('detail',[\n  '記錄 ID：'+String(r.record_id||''),'時間：'+String(r.time||''),'層級：'+levelText(String(r.level||'').toUpperCase()),'Event ID：'+String(r.event_id||''),'事件：'+String(r.event_name||''),'使用者：'+String(r.user||'—'),'SID：'+String(r.user_sid||'—'),'Token：'+String(r.token_id||'—'),'完整性：'+String(r.integrity||'—'),'處理程序：'+processText(r),'','詳細資料：',String(r.detail||'')\n].join('\\n'));});\nuser32.OnControl('search',async e=>{if(e.action==='input'){query=String(e.value||'').trim().toLowerCase();await render();}if(e.action==='keydown'&&e.key==='Escape'){query='';await user32.SetControlText('search','');await render();}});\nuser32.OnControl('level',async e=>{if(e.action!=='click')return;const i=(levels.indexOf(levelMode)+1)%levels.length;levelMode=levels[i];await user32.SetControlText('level','層級：'+levelText(levelMode));await render();});\nuser32.OnControl('refresh',async e=>{if(e.action==='click')await load();});\nawait load();\n"},"devmgmt":{"id":"devmgmt","fileName":"devmgmt.xsh","title":"裝置管理員","description":"ExOS Device Manager (.xsh / virtual device stack)","icon":"computer","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\nconst gdi32=ExOS.LoadLibrary('gdi32.dll');\nconst d3d11=ExOS.LoadLibrary('d3d11.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.WC_TREEVIEW,comctl32.STATUSCLASSNAME,comctl32.TOOLBARCLASSNAME]});\nconst hwnd=await user32.CreateWindow({title:'裝置管理員',icon:'computer',width:1080,height:720});\nawait uxtheme.SetWindowTheme(hwnd,'DeviceManager','Explorer');\nawait dwmapi.DwmSetWindowAttribute(hwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',background:'#fff'}});\nawait comctl32.CreateToolbar(hwnd,{id:'toolbar',parentId:'root',buttons:[{id:'scan',text:'掃描硬體變更',icon:'refresh'},{id:'expand',text:'全部展開',icon:'folder'},{separator:true},{id:'general',text:'一般',checkable:true,checked:true,group:'view'},{id:'driver',text:'驅動程式',checkable:true,group:'view'},{id:'resources',text:'資源',checkable:true,group:'view'}],style:{width:'100%',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'body',parentId:'root',style:{display:'grid',gridTemplateColumns:'380px minmax(0,1fr)',flex:'1',minHeight:'0',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'treeHost',parentId:'body',style:{minHeight:'0',margin:'0',borderRight:'1px solid #dbe3ec',background:'#f8fafc'}});\nawait comctl32.CreateTreeView(hwnd,{id:'devices',parentId:'treeHost',items:[],style:{width:'100%',height:'100%',border:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'detailPane',parentId:'body',style:{display:'flex',flexDirection:'column',gap:'10px',minHeight:'0',overflow:'auto',padding:'18px',background:'#fff'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'deviceTitle',parentId:'detailPane',text:'裝置管理員',style:{fontSize:'22px',fontWeight:'700',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'deviceClass',parentId:'detailPane',text:'選取左側裝置以檢視詳細資料。',style:{fontSize:'12px',color:'#64748b',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'statusCard',parentId:'detailPane',text:'ExOS 只顯示其虛擬裝置堆疊；瀏覽器主機的 PCI / USB 硬體不會暴露給 XSH。',style:{padding:'10px 12px',border:'1px solid #bfdbfe',borderRadius:'6px',background:'#eff6ff',color:'#1e3a8a',fontSize:'12.5px'}});\nawait user32.CreateControl(hwnd,{type:'pre',id:'detail',parentId:'detailPane',text:'',style:{flex:'1',minHeight:'0',overflow:'auto',margin:'0',padding:'12px',background:'#f8fafc',border:'1px solid #e2e8f0',borderRadius:'6px',fontFamily:'Consolas,monospace',fontSize:'12px',whiteSpace:'pre-wrap'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'status',parentId:'root',parts:[{text:'正在掃描 ExOS 裝置…',width:0},{text:'Browser HAL',width:170}],style:{width:'100%',margin:'0'}});\nlet deviceMap={},treeItems=[],selectedId='',viewMode='general';\nconst jsonText=x=>{try{return JSON.stringify(x,null,2);}catch(_){return String(x||'');}};\nconst addDevice=(category,id,name,icon,info)=>{deviceMap[id]={id,name,category,...info};let c=treeItems.find(x=>x.id==='cat:'+category);if(!c){c={id:'cat:'+category,text:category,icon:'folder',expanded:true,data:{category},children:[]};treeItems.push(c);}c.children.push({id:'dev:'+id,text:name,icon:icon||'computer',data:{device:id}});};\nconst renderDevice=async id=>{const d=deviceMap[id];selectedId=id||'';if(!d){await user32.SetControlText('deviceTitle','裝置管理員');await user32.SetControlText('deviceClass','選取左側裝置以檢視詳細資料。');return;}await user32.SetControlText('deviceTitle',d.name);await user32.SetControlText('deviceClass',d.category+' ｜ '+String(d.className||'ExOS Device'));await user32.SetControlText('statusCard',d.status||'這個裝置運作正常。');let lines=[];if(viewMode==='driver')lines=['驅動程式提供者：ExOS Project','驅動程式：'+String(d.driver||'exos.sys'),'模型：'+String(d.model||'Virtual Device'),'版本：6.4.0-dev-os84','簽章：ExOS built-in trusted package'];else if(viewMode==='resources')lines=['資源 / 後端','────────────',String(d.resources||'由 ExOS Browser HAL 動態配置。'),'','主機硬體直通：否','Ring 0：否','DMA：虛擬化'];else lines=['裝置類別：'+d.category,'裝置 ID：'+d.id,'狀態：'+String(d.status||'這個裝置運作正常。'),'','詳細資料：',jsonText(d.details||{})];await user32.SetControlText('detail',lines.join('\\n'));};\nconst load=async()=>{await comctl32.StatusBar_SetText('status',0,'正在掃描 ExOS 虛擬裝置…');deviceMap={};treeItems=[];try{const halCaps=await hal.QueryCapabilities();const vmm=await kernel32.QueryVmmStatistics();const vdo=await exes.QuerySystemVdo();const backing=await exes.GetBackingStore();const stack=await io.GetDriverStack('C:\\\\');const d3d=await d3d11.GetVersion();const gdiv=await gdi32.GetVersion();const mf=await ExOS.MediaFoundation.MFStartup();let dc=0,gcaps={};try{dc=await gdi32.GetDC(hwnd);gcaps={width:await gdi32.GetDeviceCaps(dc,gdi32.HORZRES),height:await gdi32.GetDeviceCaps(dc,gdi32.VERTRES),dpiX:await gdi32.GetDeviceCaps(dc,gdi32.LOGPIXELSX),dpiY:await gdi32.GetDeviceCaps(dc,gdi32.LOGPIXELSY),bpp:await gdi32.GetDeviceCaps(dc,gdi32.BITSPIXEL)};}finally{if(dc)try{await gdi32.ReleaseDC(hwnd,dc);}catch(_){}}\naddDevice('電腦','hal','ExOS Browser HAL','computer',{className:'Computer',driver:'hal.dll',model:'EXOS_BROWSER_HAL',details:halCaps,resources:'JavaScript event loop / browser capability broker'});\naddDevice('處理器','v8cpu','V8 JavaScript 執行引擎','computer',{className:'Processor',driver:'xshhost.exe',model:'V8-only execution device',details:{engine:'V8',nativeCpuHidden:true,processArchitecture:'XSH4 / wasm32 capable'},resources:'CPU scheduling由瀏覽器與 ExOS NT Scheduler 仲介'});\naddDevice('顯示卡','d3d','ExOS D3D11 Display Adapter','computer',{className:'Display',driver:'exos_d3d11.js',model:d3d.model,details:d3d,resources:'WebGL / WebGL2；主機 GPU handle 不暴露'});\naddDevice('顯示卡','gdi','ExOS GDI32 Raster Display','image',{className:'Display',driver:'exos_gdi32.js',model:gdiv.model,details:{api:gdiv,caps:gcaps},resources:'Canvas2D-backed HDC / '+gcaps.width+'x'+gcaps.height+' @ '+gcaps.dpiX+' DPI'});\naddDevice('磁碟機','exfs','ExFS 本機磁碟 (C:)','disk',{className:'DiskDrive',driver:'exes.sys / exfs.sys',model:String(vdo&&vdo.model||'ExFS VDO'),details:vdo,resources:'C: -> PHP /_exfs/ encrypted VDO'});\naddDevice('儲存控制器','vdo','EXES Virtual Storage Controller','disk',{className:'StorageController',driver:'exes.dll',model:'EXES VDO Bridge',details:{backing,driverStack:stack},resources:String(backing&&backing.path||'/_exfs/')});\naddDevice('音效、視訊及遊戲控制器','audio','ExOS Media Foundation Audio Endpoint','media',{className:'Media',driver:'exos_media.js',model:mf.model,details:mf,resources:'Web Audio API mediated by ExOS mixer'});\naddDevice('人性化介面裝置','keyboard','XSH Keyboard Input Broker','computer',{className:'HID',driver:'user32.dll',model:'Virtual HID Keyboard',details:{sandboxed:true,rawHostHid:false},resources:'USER32 message queue / keyboard events'});\naddDevice('人性化介面裝置','pointer','XSH Pointer Input Broker','computer',{className:'HID',driver:'user32.dll',model:'Virtual HID Pointer',details:{pointerEvents:true,rawHostHid:false},resources:'USER32 pointer broker'});\naddDevice('系統裝置','vmm','ExOS Virtual Memory Manager','computer',{className:'System',driver:'ntoskrnl.js',model:'EXOS_VMM_V1',details:vmm,resources:'47-bit / 128 TiB per-process synthetic VAS; 4 KiB pages'});\naddDevice('系統裝置','iomgr','ExOS I/O Manager','computer',{className:'System',driver:'ntoskrnl.js / exes.sys',model:'IRP/VDO I/O Manager',details:{driverStack:stack},resources:'IRP -> minifilter -> ExFS VDO'});\naddDevice('系統裝置','dwm','ExOS Desktop Window Manager','computer',{className:'System',driver:'dwmapi.dll',model:'EXOS_DWMAPI_V1',details:{composition:true,hostFramebufferHidden:true},resources:'Browser compositor / DWM surface'});\naddDevice('網路介面卡','netiso','ExOS Network Isolation Broker','computer',{className:'Net',driver:'xshsandbox',model:'Network isolation device',status:'裝置運作正常。XSH 直接網路存取依安全原則停用。',details:{fetch:false,xhr:false,websocket:false},resources:'connect-src none / no raw sockets'});\nawait comctl32.TreeView_SetItems('devices',treeItems);for(const c of treeItems)try{await comctl32.TreeView_Expand('devices',c.id,comctl32.TVE_EXPAND);}catch(_){}await comctl32.StatusBar_SetText('status',0,'裝置 '+Object.keys(deviceMap).length+' 個 ｜ ExOS 虛擬硬體');if(selectedId&&deviceMap[selectedId])await renderDevice(selectedId);}catch(e){await user32.SetControlText('detail','掃描失敗：\\n'+String(e&&e.stack?e.stack:e));await comctl32.StatusBar_SetText('status',0,'掃描失敗');}};\ncomctl32.OnNotify('devices',async e=>{if(e.code!=='TVN_SELCHANGED')return;const item=e.data&&e.data.item?e.data.item:null;if(item&&item.data&&item.data.device)await renderDevice(String(item.data.device));});\ncomctl32.OnNotify('toolbar',async e=>{if(e.code!=='TBN_CLICK')return;const id=String(e.data&&e.data.button?e.data.button.id:'');if(id==='scan'){await load();return;}if(id==='expand'){for(const c of treeItems)try{await comctl32.TreeView_Expand('devices',c.id,comctl32.TVE_EXPAND);}catch(_){}return;}if(['general','driver','resources'].includes(id)){viewMode=id;for(const x of ['general','driver','resources'])await comctl32.Toolbar_CheckButton('toolbar',x,x===id);if(selectedId)await renderDevice(selectedId);}});\nawait load();\n"},"paint":{"id":"paint","fileName":"mspaint.xsh","title":"小畫家","description":"ExOS Paint (.xsh / GDI32 drawing surface)","icon":"paint","singleInstance":false,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\nIcon=res://shell32.dll/paint\n*/\nconst gdi32=ExOS.LoadLibrary('gdi32.dll');\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst comdlg32=ExOS.LoadLibrary('comdlg32.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\nawait comctl32.InitCommonControlsEx({classes:[comctl32.TOOLBARCLASSNAME,comctl32.STATUSCLASSNAME]});\nconst hwnd=await user32.CreateWindow({title:'未命名 - 小畫家',icon:'paint',width:1100,height:760});\nawait uxtheme.SetWindowTheme(hwnd,'Paint','Explorer');\nawait dwmapi.DwmSetWindowAttribute(hwnd,dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,'roundsmall');\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',background:'transparent'}});\nawait comctl32.CreateToolbar(hwnd,{id:'filebar',parentId:'root',buttons:[{id:'new',text:'新增',icon:'file'},{id:'open',text:'開啟',icon:'folder'},{id:'import',text:'匯入',icon:'upload'},{id:'save',text:'儲存',icon:'save'},{id:'saveas',text:'另存新檔',icon:'download'},{separator:true},{id:'undo',text:'復原',icon:'history'},{id:'clear',text:'清除',icon:'delete'}],style:{width:'100%',margin:'0',background:'#f8fafc'}});\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'tools',\n  parentId:'root',\n  style:{\n    display:'flex',\n    alignItems:'center',\n    gap:'6px',\n    padding:'7px 9px',\n    margin:'0',\n    background:'#eef2f7',\n    borderBottom:'1px solid #cbd5e1',\n    flexWrap:'wrap'\n  }\n});\n\nfor(const [id,text] of [\n  ['pencil','鉛筆'],\n  ['line','直線'],\n  ['rect','矩形'],\n  ['ellipse','橢圓'],\n  ['eraser','橡皮擦']\n]){\n  await user32.CreateControl(hwnd,{\n    type:'button',\n    id:'tool_'+id,\n    parentId:'tools',\n    text,\n    style:{margin:'0',minWidth:'64px'}\n  });\n}\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'widthGroup',\n  parentId:'tools',\n  style:{\n    display:'flex',\n    alignItems:'center',\n    gap:'4px',\n    margin:'0 0 0 6px'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'label',\n  id:'widthLabel',\n  parentId:'widthGroup',\n  text:'粗細',\n  style:{margin:'0 4px 0 0',fontSize:'12px'}\n});\n\nfor(const w of [1,2,4,8,14]){\n  await user32.CreateControl(hwnd,{\n    type:'button',\n    id:'w_'+w,\n    parentId:'widthGroup',\n    text:String(w),\n    style:{\n      margin:'0',\n      width:'32px',\n      minWidth:'32px',\n      padding:'0'\n    }\n  });\n}\n\nawait user32.CreateControl(hwnd,{\n  type:'div',\n  id:'colorGroup',\n  parentId:'tools',\n  style:{\n    display:'flex',\n    alignItems:'center',\n    gap:'3px',\n    margin:'0 0 0 auto',\n    padding:'2px 4px',\n    background:'#f8fafc',\n    border:'1px solid #cbd5e1',\n    borderRadius:'3px',\n    whiteSpace:'nowrap'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'label',\n  id:'colorLabel',\n  parentId:'colorGroup',\n  text:'顏色',\n  style:{\n    margin:'0 5px 0 2px',\n    fontSize:'12px'\n  }\n});\n\nconst colors=[\n  ['000000','黑'],\n  ['ffffff','白'],\n  ['ef4444','紅'],\n  ['f59e0b','橘'],\n  ['eab308','黃'],\n  ['22c55e','綠'],\n  ['06b6d4','青'],\n  ['3b82f6','藍'],\n  ['8b5cf6','紫'],\n  ['ec4899','粉紅']\n];\n\nfor(const [hex,name] of colors){\n  await user32.CreateControl(hwnd,{\n    type:'button',\n    id:'c_'+hex,\n    parentId:'colorGroup',\n    text:'',\n    title:name+' #'+hex.toUpperCase(),\n    style:{\n      margin:'0',\n      width:'23px',\n      minWidth:'23px',\n      height:'23px',\n      minHeight:'23px',\n      padding:'0',\n      background:'#'+hex,\n      border:'1px solid #64748b',\n      borderRadius:'2px'\n    }\n  });\n}\n\nawait user32.CreateControl(hwnd,{\n  type:'button',\n  id:'customColor',\n  parentId:'colorGroup',\n  text:'自訂…',\n  title:'使用 comdlg32.dll ChooseColor',\n  style:{\n    margin:'0 0 0 4px',\n    minWidth:'58px',\n    height:'25px',\n    padding:'1px 7px'\n  }\n});\nawait user32.CreateControl(hwnd,{type:'div',id:'canvasWrap',parentId:'root',style:{display:'flex',flex:'1',minHeight:'0',padding:'12px',margin:'0',background:'transparent',boxSizing:'border-box'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'canvasHost',parentId:'canvasWrap',trackPointer:true,style:{flex:'1',minWidth:'0',minHeight:'0',margin:'0',background:'transparent',border:'1px solid #64748b',boxShadow:'0 1px 4px rgba(0,0,0,.18)',cursor:'crosshair'}});\nawait comctl32.CreateStatusBar(hwnd,{id:'status',parentId:'root',parts:[{text:'鉛筆 ｜ 4 px',width:0},{text:'',width:230},{text:'GDI32',width:90}],style:{width:'100%',margin:'0'}});\nlet hdc=0,\n    tool='pencil',\n    penWidth=4,\n    color=gdi32.RGB(0,0,0),\n    drawing=false,\n    start={x:0,y:0},\n    pendingPoints=[],\n    strokeTail=null,\n    flushScheduled=false,\n    drawChain=Promise.resolve(),\n    coordScheduled=false,\n    pendingCoord='',\n    canvasRect=null,\n    currentPath='',\n    undoStack=[];\nconst rgbHex=h=>gdi32.RGB(parseInt(h.slice(0,2),16),parseInt(h.slice(2,4),16),parseInt(h.slice(4,6),16));\nconst status=async(t,part=0)=>comctl32.StatusBar_SetText('status',part,String(t||''));\nconst rectNow=async()=>{canvasRect=await user32.GetControlRect('canvasHost');return canvasRect;};\nconst waitCanvasReady=async()=>{\n  let r=null;\n  for(let i=0;i<12;i++){\n    await new Promise(resolve=>requestAnimationFrame(resolve));\n    r=await rectNow();\n    if(Number(r.width)>=32&&Number(r.height)>=32)return r;\n  }\n  return r||await rectNow();\n};\nconst absPoint=e=>({x:(canvasRect?canvasRect.left:0)+Math.max(0,Math.min(canvasRect?canvasRect.width:1,Number(e.x)||0)),y:(canvasRect?canvasRect.top:0)+Math.max(0,Math.min(canvasRect?canvasRect.height:1,Number(e.y)||0))});\nconst clearUndo=async()=>{for(const u of undoStack){try{await gdi32.DeleteDC(u.dc);}catch(_){}try{await gdi32.DeleteObject(u.bmp);}catch(_){}}undoStack=[];};\nconst captureUndo=async()=>{const r=await rectNow(),w=Math.max(1,Math.floor(r.width)),h=Math.max(1,Math.floor(r.height)),dc=await gdi32.CreateCompatibleDC(hdc),bmp=await gdi32.CreateCompatibleBitmap(hdc,w,h);await gdi32.SelectObject(dc,bmp);await gdi32.BitBlt(dc,0,0,w,h,hdc,Math.floor(r.left),Math.floor(r.top),gdi32.SRCCOPY);undoStack.push({dc,bmp,w,h});while(undoStack.length>12){const u=undoStack.shift();try{await gdi32.DeleteDC(u.dc);}catch(_){}try{await gdi32.DeleteObject(u.bmp);}catch(_){}}};\nconst paintWhite=async()=>{const r=await rectNow(),brush=await gdi32.CreateSolidBrush(gdi32.RGB(255,255,255)),oldB=await gdi32.SelectObject(hdc,brush),oldP=await gdi32.SelectObject(hdc,await gdi32.GetStockObject(gdi32.NULL_PEN));await gdi32.Rectangle(hdc,Math.floor(r.left),Math.floor(r.top),Math.ceil(r.right),Math.ceil(r.bottom));await gdi32.SelectObject(hdc,oldB);await gdi32.SelectObject(hdc,oldP);await gdi32.DeleteObject(brush);};\nconst makePen=async(erasing=false)=>\n  gdi32.CreatePen({\n    style:gdi32.PS_SOLID,\n    width:erasing?Math.max(8,penWidth*3):penWidth,\n    color:erasing?gdi32.RGB(255,255,255):color\n  });\n\nconst queueCoord=(x,y)=>{\n  pendingCoord=\n    Math.round(Number(x)||0)+', '+\n    Math.round(Number(y)||0);\n\n  if(coordScheduled)return;\n  coordScheduled=true;\n\n  requestAnimationFrame(()=>{\n    coordScheduled=false;\n    status(pendingCoord,1).catch(()=>{});\n  });\n};\n\nconst submitStrokeBatch=()=>{\n  flushScheduled=false;\n\n  if(!pendingPoints.length)return;\n\n  let batch=pendingPoints.splice(0);\n\n  /*\n   * os74:\n   * Every GDI Polyline RPC must start from the previous batch's last point.\n   * os73 discarded that continuity point, producing a real gap at each rAF\n   * batch boundary.\n   */\n  if(strokeTail){\n    batch.unshift(strokeTail);\n  }\n\n  if(batch.length<2){\n    strokeTail=batch[batch.length-1]||strokeTail;\n    return;\n  }\n\n  strokeTail=batch[batch.length-1];\n\n  const erasing=(tool==='eraser');\n\n  drawChain=drawChain.then(async()=>{\n    const pen=await makePen(erasing),\n          old=await gdi32.SelectObject(hdc,pen);\n\n    try{\n      await gdi32.Polyline(hdc,batch);\n    }finally{\n      await gdi32.SelectObject(hdc,old);\n      await gdi32.DeleteObject(pen);\n    }\n  }).catch(err=>{\n    status(\n      '繪圖錯誤：'+String(err&&err.message?err.message:err),\n      1\n    ).catch(()=>{});\n  });\n};\n\nconst scheduleStrokeFlush=()=>{\n  if(flushScheduled)return;\n\n  flushScheduled=true;\n  requestAnimationFrame(()=>{\n    submitStrokeBatch();\n\n    if(pendingPoints.length){\n      scheduleStrokeFlush();\n    }\n  });\n};\n\nconst flushStrokeNow=async()=>{\n  /*\n   * A previously scheduled rAF may still fire later; submitStrokeBatch() is\n   * idempotent when no points remain.\n   */\n  if(pendingPoints.length){\n    submitStrokeBatch();\n  }\n\n  await drawChain;\n};\n\nconst pointerSamples=e=>{\n  const a=\n    Array.isArray(e&&e.coalesced)&&e.coalesced.length\n      ?e.coalesced\n      :[e];\n\n  return a.map(absPoint);\n};\nconst mimeFor=path=>{const e=String(path||'').split('.').pop().toLowerCase();return e==='jpg'||e==='jpeg'?'image/jpeg':e==='gif'?'image/gif':e==='webp'?'image/webp':e==='bmp'?'image/bmp':'image/png';};\nconst bytesToDataUrl=(bytes,mime)=>{let s='',step=0x8000;for(let i=0;i<bytes.length;i+=step)s+=String.fromCharCode.apply(null,bytes.subarray(i,Math.min(bytes.length,i+step)));return'data:'+mime+';base64,'+btoa(s);};\nconst dataUrlToBytes=url=>{const b64=String(url).split(',')[1]||'',s=atob(b64),a=new Uint8Array(s.length);for(let i=0;i<s.length;i++)a[i]=s.charCodeAt(i)&255;return a;};\nconst loadDataUrl=async(dataUrl,path='')=>{const r=await rectNow();await captureUndo();const bmp=await gdi32.CreateBitmapFromDataUrl(dataUrl),bi=await gdi32.GetObject(bmp),dc=await gdi32.CreateCompatibleDC(hdc);await gdi32.SelectObject(dc,bmp);await paintWhite();const scale=Math.min(r.width/bi.width,r.height/bi.height,1),w=Math.max(1,Math.round(bi.width*scale)),h=Math.max(1,Math.round(bi.height*scale)),x=Math.floor(r.left+(r.width-w)/2),y=Math.floor(r.top+(r.height-h)/2);await gdi32.StretchBlt(hdc,x,y,w,h,dc,0,0,bi.width,bi.height,gdi32.SRCCOPY);await gdi32.DeleteDC(dc);await gdi32.DeleteObject(bmp);currentPath=path||'';await user32.SetWindowText(hwnd,(currentPath?currentPath.split('\\\\').pop():'未命名')+' - 小畫家');await status((currentPath||'圖片已載入')+' ｜ '+bi.width+'×'+bi.height,1);};\nconst loadPath=async path=>{const h=await kernel32.CreateFile(path,'r','OPEN_EXISTING');try{const n=await kernel32.GetFileSize(h),r=await kernel32.ReadFileBuffer(h,n,0),bytes=r.data instanceof Uint8Array?r.data:new Uint8Array(r.data);await loadDataUrl(bytesToDataUrl(bytes,mimeFor(path)),path);}finally{await kernel32.CloseHandle(h);}};\nconst saveTo=async path=>{const r=await rectNow(),url=await gdi32.GetDCDataUrl(hdc,{left:r.left,top:r.top,right:r.right,bottom:r.bottom},'image/png',1),bytes=dataUrlToBytes(url),h=await kernel32.CreateFile(path,'w','CREATE_ALWAYS');try{await kernel32.WriteFile(h,bytes,0);await kernel32.FlushFileBuffers(h);}finally{await kernel32.CloseHandle(h);}currentPath=path;await user32.SetWindowText(hwnd,path.split('\\\\').pop()+' - 小畫家');await status('已儲存 '+path+' ｜ '+bytes.length.toLocaleString()+' bytes',1);};\nconst saveAs=async()=>{let cwd=await kernel32.GetCurrentDirectory(),r=await comdlg32.GetSaveFileName({hwndOwner:hwnd,title:'另存新檔',initialDir:currentPath?currentPath.replace(/\\\\[^\\\\]+$/,''):cwd,file:currentPath?currentPath.split('\\\\').pop():'未命名.png',defaultExt:'png',overwritePrompt:true,filter:[{name:'PNG 圖片',pattern:'*.png'}]});if(!r)return false;await saveTo(r.file);return true;};\nawait waitCanvasReady();\nhdc=await gdi32.GetDC(hwnd);\nawait paintWhite();\n\nfor(const id of ['pencil','line','rect','ellipse','eraser'])user32.OnControl('tool_'+id,async e=>{if(e.action!=='click')return;tool=id;await status(({pencil:'鉛筆',line:'直線',rect:'矩形',ellipse:'橢圓',eraser:'橡皮擦'})[tool]+' ｜ '+penWidth+' px');});\nfor(const w of [1,2,4,8,14])user32.OnControl('w_'+w,async e=>{if(e.action!=='click')return;penWidth=w;await status((tool==='eraser'?'橡皮擦':tool==='pencil'?'鉛筆':tool==='line'?'直線':tool==='rect'?'矩形':'橢圓')+' ｜ '+penWidth+' px');});\nfor(const [hex] of colors)user32.OnControl('c_'+hex,async e=>{if(e.action==='click'){color=rgbHex(hex);await status('顏色 #'+hex.toUpperCase(),1);}});\nuser32.OnControl('customColor',async e=>{if(e.action!=='click')return;const r=await comdlg32.ChooseColor({hwndOwner:hwnd,rgbResult:color,title:'編輯色彩'});if(r){color=r.rgbResult;await status('自訂顏色 RGB('+r.rgb.r+', '+r.rgb.g+', '+r.rgb.b+')',1);}});\nuser32.OnControl('canvasHost',async e=>{\n  if(e.action==='pointerdown'){\n    await rectNow();\n    await flushStrokeNow();\n    await captureUndo();\n\n    drawing=true;\n    start=absPoint(e);\n    strokeTail=start;\n    pendingPoints=[];\n\n    queueCoord(e.x,e.y);\n    return;\n  }\n\n  if(e.action==='pointermove'){\n    queueCoord(e.x,e.y);\n\n    if(!drawing)return;\n\n    if(tool==='pencil'||tool==='eraser'){\n      const samples=pointerSamples(e);\n\n      for(const p of samples){\n        pendingPoints.push(p);\n      }\n\n      scheduleStrokeFlush();\n    }\n\n    return;\n  }\n\n  if(\n    e.action==='pointerup'||\n    e.action==='pointercancel'\n  ){\n    if(!drawing)return;\n\n    drawing=false;\n    const end=absPoint(e);\n\n    queueCoord(e.x,e.y);\n\n    if(tool==='pencil'||tool==='eraser'){\n      pendingPoints.push(end);\n      await flushStrokeNow();\n      strokeTail=null;\n      pendingPoints=[];\n      return;\n    }\n\n    const pen=await makePen(false),\n          oldP=await gdi32.SelectObject(hdc,pen),\n          oldB=await gdi32.SelectObject(\n            hdc,\n            await gdi32.GetStockObject(gdi32.NULL_BRUSH)\n          );\n\n    try{\n      if(tool==='line'){\n        await gdi32.MoveToEx(\n          hdc,\n          start.x,\n          start.y\n        );\n        await gdi32.LineTo(\n          hdc,\n          end.x,\n          end.y\n        );\n      }else if(tool==='rect'){\n        await gdi32.Rectangle(\n          hdc,\n          start.x,\n          start.y,\n          end.x,\n          end.y\n        );\n      }else if(tool==='ellipse'){\n        await gdi32.Ellipse(\n          hdc,\n          start.x,\n          start.y,\n          end.x,\n          end.y\n        );\n      }\n    }finally{\n      await gdi32.SelectObject(hdc,oldP);\n      await gdi32.SelectObject(hdc,oldB);\n      await gdi32.DeleteObject(pen);\n    }\n\n    return;\n  }\n});\ncomctl32.OnNotify('filebar',async e=>{if(e.code!=='TBN_CLICK')return;const id=String(e.data&&e.data.button?e.data.button.id:'');try{if(id==='new'){await captureUndo();await paintWhite();currentPath='';await user32.SetWindowText(hwnd,'未命名 - 小畫家');return;}if(id==='open'){const cwd=await kernel32.GetCurrentDirectory(),r=await comdlg32.GetOpenFileName({hwndOwner:hwnd,title:'開啟圖片',initialDir:currentPath?currentPath.replace(/\\\\[^\\\\]+$/,''):cwd,filter:[{name:'圖片檔案',pattern:'*.png;*.jpg;*.jpeg;*.gif;*.webp;*.bmp'},{name:'所有檔案',pattern:'*.*'}]});if(r)await loadPath(r.file);return;}if(id==='import'){const u=await user32.PickImageDataUrl();if(u)await loadDataUrl(u,'');return;}if(id==='save'){if(currentPath&&/\\.png$/i.test(currentPath))await saveTo(currentPath);else await saveAs();return;}if(id==='saveas'){await saveAs();return;}if(id==='undo'){const u=undoStack.pop();if(!u)return;const r=await rectNow();await gdi32.StretchBlt(hdc,r.left,r.top,r.width,r.height,u.dc,0,0,u.w,u.h,gdi32.SRCCOPY);await gdi32.DeleteDC(u.dc);await gdi32.DeleteObject(u.bmp);return;}if(id==='clear'){await captureUndo();await paintWhite();return;}}catch(err){await user32.MessageBox(String(err&&err.message?err.message:err),'小畫家');}});\nif(process.argv[1]){try{await loadPath(String(process.argv[1]));}catch(e){await user32.MessageBox('無法開啟圖片：\\n'+String(e&&e.message?e.message:e),'小畫家');}}\n"},"audio_preview":{"id":"audio_preview","fileName":"audio_preview.xsh","title":"音訊預覽","description":"ExOS Audio Preview (.xsh / MediaFoundation / GDI waveform+spectrum)","icon":"media","singleInstance":false,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\nIcon=res://shell32.dll/media\n*/\nconst gdi32=ExOS.LoadLibrary('gdi32.dll');\nconst comdlg32=ExOS.LoadLibrary('comdlg32.dll');\nconst media=ExOS.MediaFoundation;\n\nconst fmtTime=v=>{\n  let s=Math.max(0,Number(v)||0),h=Math.floor(s/3600);s-=h*3600;\n  const m=Math.floor(s/60),sec=Math.floor(s-m*60),ss=String(sec).padStart(2,'0');\n  return h>0?`${h}:${String(m).padStart(2,'0')}:${ss}`:`${m}:${ss}`;\n};\nconst leaf=p=>{p=String(p||'').replace(/\\//g,'\\\\');const i=p.lastIndexOf('\\\\');return i>=0?p.substring(i+1):p;};\n\nlet path=String(process.argv[1]||'');\nif(!path){\n  const picked=await comdlg32.GetOpenFileName({\n    title:'開啟音訊',\n    initialDir:await kernel32.GetCurrentDirectory(),\n    filter:[\n      {name:'音訊檔案',pattern:'*.mp3;*.wav;*.ogg;*.m4a;*.aac;*.flac'},\n      {name:'所有檔案',pattern:'*.*'}\n    ]\n  });\n  if(!picked){await process.ExitProcess(0);return;}\n  path=String(picked.file||'');\n}\nif(!path)throw new Error('沒有指定音訊檔案。');\n\nconst hwnd=await user32.CreateWindow({title:'音訊預覽 - '+leaf(path),icon:'media',width:820,height:560});\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',minHeight:'0',margin:'0',padding:'0',overflow:'hidden'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'header',parentId:'root',style:{display:'flex',alignItems:'center',gap:'12px',padding:'14px 16px',margin:'0',background:'#f8fafc',borderBottom:'1px solid #dbe3ec'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'mediaIcon',parentId:'header',text:'♪',style:{display:'flex',alignItems:'center',justifyContent:'center',width:'48px',height:'48px',borderRadius:'7px',background:'#0078d7',color:'#fff',fontSize:'28px',fontWeight:'600',flex:'0 0 48px'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'headline',parentId:'header',style:{display:'flex',flexDirection:'column',gap:'3px',minWidth:'0',flex:'1'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'fileName',parentId:'headline',text:leaf(path),style:{margin:'0',fontSize:'17px',fontWeight:'600',overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'mediaInfo',parentId:'headline',text:'正在解碼音訊…',style:{margin:'0',fontSize:'12px',color:'#64748b'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'visualHost',parentId:'root',style:{flex:'1',minHeight:'190px',margin:'14px 16px 8px',border:'1px solid #334155',borderRadius:'5px',background:'transparent',pointerEvents:'none'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'seekRow',parentId:'root',style:{display:'grid',gridTemplateColumns:'54px minmax(0,1fr) 54px',alignItems:'center',gap:'9px',padding:'4px 16px',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'currentTime',parentId:'seekRow',text:'0:00',style:{margin:'0',fontFamily:'Consolas,monospace',fontSize:'12px'}});\nawait user32.CreateControl(hwnd,{type:'input',inputType:'range',id:'seek',parentId:'seekRow',value:'0',min:'0',max:'1000',step:'1',style:{width:'100%',margin:'0',padding:'0'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'duration',parentId:'seekRow',text:'0:00',style:{margin:'0',textAlign:'right',fontFamily:'Consolas,monospace',fontSize:'12px'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'controls',parentId:'root',style:{display:'flex',alignItems:'center',gap:'8px',padding:'9px 16px 13px',margin:'0',background:'#f8fafc',borderTop:'1px solid #e2e8f0'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'play',parentId:'controls',text:'▶ 播放',style:{margin:'0',minWidth:'88px',fontWeight:'600'}});\nawait user32.CreateControl(hwnd,{type:'button',id:'stop',parentId:'controls',text:'■ 停止',style:{margin:'0',minWidth:'78px'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'loopGroup',parentId:'controls',style:{display:'flex',alignItems:'center',gap:'5px',margin:'0 10px 0 4px'}});\nawait user32.CreateControl(hwnd,{type:'input',inputType:'checkbox',id:'loop',parentId:'loopGroup',checked:false,style:{width:'16px',height:'16px',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'loopLabel',parentId:'loopGroup',text:'循環',style:{margin:'0',fontSize:'12px'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'volumeLabel',parentId:'controls',text:'音量 100%',style:{margin:'0 0 0 auto',fontSize:'12px',whiteSpace:'nowrap'}});\nawait user32.CreateControl(hwnd,{type:'input',inputType:'range',id:'volume',parentId:'controls',value:'100',min:'0',max:'100',step:'1',style:{width:'150px',margin:'0',padding:'0'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'status',parentId:'root',text:'MediaFoundation：準備中',style:{margin:'0',padding:'5px 10px',minHeight:'24px',boxSizing:'border-box',borderTop:'1px solid #cbd5e1',background:'#f1f5f9',color:'#475569',fontSize:'11px'}});\n\nawait media.MFStartup();\nconst session=await media.CreateAudioSession({fftSize:2048,smoothing:0.72});\nlet track;\ntry{\n  track=await media.CreateSourceFromPath(session.handle,path,{volume:1,pan:0,loop:false,analyser:{fftSize:2048,smoothing:0.68}});\n}catch(err){\n  await user32.MessageBox('音訊解碼失敗：\\n\\n'+String(err&&err.message?err.message:err),'音訊預覽');\n  throw err;\n}\nconst duration=Number(track.duration)||0;\nawait user32.SetControlText('mediaInfo',`${track.sampleRate} Hz ｜ ${track.channels} channel${track.channels===1?'':'s'} ｜ ${fmtTime(duration)}`);\nawait user32.SetControlText('duration',fmtTime(duration));\nawait user32.SetControlText('status',`MediaFoundation ｜ ${path}`);\n\nconst hdc=await gdi32.GetDC(hwnd);\nconst wavePen=await gdi32.CreatePen({style:gdi32.PS_SOLID,width:1,color:gdi32.RGB(80,220,255)});\nconst spectrumPen=await gdi32.CreatePen({style:gdi32.PS_SOLID,width:2,color:gdi32.RGB(96,165,250)});\nlet seeking=false;\nconst updatePlayText=state=>user32.SetControlText('play',state==='playing'?'❚❚ 暫停':'▶ 播放');\n\nuser32.OnControl('play',async e=>{\n  if(e.action!=='click')return;\n  const s=await media.GetState(track.handle);\n  if(s.state==='playing'){await media.Pause(track.handle);await updatePlayText('paused');}\n  else{await media.Play(track.handle);await updatePlayText('playing');}\n});\nuser32.OnControl('stop',async e=>{if(e.action!=='click')return;await media.Stop(track.handle);await updatePlayText('stopped');await user32.SetControlText('seek','0');await user32.SetControlText('currentTime','0:00');});\nuser32.OnControl('loop',async e=>{if(e.action==='change'||e.action==='click')await media.SetLoop(track.handle,!!e.checked);});\nuser32.OnControl('volume',async e=>{\n  if(e.action!=='input'&&e.action!=='change')return;\n  const pct=Math.max(0,Math.min(100,Number(e.value)||0));\n  await user32.SetControlText('volumeLabel','音量 '+Math.round(pct)+'%');\n  await media.SetVolume(track.handle,pct/100);\n});\nuser32.OnControl('seek',async e=>{\n  if(duration<=0)return;\n  const n=Math.max(0,Math.min(1000,Number(e.value)||0));\n  if(e.action==='input'){seeking=true;await user32.SetControlText('currentTime',fmtTime(duration*n/1000));return;}\n  if(e.action==='change'){await media.Seek(track.handle,duration*n/1000);seeking=false;}\n});\n\nconst drawVisual=async()=>{\n  const r=await user32.GetControlRect('visualHost'),left=Math.round(r.left),top=Math.round(r.top),width=Math.max(20,Math.round(r.width)),height=Math.max(40,Math.round(r.height));\n  await gdi32.PatBlt(hdc,left,top,width,height,gdi32.BLACKNESS);\n  const wave=await media.GetWaveform(track.handle,{bins:160});\n  if(wave&&wave.length>1){\n    const pts=[],mid=top+height*0.43,amp=height*0.33;\n    for(let i=0;i<wave.length;i++){const norm=(Number(wave[i])-128)/128;pts.push({x:left+(i/(wave.length-1))*width,y:mid+norm*amp});}\n    const old=await gdi32.SelectObject(hdc,wavePen);try{await gdi32.Polyline(hdc,pts);}finally{await gdi32.SelectObject(hdc,old);}\n  }\n  const spec=await media.GetSpectrum(track.handle,{bins:96});\n  if(spec&&spec.length>1){\n    const pts=[],base=top+height-12,amp=height*0.31;\n    for(let i=0;i<spec.length;i++){const v=Math.max(0,Math.min(255,Number(spec[i])||0));pts.push({x:left+(i/(spec.length-1))*width,y:base-(v/255)*amp});}\n    const old=await gdi32.SelectObject(hdc,spectrumPen);try{await gdi32.Polyline(hdc,pts);}finally{await gdi32.SelectObject(hdc,old);}\n  }\n};\n\nlet lastUi=0,lastVisual=0;\nwhile(true){\n  const now=await new Promise(resolve=>requestAnimationFrame(resolve));\n  if(now-lastUi>=120){\n    lastUi=now;\n    const s=await media.GetState(track.handle),current=Math.max(0,Number(s.currentTime)||0);\n    if(!seeking&&duration>0){await user32.SetControlText('seek',String(Math.round(Math.min(1,current/duration)*1000)));await user32.SetControlText('currentTime',fmtTime(current));}\n    await updatePlayText(s.state);\n    await user32.SetControlText('status',`狀態 ${String(s.state||'')} ｜ ${fmtTime(current)} / ${fmtTime(duration)} ｜ ${Math.round((Number(s.volume)||0)*100)}%`);\n  }\n  if(now-lastVisual>=90){lastVisual=now;try{await drawVisual();}catch(ignoreVisual){}}\n}\n"},"zipfolder":{"id":"zipfolder","fileName":"zipfolder.xsh","title":"ZIP 壓縮資料夾","description":"zipfldr.dll Shell Namespace Extension host","icon":"zipfolder","singleInstance":false,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\nIcon=res://shell32.dll/zipfolder\n*/\nconst zipfldr=ExOS.LoadLibrary('zipfldr.dll');\nconst comdlg32=ExOS.LoadLibrary('comdlg32.dll');\nconst comctl32=ExOS.LoadLibrary('comctl32.dll');\nconst shell32=ExOS.LoadLibrary('shell32.dll');\nlet archivePath=String(process.argv[1]||'');\nif(!archivePath)throw new Error('zipfolder.xsh requires a .zip path.');\nconst extractMode=String(process.argv[2]||'').toLowerCase()==='/extract';\nlet inner='',zip=null,selected=[];\nconst leaf=p=>{p=String(p||'').replace(/\\//g,'\\\\');const i=p.lastIndexOf('\\\\');return i>=0?p.slice(i+1):p;};\nconst parent=p=>{p=String(p||'').replace(/\\//g,'\\\\');const i=p.lastIndexOf('\\\\');return i>2?p.slice(0,i):'C:\\\\';};\nconst currentVirtual=()=>zipfldr.MakeVirtualPath(archivePath,inner);\nconst hwnd=await user32.CreateWindow({title:'壓縮資料夾 - '+leaf(archivePath),icon:'zipfolder',width:960,height:670});\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',width:'100%',height:'100%',margin:'0',padding:'0',overflow:'hidden'}});\nawait user32.CreateControl(hwnd,{type:'div',id:'bar',parentId:'root',style:{display:'flex',gap:'6px',alignItems:'center',padding:'7px 9px',background:'#f1f5f9',borderBottom:'1px solid #cbd5e1'}});\nfor(const [id,text] of [['up','↑ 上一層'],['refresh','⟳ 重新整理'],['add','＋ 加入檔案'],['folder','新增資料夾'],['extract','全部解壓縮']])await user32.CreateControl(hwnd,{type:'button',id,parentId:'bar',text,style:{margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'path',parentId:'bar',text:'',style:{margin:'0 0 0 8px',padding:'4px 7px',background:'#fff',border:'1px solid #cbd5e1',flex:'1',overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}});\nawait comctl32.CreateListView(hwnd,{id:'list',parentId:'root',view:'details',multiSelect:true,dragDrop:true,style:{flex:'1',minHeight:'0',margin:'0'},columns:[{id:'name',text:'名稱',width:410},{id:'type',text:'類型',width:180},{id:'size',text:'大小',width:120}]});\nawait user32.CreateControl(hwnd,{type:'label',id:'status',parentId:'root',text:'',style:{padding:'5px 9px',margin:'0',background:'#f8fafc',borderTop:'1px solid #cbd5e1',fontSize:'11px'}});\nconst fmt=n=>{n=Number(n)||0;if(n<1024)return n+' B';if(n<1048576)return(n/1024).toFixed(1)+' KB';return(n/1048576).toFixed(1)+' MB';};\nasync function openZip(){if(zip)try{await zipfldr.CloseArchive(zip);}catch(_){}const r=await zipfldr.OpenArchive(archivePath);zip=r.handle;}\nasync function load(){await openZip();const rows=await zipfldr.EnumItems(zip,inner);await comctl32.ListView_DeleteAllItems('list');for(const x of rows){x.virtualPath=await zipfldr.MakeVirtualPath(archivePath,x.path);await comctl32.ListView_InsertItem('list',{text:x.name,subItems:[x.directory?'檔案資料夾':'ZIP 檔案',x.directory?'':fmt(x.size)],image:x.directory?'folder':'file',data:x});}await user32.SetControlText('path',archivePath+'!\\\\'+inner.replace(/\\//g,'\\\\'));await user32.SetControlText('status',rows.length+' 個項目 ｜ ZIP 壓縮資料夾 ｜ 可拖曳複製進出');}\nasync function chooseDest(){const r=await comdlg32.GetSaveFileName({hwndOwner:hwnd,title:'選擇解壓縮目的資料夾（檔名只用來定位資料夾）',initialDir:parent(archivePath),file:'extract.here',filter:[{name:'目的位置',pattern:'*.*'}],overwritePrompt:false});return r?parent(r.file):'';}\nasync function extractAll(){const d=await chooseDest();if(!d)return;await user32.SetControlText('status','正在解壓縮…');const r=await zipfldr.ExtractAll(zip,d);await user32.MessageBox('已解壓縮到：\\n'+d+'\\n\\n項目：'+r.count,'ZIP 壓縮資料夾');await user32.SetControlText('status','解壓縮完成。');}\ncomctl32.OnNotify('list',async e=>{\n  if(e.code==='LVN_ITEMCHANGED'){const rows=await comctl32.ListView_GetSelectedItems('list');selected=rows.map(r=>r.data).filter(Boolean);return;}\n  if(e.code==='NM_DBLCLK'){const row=e.data&&e.data.item&&e.data.item.data;if(!row)return;if(row.directory){inner=row.path;await load();}else{const d=await chooseDest();if(d)await zipfldr.ExtractItem(zip,row.path,d);}return;}\n  if(e.code==='LVN_BEGINDRAG'){const rows=await comctl32.ListView_GetSelectedItems('list'),paths=rows.map(r=>r.data&&r.data.virtualPath).filter(Boolean);if(paths.length)await shell32.BeginDragDrop(paths,{allowedEffects:['copy']});return;}\n  if(e.code==='LVN_DRAGOVER'){await shell32.DragOver(currentVirtual(),{ctrlKey:true});return;}\n  if(e.code==='NM_DROP'){\n    if(e.data&&Array.isArray(e.data.externalFiles)){for(const f of e.data.externalFiles)await zipfldr.AddPickedFile(zip,f.token,inner);await load();return;}\n    const r=await shell32.Drop(currentVirtual(),{ctrlKey:true});if(r&&r.ok)await load();return;\n  }\n  if(e.code==='LVN_KEYDOWN'&&e.data&&e.data.key==='Delete'&&selected.length){for(const x of selected)await zipfldr.DeleteItem(zip,x.path);selected=[];await load();return;}\n});\nuser32.OnControl('up',async e=>{if(e.action!=='click'||!inner)return;const i=inner.lastIndexOf('/');inner=i>=0?inner.slice(0,i):'';await load();});\nuser32.OnControl('refresh',async e=>{if(e.action==='click')await load();});\nuser32.OnControl('extract',async e=>{if(e.action==='click')await extractAll();});\nuser32.OnControl('add',async e=>{if(e.action!=='click')return;const f=await comdlg32.GetOpenFileName({hwndOwner:hwnd,title:'加入 ZIP',initialDir:parent(archivePath),filter:[{name:'所有檔案',pattern:'*.*'}]});if(!f)return;await zipfldr.AddPath(zip,f.file,inner);await load();});\nuser32.OnControl('folder',async e=>{if(e.action!=='click')return;const name=await user32.PromptBox('資料夾名稱：','新增 ZIP 資料夾','新資料夾');if(!name)return;await zipfldr.CreateFolder(zip,(inner?inner+'/':'')+name);await load();});\nawait load();if(extractMode)setTimeout(()=>extractAll().catch(()=>{}),100);\n"},"winmine":{"id":"winmine","fileName":"winmine.xsh","title":"踩地雷","description":"ExOS Minesweeper built-in mini game (.xsh / GDI32)","icon":"minesweeper","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\nIcon=res://shell32.dll/minesweeper\n*/\nconst gdi32=ExOS.LoadLibrary('gdi32.dll');\nconst uxtheme=ExOS.LoadLibrary('uxtheme.dll');\nconst dwmapi=ExOS.LoadLibrary('dwmapi.dll');\n\nconst hwnd=await user32.CreateWindow({\n  title:'踩地雷',\n  icon:'minesweeper',\n  width:920,\n  height:650\n});\n\nawait uxtheme.SetWindowTheme(hwnd,'Games','Explorer');\nawait dwmapi.DwmSetWindowAttribute(\n  hwnd,\n  dwmapi.DWMWA_WINDOW_CORNER_PREFERENCE,\n  'roundsmall'\n);\n\nawait user32.CreateControl(hwnd,{\n  type:'div',id:'root',\n  style:{\n    display:'flex',flexDirection:'column',\n    width:'100%',height:'100%',minHeight:'0',\n    margin:'0',padding:'0',overflow:'hidden',\n    background:'#d1d5db'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',id:'toolbar',parentId:'root',\n  style:{\n    display:'flex',alignItems:'center',gap:'7px',\n    padding:'8px 10px',margin:'0',\n    background:'#e5e7eb',borderBottom:'1px solid #94a3b8'\n  }\n});\n\nfor(const [id,text] of [\n  ['beginner','初級'],\n  ['intermediate','中級'],\n  ['expert','高級']\n]){\n  await user32.CreateControl(hwnd,{\n    type:'button',id:'level_'+id,parentId:'toolbar',text,\n    style:{margin:'0',minWidth:'62px'}\n  });\n}\n\nawait user32.CreateControl(hwnd,{\n  type:'label',id:'mineLabel',parentId:'toolbar',text:'地雷 010',\n  style:{\n    margin:'0 0 0 14px',padding:'4px 8px',\n    minWidth:'82px',boxSizing:'border-box',\n    background:'#111827',color:'#ef4444',\n    border:'2px inset #94a3b8',\n    fontFamily:'Consolas,monospace',fontSize:'18px',fontWeight:'700'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'button',id:'reset',parentId:'toolbar',text:'🙂',\n  title:'重新開始',\n  style:{margin:'0 auto',width:'46px',minWidth:'46px',fontSize:'20px'}\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'label',id:'timerLabel',parentId:'toolbar',text:'000',\n  style:{\n    margin:'0',padding:'4px 8px',\n    minWidth:'72px',boxSizing:'border-box',textAlign:'right',\n    background:'#111827',color:'#ef4444',\n    border:'2px inset #94a3b8',\n    fontFamily:'Consolas,monospace',fontSize:'18px',fontWeight:'700'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',id:'boardWrap',parentId:'root',\n  style:{\n    display:'flex',alignItems:'center',justifyContent:'center',\n    flex:'1',minHeight:'0',padding:'12px',margin:'0',\n    background:'#cbd5e1',boxSizing:'border-box'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'div',id:'boardHost',parentId:'boardWrap',\n  trackPointer:true,\n  preventContextMenu:true,\n  style:{\n    flex:'1',width:'100%',height:'100%',minWidth:'0',minHeight:'0',\n    margin:'0',padding:'0',background:'transparent',\n    border:'2px inset #94a3b8',boxSizing:'border-box',\n    cursor:'default',touchAction:'none'\n  }\n});\n\nawait user32.CreateControl(hwnd,{\n  type:'label',id:'status',parentId:'root',text:'初級 ｜ 左鍵開啟，右鍵插旗',\n  style:{\n    margin:'0',padding:'5px 9px',minHeight:'25px',boxSizing:'border-box',\n    background:'#f1f5f9',borderTop:'1px solid #94a3b8',\n    color:'#475569',fontSize:'11px'\n  }\n});\n\nconst LEVELS={\n  beginner:{name:'初級',rows:9,cols:9,mines:10},\n  intermediate:{name:'中級',rows:16,cols:16,mines:40},\n  expert:{name:'高級',rows:16,cols:30,mines:99}\n};\n\nlet levelKey='beginner';\nlet level=LEVELS[levelKey];\nlet cells=[];\nlet generated=false;\nlet running=false;\nlet gameOver=false;\nlet won=false;\nlet flags=0;\nlet opened=0;\nlet startedAt=0;\nlet elapsed=0;\nlet hdc=0;\nlet boardRect=null;\nlet geom=null;\n\nconst rgb=(r,g,b)=>gdi32.RGB(r,g,b);\nconst colors={\n  hidden:rgb(203,213,225),\n  open:rgb(241,245,249),\n  mine:rgb(254,202,202),\n  edge:rgb(100,116,139),\n  text:rgb(15,23,42),\n  flag:rgb(220,38,38),\n  nums:[\n    rgb(15,23,42),\n    rgb(37,99,235),\n    rgb(22,163,74),\n    rgb(220,38,38),\n    rgb(88,28,135),\n    rgb(180,83,9),\n    rgb(8,145,178),\n    rgb(17,24,39),\n    rgb(100,116,139)\n  ]\n};\n\nlet pen=0,font=0,hiddenBrush=0,openBrush=0,mineBrush=0,boardBgBrush=0;\n\nconst pad3=n=>String(Math.max(0,Math.min(999,Math.floor(Number(n)||0)))).padStart(3,'0');\nconst indexOf=(r,c)=>r*level.cols+c;\nconst inBounds=(r,c)=>r>=0&&c>=0&&r<level.rows&&c<level.cols;\nconst neighbours=(r,c)=>{\n  const out=[];\n  for(let dr=-1;dr<=1;dr++)for(let dc=-1;dc<=1;dc++){\n    if(!dr&&!dc)continue;\n    const rr=r+dr,cc=c+dc;\n    if(inBounds(rr,cc))out.push(indexOf(rr,cc));\n  }\n  return out;\n};\n\nconst updateTop=async()=>{\n  await user32.SetControlText('mineLabel','地雷 '+pad3(level.mines-flags));\n  await user32.SetControlText('timerLabel',pad3(elapsed));\n  await user32.SetControlText('reset',gameOver?(won?'😎':'😵'):'🙂');\n};\n\nconst waitBoardReady=async()=>{\n  let r=null;\n  for(let i=0;i<12;i++){\n    await new Promise(resolve=>requestAnimationFrame(resolve));\n    r=await user32.GetControlRect('boardHost');\n    if(Number(r.width)>=120&&Number(r.height)>=120)return r;\n  }\n  return r||await user32.GetControlRect('boardHost');\n};\n\nconst measureBoard=async()=>{\n  boardRect=await user32.GetControlRect('boardHost');\n  const cell=Math.max(12,Math.floor(Math.min(\n    (Number(boardRect.width)-8)/level.cols,\n    (Number(boardRect.height)-8)/level.rows\n  )));\n  const width=cell*level.cols;\n  const height=cell*level.rows;\n  geom={\n    cell,\n    left:Number(boardRect.left)+Math.floor((Number(boardRect.width)-width)/2),\n    top:Number(boardRect.top)+Math.floor((Number(boardRect.height)-height)/2),\n    width,height\n  };\n  return geom;\n};\n\nconst selectBrush=async h=>gdi32.SelectObject(hdc,h);\nconst fillCell=async(r,c,brush)=>{\n  const g=geom||await measureBoard();\n  const x=g.left+c*g.cell,y=g.top+r*g.cell;\n  const old=await selectBrush(brush);\n  try{await gdi32.Rectangle(hdc,x,y,x+g.cell,y+g.cell);}\n  finally{await selectBrush(old);}\n};\n\nconst textCell=async(r,c,text,color)=>{\n  const g=geom||await measureBoard();\n  const x=g.left+c*g.cell,y=g.top+r*g.cell;\n  const oldColor=await gdi32.SetTextColor(hdc,color);\n  try{\n    await gdi32.TextOut(\n      hdc,\n      x+Math.max(2,Math.floor(g.cell/2)-5),\n      y+Math.max(1,Math.floor(g.cell/2)-9),\n      String(text)\n    );\n  }finally{\n    await gdi32.SetTextColor(hdc,oldColor);\n  }\n};\n\nconst drawCell=async idx=>{\n  const cell=cells[idx];\n  const r=Math.floor(idx/level.cols),c=idx%level.cols;\n  if(cell.open){\n    await fillCell(r,c,cell.mine?mineBrush:openBrush);\n    if(cell.mine)await textCell(r,c,'✹',colors.text);\n    else if(cell.adj>0)await textCell(r,c,String(cell.adj),colors.nums[cell.adj]||colors.text);\n    return;\n  }\n  await fillCell(r,c,hiddenBrush);\n  if(cell.flag)await textCell(r,c,'⚑',colors.flag);\n};\n\nconst clearBoardHost=async()=>{\n  boardRect=await user32.GetControlRect('boardHost');\n  const left=Math.round(Number(boardRect.left)||0),top=Math.round(Number(boardRect.top)||0),\n        width=Math.max(1,Math.round(Number(boardRect.width)||1)),height=Math.max(1,Math.round(Number(boardRect.height)||1));\n  const saved=await gdi32.SaveDC(hdc);\n  try{\n    await gdi32.IntersectClipRect(hdc,left,top,left+width,top+height);\n    const old=await gdi32.SelectObject(hdc,boardBgBrush);\n    try{await gdi32.PatBlt(hdc,left,top,width,height,0x00F00021);}finally{await gdi32.SelectObject(hdc,old);}\n  }finally{await gdi32.RestoreDC(hdc,-1);}\n};\n\nconst drawAll=async()=>{\n  await measureBoard();\n  await clearBoardHost();\n  const saved=await gdi32.SaveDC(hdc);\n  try{\n    await gdi32.IntersectClipRect(hdc,Math.round(boardRect.left),Math.round(boardRect.top),Math.round(boardRect.left+boardRect.width),Math.round(boardRect.top+boardRect.height));\n    for(let i=0;i<cells.length;i++)await drawCell(i);\n  }finally{await gdi32.RestoreDC(hdc,-1);}\n};\n\nconst resetCells=()=>{\n  cells=Array.from({length:level.rows*level.cols},()=>({mine:false,open:false,flag:false,adj:0}));\n  generated=false;running=false;gameOver=false;won=false;flags=0;opened=0;elapsed=0;startedAt=0;\n};\n\nconst generate=firstIdx=>{\n  const fr=Math.floor(firstIdx/level.cols),fc=firstIdx%level.cols;\n  const forbidden=new Set([firstIdx,...neighbours(fr,fc)]);\n  const choices=[];\n  for(let i=0;i<cells.length;i++)if(!forbidden.has(i))choices.push(i);\n  for(let i=choices.length-1;i>0;i--){\n    const j=Math.floor(Math.random()*(i+1));\n    const t=choices[i];choices[i]=choices[j];choices[j]=t;\n  }\n  for(let i=0;i<Math.min(level.mines,choices.length);i++)cells[choices[i]].mine=true;\n  for(let i=0;i<cells.length;i++){\n    if(cells[i].mine)continue;\n    const r=Math.floor(i/level.cols),c=i%level.cols;\n    cells[i].adj=neighbours(r,c).reduce((n,k)=>n+(cells[k].mine?1:0),0);\n  }\n  generated=true;\n};\n\nconst finish=async didWin=>{\n  gameOver=true;running=false;won=!!didWin;\n  if(won){\n    for(let i=0;i<cells.length;i++)if(cells[i].mine&&!cells[i].flag){cells[i].flag=true;flags++;}\n    await user32.SetControlText('status',level.name+' ｜ 完成！時間 '+elapsed+' 秒');\n  }else{\n    for(let i=0;i<cells.length;i++)if(cells[i].mine)cells[i].open=true;\n    await user32.SetControlText('status',level.name+' ｜ 踩到地雷，按笑臉重新開始');\n  }\n  await updateTop();\n  await drawAll();\n};\n\nconst reveal=async idx=>{\n  if(gameOver||cells[idx].flag||cells[idx].open)return;\n  if(!generated){\n    generate(idx);\n    running=true;\n    startedAt=performance.now();\n  }\n  if(cells[idx].mine){\n    cells[idx].open=true;\n    await finish(false);\n    return;\n  }\n  const queue=[idx];\n  const changed=[];\n  while(queue.length){\n    const cur=queue.shift();\n    const cell=cells[cur];\n    if(cell.open||cell.flag||cell.mine)continue;\n    cell.open=true;opened++;changed.push(cur);\n    if(cell.adj===0){\n      const r=Math.floor(cur/level.cols),c=cur%level.cols;\n      for(const n of neighbours(r,c))if(!cells[n].open&&!cells[n].flag&&!cells[n].mine)queue.push(n);\n    }\n  }\n  for(const i of changed)await drawCell(i);\n  if(opened>=cells.length-level.mines)await finish(true);\n};\n\nconst toggleFlag=async idx=>{\n  if(gameOver||cells[idx].open)return;\n  cells[idx].flag=!cells[idx].flag;\n  flags+=cells[idx].flag?1:-1;\n  await updateTop();\n  await drawCell(idx);\n};\n\nconst hitTest=(x,y)=>{\n  if(!geom)return-1;\n  const xx=Number(x)+Number(boardRect.left),yy=Number(y)+Number(boardRect.top);\n  if(xx<geom.left||yy<geom.top||xx>=geom.left+geom.width||yy>=geom.top+geom.height)return-1;\n  const c=Math.floor((xx-geom.left)/geom.cell),r=Math.floor((yy-geom.top)/geom.cell);\n  return inBounds(r,c)?indexOf(r,c):-1;\n};\n\nconst newGame=async key=>{\n  if(key&&LEVELS[key]){\n    levelKey=key;level=LEVELS[key];\n    /* Difficulty changes redraw into the existing resizable window. */\n    await new Promise(resolve=>requestAnimationFrame(resolve));\n  }\n  resetCells();\n  await measureBoard();\n  await updateTop();\n  await user32.SetControlText('status',level.name+' ｜ 左鍵開啟，右鍵插旗');\n  await drawAll();\n};\n\nuser32.OnControl('boardHost',async e=>{\n  if(e.action!=='pointerdown')return;\n  const idx=hitTest(e.x,e.y);\n  if(idx<0)return;\n  if(Number(e.button)===2){await toggleFlag(idx);return;}\n  if(Number(e.button)===0)await reveal(idx);\n});\n\nfor(const key of Object.keys(LEVELS)){\n  user32.OnControl('level_'+key,async e=>{if(e.action==='click')await newGame(key);});\n}\nuser32.OnControl('reset',async e=>{if(e.action==='click')await newGame(levelKey);});\n\nawait waitBoardReady();\nhdc=await gdi32.GetDC(hwnd);\npen=await gdi32.CreatePen({style:gdi32.PS_SOLID,width:1,color:colors.edge});\nhiddenBrush=await gdi32.CreateSolidBrush(colors.hidden);\nopenBrush=await gdi32.CreateSolidBrush(colors.open);\nmineBrush=await gdi32.CreateSolidBrush(colors.mine);\nboardBgBrush=await gdi32.CreateSolidBrush(rgb(203,213,225));\nfont=await gdi32.CreateFont({height:16,weight:700,faceName:'Segoe UI'});\nawait gdi32.SelectObject(hdc,pen);\nawait gdi32.SelectObject(hdc,font);\nawait gdi32.SetBkMode(hdc,gdi32.TRANSPARENT);\nawait newGame(levelKey);\n\nlet lastGeometryCheck=0;\nwhile(true){\n  const now=await new Promise(resolve=>requestAnimationFrame(resolve));\n  if(running&&!gameOver){\n    const sec=Math.min(999,Math.floor((now-startedAt)/1000));\n    if(sec!==elapsed){elapsed=sec;await updateTop();}\n  }\n  if(now-lastGeometryCheck>=600){\n    lastGeometryCheck=now;\n    const old=geom&&{w:geom.width,h:geom.height,left:geom.left,top:geom.top,cell:geom.cell};\n    await measureBoard();\n    if(!old||old.w!==geom.width||old.h!==geom.height||old.left!==geom.left||old.top!==geom.top||old.cell!==geom.cell)await drawAll();\n  }\n}\n"},"user32_ntapi_demo":{"id":"user32_ntapi_demo","fileName":"user32_ntapi_demo.xsh","title":"USER32 NT API Demo","description":"COMBOBOX/LISTBOX/GROUPBOX, coordinates, capture, window styles, metrics and hotkeys","icon":"computer","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst hwnd=await user32.CreateWindow({title:'USER32 NT API Demo',icon:'computer',width:780,height:590});\n\nawait user32.CreateControl(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',height:'100%',padding:'12px',gap:'10px',boxSizing:'border-box',background:'#f5f7fa'}});\nawait user32.CreateControl(hwnd,{type:'groupbox',id:'gbControls',parentId:'root',text:'標準 Window Classes',style:{display:'grid',gridTemplateColumns:'150px minmax(0,1fr)',gap:'8px 12px',padding:'10px',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'comboLabel',parentId:'gbControls',text:'COMBOBOX：'});\nawait user32.CreateControl(hwnd,{type:'combobox',id:'combo',parentId:'gbControls',items:[{text:'Segoe UI',value:'segoe'},{text:'Microsoft JhengHei',value:'jhenghei'},{text:'Consolas',value:'consolas'}],selectedIndex:1,style:{width:'260px'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'listLabel',parentId:'gbControls',text:'LISTBOX（多選）：'});\nawait user32.CreateControl(hwnd,{type:'listbox',id:'list',parentId:'gbControls',multiple:true,rows:5,items:['Desktop','Documents','Downloads','Public Desktop','Public Documents'],selectedIndices:[0,2],style:{width:'300px'}});\n\nawait user32.CreateControl(hwnd,{type:'groupbox',id:'gbApi',parentId:'root',text:'Window / Coordinates / Metrics',style:{display:'flex',flexWrap:'wrap',gap:'7px',padding:'10px',margin:'0'}});\nfor(const [id,t] of [['coords','座標轉換'],['capture','SetCapture'],['topmost','TOPMOST'],['caption','切換標題列'],['metrics','GetSystemMetrics']])await user32.CreateControl(hwnd,{type:'button',id,parentId:'gbApi',text:t,style:{minWidth:'105px',margin:'0'}});\n\nawait user32.CreateControl(hwnd,{type:'groupbox',id:'gbHotkey',parentId:'root',text:'RegisterHotKey',style:{display:'flex',alignItems:'center',gap:'10px',padding:'10px',margin:'0'}});\nawait user32.CreateControl(hwnd,{type:'label',id:'hotkeyInfo',parentId:'gbHotkey',text:'Ctrl + Shift + H：等待註冊…',style:{margin:'0'}});\n\nawait user32.CreateControl(hwnd,{type:'pre',id:'log',parentId:'root',text:'',style:{flex:'1',minHeight:'170px',margin:'0',padding:'9px',overflow:'auto',background:'#0f172a',color:'#e2e8f0',fontFamily:'Consolas,monospace',fontSize:'12px',whiteSpace:'pre-wrap'}});\n\nconst log=async s=>user32.AppendControlText('log',String(s)+'\\n');\nconst combo=await user32.GetControlProperty('combo','items');\nawait log('COMBOBOX class = '+await user32.GetControlProperty('combo','win32Class')+' / items='+combo.length);\nawait log('LISTBOX class = '+await user32.GetControlProperty('list','win32Class')+' / selected='+JSON.stringify(await user32.GetControlProperty('list','selectedIndices')));\n\nuser32.OnControl('combo',async e=>{if(e.action==='change')await log('COMBOBOX change -> '+e.text+' / '+e.value);});\nuser32.OnControl('list',async e=>{if(e.action==='change')await log('LISTBOX selection -> '+JSON.stringify(e.selectedTexts));});\n\nuser32.OnControl('coords',async e=>{if(e.action!=='click')return;const c={x:20,y:30},s=await user32.ClientToScreen(hwnd,c),b=await user32.ScreenToClient(hwnd,s),cur=await user32.GetCursorPos();await log('Client '+JSON.stringify(c)+' -> Screen '+JSON.stringify(s)+' -> Client '+JSON.stringify(b)+' / cursor '+JSON.stringify(cur));});\nuser32.OnControl('metrics',async e=>{if(e.action!=='click')return;await log('Screen = '+await user32.GetSystemMetrics(user32.SM_CXSCREEN)+' x '+await user32.GetSystemMetrics(user32.SM_CYSCREEN)+' / caption='+await user32.GetSystemMetrics(user32.SM_CYCAPTION));});\n\nlet captured=false;\nuser32.OnControl('capture',async e=>{if(e.action!=='click')return;if(!captured){await user32.SetCapture(hwnd);captured=true;await user32.SetControlText('capture','ReleaseCapture');await log('Mouse capture ON：移動滑鼠，即使超出 client 仍會收到 window capture event。');}else{await user32.ReleaseCapture();captured=false;await user32.SetControlText('capture','SetCapture');await log('Mouse capture OFF');}});\nuser32.OnWindow(hwnd,async e=>{if(String(e.action||'').indexOf('capture-')===0){if(e.action==='capture-pointerup')await log('capture pointerup @ '+Math.round(e.x)+','+Math.round(e.y));}});\n\nlet topmost=false;\nuser32.OnControl('topmost',async e=>{if(e.action!=='click')return;topmost=!topmost;await user32.SetWindowPos(hwnd,topmost?user32.HWND_TOPMOST:user32.HWND_NOTOPMOST,0,0,0,0,user32.SWP_NOMOVE|user32.SWP_NOSIZE);await log('TOPMOST = '+topmost);});\n\nlet caption=true;\nuser32.OnControl('caption',async e=>{if(e.action!=='click')return;let st=await user32.GetWindowLong(hwnd,user32.GWL_STYLE);caption=!caption;st=caption?(st|user32.WS_CAPTION):(st&(~user32.WS_CAPTION));await user32.SetWindowLong(hwnd,user32.GWL_STYLE,st);await log('WS_CAPTION = '+caption+'（按這個 client button 可再切回）');});\n\nconst HOTKEY_ID=1001;\nconst registered=await user32.RegisterHotKey(hwnd,HOTKEY_ID,user32.MOD_CONTROL|user32.MOD_SHIFT|user32.MOD_NOREPEAT,72);\nawait user32.SetControlText('hotkeyInfo',registered?'Ctrl + Shift + H：已註冊（ExOS 桌面全域）':'Ctrl + Shift + H：註冊失敗／已被占用');\nuser32.OnHotKey(HOTKEY_ID,async e=>{await log('WM_HOTKEY id='+e.id+' vk='+e.vkCode+' modifiers=0x'+Number(e.modifiers).toString(16));});\nawait log('USER32 NT compatibility demo ready.');\n"},"nt_arch_compat_demo":{"id":"nt_arch_compat_demo","fileName":"nt_arch_compat_demo.xsh","title":"NT 架構相容性測試","description":"os84 ntoskrnl/WinUI/D3D11/OLE/Shell/ADVAPI/RichEdit semantic compatibility demo","icon":"cmd","singleInstance":true,"imageFormat":"EXOS_XSH_PE_V1","machine":"EXOS_IMAGE_FILE_MACHINE_V8","subsystem":2,"subsystemName":"IMAGE_SUBSYSTEM_WINDOWS_GUI","source":"/*EXOS_XSH_PE_V1\nMachine=EXOS_IMAGE_FILE_MACHINE_V8\nSubsystem=IMAGE_SUBSYSTEM_WINDOWS_GUI\nSubsystemVersion=1.0\nEntryPoint=main\nCharacteristics=EXECUTABLE_IMAGE|SANDBOXED\nMinimumEngine=V8\n*/\nconst nt=ExOS.LoadLibrary('ntoskrnl.exe');\nconst winui=ExOS.LoadLibrary('winui.dll');\nconst d3d=ExOS.LoadLibrary('d3d11.dll');\nconst ole=ExOS.LoadLibrary('ole32.dll');\nconst shell=ExOS.LoadLibrary('shell32.dll');\nconst adv=ExOS.LoadLibrary('advapi32.dll');\nconst rich=ExOS.LoadLibrary('msftedit.dll');\n\nconst hwnd=await user32.CreateWindow({title:'NT Architecture Compatibility - os84',width:780,height:600});\nawait winui.Render(hwnd,{type:'div',id:'root',style:{display:'flex',flexDirection:'column',height:'100%',padding:'10px',gap:'8px'},children:[\n {type:'label',id:'title',text:'ExOS NT/Win32 semantic compatibility test',style:{fontSize:'17px',fontWeight:'600'}},\n {type:'groupbox',id:'group',text:'WinUI / RichEdit',style:{padding:'10px'},children:[\n  {type:'combobox',id:'combo',items:['NT Kernel','Shell','OLE','RichEdit'],selectedIndex:0,style:{width:'220px'}},\n  {type:'richedit',id:'rich',html:'<b>alpha</b><br>beta<br>gamma',style:{height:'120px',border:'1px solid #94a3b8',padding:'6px'}}\n ]},\n {type:'textarea',id:'log',readOnly:true,style:{flex:'1',width:'100%',fontFamily:'Consolas,monospace',fontSize:'11px'}}\n]});\n\nconst result={};\nresult.kernel=await nt.GetVersion();\nresult.time=await nt.KeQuerySystemTime();\nconst ev=await nt.KeInitializeEvent('SynchronizationEvent',false);\nawait nt.KeSetEvent(ev);\nresult.wait=await nt.KeWaitForSingleObject(ev,50);\nawait nt.NtClose(ev);\nresult.winui=await winui.QueryCapabilities();\nresult.d3d=await d3d.GetCompatibilityCaps();\nresult.guid=await ole.CoCreateGuid();\nconst stm=await ole.CreateStreamOnHGlobal();\nawait ole.StreamWrite(stm,new TextEncoder().encode('hello OLE'));\nawait ole.StreamSeek(stm,0,'SET');\nresult.stream=new TextDecoder().decode(await ole.StreamRead(stm,64));\nawait ole.ReleaseStream(stm);\nresult.desktop=await shell.SHGetKnownFolderPath('FOLDERID_DESKTOP');\nresult.documents=await shell.SHGetKnownFolderPath('FOLDERID_DOCUMENTS');\nconst tok=await adv.OpenProcessToken(-1,adv.TOKEN_QUERY);\nresult.token=await adv.GetTokenInformation(tok,adv.TokenUser);\nresult.acl=await adv.GetAclInformation([{type:'ALLOW',sid:'S-1-1-0',mask:0x80000000}]);\nresult.lines=await rich.GetLineCount('rich');\nresult.find=await rich.FindText('rich','beta',{matchCase:true});\nresult.richState=await rich.GetState('rich');\nawait winui.SetText('title','PASS - NT Architecture Compatibility');\nawait winui.SetProperty('combo','selectedIndex',2);\nawait user32.SetControlText('log',JSON.stringify(result,null,2));\nprocess.ntCompatResult=result;\n"}},"mediaApi":"EXOS_MEDIA_FOUNDATION_V1","comdlgApi":"EXOS_COMDLG32_V1","wininetApi":"EXOS_WININET_V1","ws2Api":"EXOS_WS2_32_V1","oleApi":"EXOS_OLE32_V2","cryptApi":"EXOS_CRYPT32_V1","richEditApi":"EXOS_RICHEDIT_V3","webViewApi":"EXOS_WEBVIEW2_V1","zipfldrApi":"EXOS_ZIPFLDR_V1"});
})(window);
